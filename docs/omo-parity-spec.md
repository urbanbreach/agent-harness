# Legacy OMO parity specification

This file is retained as background migration evidence only. It is not the product direction.

The active public contract is single-operator workflow orchestration with explicit operator-owned escalation. Any legacy OMO parity language in historical plans must be interpreted as migration context, not as a requirement to ship multiple default main agents or exact external-runtime behavior.
