Task 15 evidence (Wave 3)

Implemented:
- Minimal single-turn agent runtime in crates/harness-core/src/agent.rs
- Coordinator integration for SpawnAgent -> scheduled AgentTurn tasks
- Provider streaming events routed back via Command and persisted as:
  - provider_request_started
  - provider_stream_delta
  - provider_request_finished
- Scheduler-based provider/model concurrency queueing for agent turns

Validation:
- cargo test -p harness-core --all-features : PASS
- cargo test --workspace --all-features : PASS

New integration tests:
- coord_spawn_two_agents_respects_provider_concurrency_and_queues
- coord_agent_turn_provider_events_have_isolated_correlation_ids
