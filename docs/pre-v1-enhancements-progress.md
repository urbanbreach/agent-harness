# Pre-V1 Enhancements Progress

Status: complete. This file is the durable evidence ledger required by
`docs/pre-v1-enhancements-prd.md` §0.1 and §7. A criterion is not complete until
its row cites changed files, command output, source references, and the relevant
"breaks if" statement for new tests.

## Baseline reference citations read

| Surface | Source citations used for implementation parity |
|---|---|
| Prompt cache | `inspirations/reference cache implementation/packages/ai/src/providers/openai-prompt-cache.ts`; `inspirations/reference cache implementation/packages/ai/src/providers/openai-responses.ts`; `inspirations/reference cache implementation/packages/ai/src/providers/openai-completions.ts` |
| Codex OAuth | `inspirations/reference implementation/packages/reference implementation/src/plugin/openai/codex.ts`; `inspirations/reference implementation/packages/reference implementation/src/provider/auth.ts`; `inspirations/reference implementation/packages/reference implementation/src/auth/index.ts` |
| GitHub Copilot OAuth | `inspirations/reference implementation/packages/reference implementation/src/plugin/github-copilot/copilot.ts`; `inspirations/reference implementation/packages/reference implementation/src/plugin/github-copilot/models.ts`; `inspirations/reference implementation/packages/reference implementation/test/plugin/github-copilot-models.test.ts` |
| Onboarding/auth UX | `inspirations/reference implementation/packages/reference implementation/src/cli/cmd/run/splash.ts`; `inspirations/reference implementation/packages/reference implementation/src/cli/cmd/tui/component/dialog-provider.tsx`; `inspirations/reference implementation/packages/reference implementation/src/cli/cmd/tui/component/dialog-model.tsx`; `inspirations/reference implementation/packages/reference implementation/src/cli/cmd/account.ts` |
| Model resolution | `inspirations/model reference/packages/model-core/src/model-family-detectors.ts`; `.../model-settings-compatibility.ts`; `.../context-limit-resolver.ts`; `.../fallback-chain-from-models.ts`; `.../model-resolution-pipeline.ts` |
| Harness seams | `crates/harness-providers/AGENTS.md`; `crates/harness-core/AGENTS.md`; `crates/harness-tools/AGENTS.md`; `crates/harness-tui/AGENTS.md`; `.agent-harness/AGENTS.md` |

## Baseline implementation audit

| Workstream | Current state before this PRD implementation | Initial action |
|---|---|---|
| WS1 cache parity | `crates/harness-providers/src/openai.rs` parses cache telemetry but request structs have no `prompt_cache_key`; `CompletionRequest` has no explicit request context; `crates/harness/src/dynamic_prompt.rs` renders environment before project/skill guidance. | Add request context/cache retention and tests first. |
| WS2 model seam | `crates/harness/src/dynamic_prompt.rs` selects prompt bodies with substring checks in `provider_prompt`. | Add explicit model-resolution API and route prompt selection through it. |
| WS3 credentials | Provider config only has inline/env API-key fields; no credential store or refresh abstraction. | Add credential store/resolver with tests. |
| WS4 Codex OAuth | No Codex auth provider implementation or request decoration. | Add mocked PKCE/device/request-decoration implementation. |
| WS5 Copilot OAuth | No Copilot auth provider implementation or request decoration. | Add mocked device/public-enterprise/request-decoration implementation. |
| WS6 onboarding/auth UX | CLI has no `auth` subcommand; TUI has no first-run auth flow or mid-session auth command. | Add always-available CLI/TUI command metadata plus deterministic UI snapshots. |
| WS7 prompt parity | Non-GPT prompts are hardcoded Rust constants, not data assets. | Move family prompt bodies to `.agent-harness/prompt-families/*.md` and test drift. |
| WS8 skill hardening | Skill `resources` frontmatter is deferred metadata (`deferred_resources`), not loaded. | Load capped resources through normal skill activation and add escape tests. |

## Acceptance criteria evidence ledger

### Cache and prompts

| Criterion | Changed files | Evidence command/result | Source citation | Breaks if |
|---|---|---|---|---|
| OpenAI-compatible requests set a stable clamped per-session `prompt_cache_key` from `ProviderRequestContext`, omitted for missing session id or `cacheRetention = none`. | `crates/harness-providers/src/lib.rs`; `crates/harness-providers/src/openai.rs`; `crates/harness-core/src/agent.rs`; provider fixture updates | `cargo test -p harness-providers openai_compatible_ -- --nocapture` → 7 passed, 1 env-gated ignored; `cargo check --workspace` → passed. | reference cache implementation `openai-prompt-cache.ts`; reference cache implementation `openai-responses.ts`; reference cache implementation `openai-completions.ts`; PRD §3.5/WS1 | `openai_compatible_request_uses_stable_clamped_prompt_cache_key` breaks if request building drops, cross-contaminates, disables, or destabilizes the cache key. |
| Composed system prompt keeps volatile env fields at the prefix tail. | `crates/harness/src/dynamic_prompt.rs` | `cargo test -p harness dynamic_prompt_keeps_volatile_environment_at_stable_prefix_tail -- --nocapture` → passed; `cargo check --workspace` → passed. | PRD §3.1/WS1; harness dynamic prompt composition seam | `dynamic_prompt_keeps_volatile_environment_at_stable_prefix_tail` breaks if date/branch/workspace metadata moves ahead of stable base/project/skill text. |
| Cache read/write tokens are surfaced separately in a TUI status surface. | `crates/harness-tui/src/app.rs`; `crates/harness-tui/src/app/session_projection.rs`; `crates/harness-tui/src/app/tests.rs`; TUI fixture field updates | `cargo test -p harness-tui cache_read_write_tokens_render_as_separate_status_labels -- --nocapture` → passed; `cargo check --workspace` → passed. | `crates/harness-core/src/event.rs` cache fields; PRD WS1 | `cache_read_write_tokens_render_as_separate_status_labels` breaks if the operator-visible status stops reflecting `cache_*_tokens` or collapses read/write semantics. |
| `cacheRetention` defaults to `short`, `none` omits cache fields, `long` adds only provider-supported long-retention fields. | `crates/harness-core/src/config.rs`; `crates/harness-providers/src/lib.rs`; `crates/harness-providers/src/openai.rs`; `configs/config.json`; `configs/harness.example.jsonc`; `docs/config.md`; config/schema tests | `cargo test -p harness-core openai_compatible_cache_retention_normalizes_from_options_and_rejects_conflicts -- --nocapture` → passed; `cargo test -p harness --test config_schema_cli_test schema_cli_prints_runtime_json_schema -- --nocapture` → passed; `cargo test -p harness --test config_schema_cli_test checked_in_runtime_schema_matches_generated_schema -- --nocapture` → passed; `cargo test -p harness --test config_schema_cli_test config_validate -- --nocapture` → 27 passed; `cargo fmt --all -- --check` → passed; `cargo check --workspace` → passed. | reference cache implementation cache references; PRD §3.5; AGENTS UPDATE-TOGETHER config/docs/schema rule | `openai_compatible_long_cache_retention_is_direct_openai_only` breaks if long retention is emitted for non-direct OpenAI endpoints or omitted for direct OpenAI; the config normalization test breaks if `options.cacheRetention` stops normalizing or conflicts are accepted. |
| Model family/capability resolution uses an explicit tested seam with reference behavior depth. | `crates/harness-core/src/model_resolution.rs`; `crates/harness-core/src/config.rs`; `crates/harness-core/src/agent.rs`; `crates/harness/src/dynamic_prompt.rs`; `crates/harness/src/prompt.rs`; `crates/harness-core/tests/model_variant_resolution_test.rs`; prompt test helpers | `cargo test -p harness-core --test model_variant_resolution_test -- --nocapture` → 8 passed; `cargo test -p harness-core model_resolution -- --nocapture` → 3 passed; `cargo test -p harness provider_prompt_uses_resolved_metadata_family_not_model_substrings -- --nocapture` → passed; `cargo test -p harness provider_prompt_uses_gpt_prompt_for_gpt_models -- --nocapture` → passed; `cargo test -p harness --test prompt_cli_test -- --nocapture` → 24 passed after mock digest normalized volatile context ids; `cargo test -p harness --test bootstrap_profiles_test -- --nocapture` → 30 passed with updated volatile-tail composed prompt snapshots; `cargo test -p harness-providers request_digest_ignores_volatile_provider_context_ids -- --nocapture` → passed; `cargo fmt --all -- --check` → passed; `cargo check --workspace` → passed. | `model-family-detectors.ts`; `model-capability-heuristics.ts`; `model-settings-compatibility.ts`; `context-limit-resolver.ts`; `fallback-chain-from-models.ts`; `model-resolution-pipeline.ts`; PRD WS2 | `model_resolution_prefers_metadata_family_and_exposes_capabilities` breaks if catalog metadata no longer beats model-id heuristics or if capabilities stop carrying family/modalities/context data; `unknown_models_use_explicit_default_fallback` breaks if the fallback becomes implicit or panics. |
| Roadmap substring-heuristic/preset claim reconciled honestly. | `docs/config.md`; `docs/roadmap-v1.md`; this file | `cargo test -p harness --test config_docs_reference_test -- --nocapture` → 20 passed; roadmap boxes cite evidence commands and this progress ledger. | `docs/roadmap-v1.md`; PRD §3.2; model-core references listed above | N/A. |
| Non-GPT family prompts meet skeleton/branding-free/unsupported-tool-free bar from data assets. | `.gitignore`; `.agent-harness/prompt-families/{anthropic,gemini,kimi,trinity}.md`; `crates/harness-core/src/model_resolution.rs`; `crates/harness/src/dynamic_prompt.rs`; `crates/harness/src/doctor.rs`; `crates/harness/tests/bootstrap_profiles_test.rs`; `crates/harness/tests/config_schema_cli/02_doctor_cli_reports_shipped_orchestration_health_test.rs`; `crates/harness/tests/snapshots/v1_family_prompts/*.txt`; `docs/config.md`; `docs/roadmap-v1.md`; `docs/claim-evidence-matrix.md`; this file | `cargo test -p harness family_prompt -- --nocapture` → passed (dynamic prompt family asset drift/fallback tests); `cargo test -p harness --test bootstrap_profiles_test shipped_v1_family_prompt_assets_match_golden_snapshots -- --nocapture` → passed; `cargo test -p harness --test config_schema_cli_test prompt_family_asset -- --nocapture` → passed; `cargo test -p harness --test config_schema_cli_test doctor_cli_json_reports_resolved_route_metadata -- --nocapture` → passed. | reference prompt parity mandate in PRD §0.6/§3.4/§3.5/WS7; `model-resolution-pipeline.ts` family-selection references from the baseline citation table; harness `.agent-harness/AGENTS.md` runtime asset contract | `family_prompt_assets_are_structured_branding_free_and_tool_safe` breaks if an asset is missing/empty, loses required skeleton sections, reintroduces forbidden upstream branding markers, or claims unavailable todo tools; `family_prompt_missing_asset_falls_back_to_default_with_status_warning` and `doctor_cli_json_reports_prompt_family_asset_fallback_warning` break if missing assets stop failing closed to the default prompt with doctor/readiness warning; `shipped_v1_family_prompt_assets_match_golden_snapshots` breaks if a shipped family composed prompt drifts without snapshot review. |

### Provider auth

| Criterion | Changed files | Evidence command/result | Source citation | Breaks if |
|---|---|---|---|---|
| Credential abstraction supports stored `oauth`, stored `api_key`, `apiKeyEnv`, and inline `apiKey` precedence without a new transport. | `crates/harness-core/src/auth.rs`; `crates/harness-core/src/config.rs`; `crates/harness-core/src/lib.rs`; `crates/harness-providers/src/lib.rs`; `crates/harness-providers/src/openai.rs`; `crates/harness/src/bootstrap.rs`; `configs/config.json`; `configs/harness.example.jsonc`; `docs/config.md`; schema/config tests | `cargo test -p harness-core auth -- --nocapture` → 7 passed; `cargo test -p harness-core openai_compatible_auth_provider_normalizes_from_options_and_rejects_conflicts -- --nocapture` → passed; `cargo test -p harness-providers openai_compatible_uses_credential_source_before_static_api_key -- --nocapture` → passed; `cargo test -p harness --test config_schema_cli_test schema_cli_prints_runtime_json_schema -- --nocapture` → passed; `cargo test -p harness --test config_schema_cli_test checked_in_runtime_schema_matches_generated_schema -- --nocapture` → passed; `cargo fmt --all -- --check` → passed; `cargo check --workspace` → passed. | reference implementation `provider/auth.ts`; `auth/index.ts`; PRD §3.5; harness provider transport seam | `credential_resolution_precedence_prefers_stored_then_env_then_inline` breaks if stored OAuth/API-key no longer outrank env/inline fallback; `openai_compatible_uses_credential_source_before_static_api_key` breaks if OAuth-backed providers bypass the existing OpenAI-compatible transport or ignore the credential source. |
| Stored credentials persist outside config with restrictive permissions and support-bundle/event exclusion; auth-management deletion remains a WS6 CLI surface. | `crates/harness-core/src/auth.rs`; `crates/harness-core/Cargo.toml`; `crates/harness/src/sessions.rs`; `crates/harness/src/lib.rs`; `crates/harness/tests/replay_sessions_cli/09_sessions_export_support_scan_test.rs`; `docs/config.md` | `cargo test -p harness-core auth -- --nocapture` → 7 passed; `cargo test -p harness --test replay_sessions_cli_test sessions_export_cli_excludes_stored_credentials_and_scans_for_leaks -- --nocapture` → passed; `cargo test -p harness --test replay_sessions_cli_test sessions_export -- --nocapture` → 9 passed; reconciliation gate `cargo check -p harness-core` → passed after adding the Windows ACL path. | reference implementation `auth/index.ts`; PRD §3.5/WS3; harness support-bundle redaction invariant | `credential_store_round_trips_replaces_atomically_and_uses_restrictive_permissions` breaks if store files stop living outside config, stop atomic replacement, or lose restrictive POSIX `0600` mode; the `cfg(windows)` ACL implementation breaks if Windows credential files stop applying a protected owner-only DACL; `credential_store_manifest_excludes_secret_material` and `sessions_export_cli_excludes_stored_credentials_and_scans_for_leaks` break if support exports include stored credential material or fail to scan stored secret values. |
| Access tokens auto-refresh with single-flight behavior and existing provider error categories. | `crates/harness-core/src/auth.rs`; `crates/harness-providers/src/lib.rs`; `crates/harness-providers/src/openai.rs`; `crates/harness/src/bootstrap.rs` | `cargo test -p harness-core auth -- --nocapture` → 7 passed; `cargo test -p harness-providers openai_compatible_uses_credential_source_before_static_api_key -- --nocapture` → passed; `cargo check --workspace` → passed. | reference implementation Codex refresh logic in `plugin/openai/codex.ts`; reference implementation `provider/auth.ts`; PRD WS3 | `expired_oauth_refresh_is_single_flight_and_persisted` breaks if concurrent refreshes duplicate network/token work or fail to persist the refreshed OAuth credential; provider credential error mapping breaks if credential failures stop surfacing through credential-category provider errors. |
| Codex OAuth PKCE/device/timeout/error/account-id redaction/request decoration work. | `crates/harness-core/src/auth/codex.rs`; `crates/harness-core/src/auth.rs`; `crates/harness-core/Cargo.toml`; `crates/harness-providers/src/lib.rs`; `crates/harness-providers/src/openai.rs`; `crates/harness/src/auth_cmd.rs`; `crates/harness/src/bootstrap.rs`; `crates/harness/Cargo.toml` | `cargo test -p harness-core codex -- --nocapture` → 6 passed; `cargo test -p harness auth -- --nocapture` → passed, including `codex_browser_login_loopback_uses_cli_listener_and_stores_credential`; `cargo test -p harness-providers codex_auth_profile_rewrites_endpoint_and_adds_context_headers -- --nocapture` → passed; `cargo test -p harness-providers -- --nocapture` → 27 passed, 1 env-gated ignored, recorded tests 9 passed; `cargo check --workspace` → passed. | reference implementation `plugin/openai/codex.ts`; reference implementation `provider/auth.ts`; PRD §3.3/WS4 | `codex_pkce_verifier_and_challenge_match_s256_base64url` breaks if PKCE verifier/challenge generation drifts from S256/base64url; `codex_loopback_callback_validates_state_and_stores_tokens`, `codex_loopback_rejects_bad_state_missing_code_and_timeout_without_storing`, and `codex_browser_login_loopback_uses_cli_listener_and_stores_credential` break if callback listener binding, state, error, timeout, token-exchange shape, storage, or secret redaction regresses; `codex_device_flow_polls_pending_then_exchanges_and_stores_credential` breaks if usercode→pending poll→token exchange storage stops working; `codex_auth_profile_rewrites_endpoint_and_adds_context_headers` breaks if Codex requests stop stripping config authorization, rewriting to the Codex endpoint, carrying bearer/account/session/request headers, or preserving safe custom headers. |
| GitHub Copilot device/public-enterprise/token decision/request decoration work. | `crates/harness-core/src/auth/copilot.rs`; `crates/harness-core/src/auth.rs`; `crates/harness/src/bootstrap.rs`; `crates/harness-providers/src/openai.rs`; `docs/config.md`; this file | `cargo test -p harness-core copilot -- --nocapture` → 7 passed; `cargo test -p harness-core auth -- --nocapture` → 20 passed; `cargo test -p harness-providers github_copilot_auth_profile_rewrites_public_and_enterprise_headers -- --nocapture` → passed; `cargo test -p harness-providers -- --nocapture` → 28 passed, 1 env-gated ignored, recorded tests 9 passed; `cargo check --workspace` → passed. Recorded decision: the cited reference implementation Copilot plugin stores the GitHub device-code `access_token` as both `access` and `refresh` and sends that same value directly as the Copilot bearer; no GitHub→Copilot token exchange is used for the V1 deterministic path. | reference implementation `plugin/github-copilot/copilot.ts`; `models.ts`; `github-copilot-models.test.ts`; PRD WS5 | `copilot_device_flow_uses_direct_github_token_and_stores_public_credential` breaks if the GitHub device-code response stops storing the direct token or accidentally adds a Copilot token-exchange call; `copilot_device_poll_honors_pending_and_slow_down_safety_margin` breaks if RFC pending/slow_down interval handling or the 3s safety margin regresses; `copilot_device_errors_fail_cleanly_without_storing_credentials` and `copilot_device_timeout_stores_no_credentials` break if denied/expired/malformed/timeout paths persist credentials; `copilot_enterprise_normalizes_and_stores_domain` breaks if enterprise URL validation/domain storage/base selection changes; `credential_resolution_preserves_copilot_enterprise_url` breaks if stored Enterprise domains stop reaching provider request decoration; `github_copilot_auth_profile_rewrites_public_and_enterprise_headers` breaks if public/enterprise endpoint rewrite, bearer choice, `x-initiator`, vision, stale auth stripping, or Copilot headers regress. |
| `harness auth login/logout/list` exist, run at any time, preserve fallbacks, and never print secrets. | `crates/harness/src/auth_cmd.rs`; `crates/harness/src/lib.rs`; `crates/harness/tests/config_schema_cli/02c_config_validate_and_provider_credentials_test.rs`; `docs/config.md` | `cargo test -p harness auth -- --nocapture` → passed, including API-key stdin login, mocked OAuth replacement, JSON list redaction, and logout fallback preservation. | reference implementation account/auth command surfaces; PRD WS6 | `auth_login_list_and_logout_run_outside_onboarding_without_printing_secrets` breaks if auth becomes first-run-only, login/logout mutate config/env fallbacks, or stdout/stderr/list JSON leaks API keys, OAuth tokens, refresh tokens, account ids, or fallback env values. |
| Same auth flows are invokable mid-session from TUI command palette/slash command. | `crates/harness-tui/src/keybindings.rs`; `crates/harness-tui/src/app/session_navigation.rs`; `crates/harness-tui/src/app.rs`; `crates/harness/src/tui.rs`; `crates/harness/src/lib.rs`; `crates/harness/src/auth_cmd.rs` | `cargo test -p harness-tui auth -- --nocapture` → passed; `cargo test -p harness tui::tests::tui_auth_backend_runs_same_auth_command_and_redacts_output -- --nocapture` → passed; `cargo test -p harness auth -- --nocapture` → passed. | reference implementation dialog-provider/model references; PRD WS6 | `auth_slash_and_palette_emit_ui_intent_mid_session` breaks if `/auth`/`/login` or the `Auth` palette command disappear, stop using centralized command metadata, or stop emitting `UiIntent::OpenAuthManager`; `tui_auth_backend_runs_same_auth_command_and_redacts_output` breaks if the TUI intent stops invoking the same auth backend as CLI, stops writing stored credentials, or leaks auth secrets in operator notices. |
| First-run onboarding has reference implementation visual/behavioral parity and skips preconfigured users. | `crates/harness-tui/src/app/onboarding.rs`; `crates/harness-tui/src/ui_lifecycle.rs`; `crates/harness-tui/src/ui.rs`; `crates/harness/src/auth_cmd.rs`; `crates/harness/src/tui.rs`; `crates/harness-tui/src/lib_tests.rs` | `cargo test -p harness-tui onboarding -- --nocapture` → passed; `cargo test -p harness auth -- --nocapture` → passed for readiness predicate coverage. | reference implementation splash/dialog references and PRD §3.5 screen inventory | `onboarding_inventory_screens_render_in_startup_surface`, `onboarding_inventory_has_focus_hints_redaction_and_skill_selection`, `onboarding_skip_is_launch_local_and_writes_no_auth_intent`, and `onboarding_required_only_when_configured_auth_provider_has_no_usable_fallback` break if the screen inventory, focus/key hints, redaction, launch-local skip, or preconfigured credential/env/inline skip predicate regresses. |
| TUI skill listing/selection UX is visually aligned with reference implementation skill surface. | `crates/harness-tui/src/app/onboarding.rs`; `crates/harness-tui/src/lib_tests.rs` | `cargo test -p harness-tui onboarding -- --nocapture` → passed. | reference implementation dialog-select style references; PRD WS6 | `onboarding_inventory_has_focus_hints_redaction_and_skill_selection` breaks if the skill-selection screen stops exposing grouped skill names/descriptions/selection rows (`build`, `plan`, `explore`) with the same onboarding selection behavior. |
| Doctor reports per-provider auth status with redacted values. | `crates/harness/src/doctor.rs`; `crates/harness/src/auth_cmd.rs`; `crates/harness/tests/config_schema_cli/02c_config_validate_and_provider_credentials_test.rs` | `cargo test -p harness auth -- --nocapture` → passed. | PRD §3.4/WS6; redaction path in `crates/harness-core/src/redact.rs` | `doctor_cli_json_reports_redacted_per_provider_auth_status` breaks if doctor omits per-provider auth details, reports transport proof from a live network probe, or leaks stored account/token values. |

### Skills

| Criterion | Changed files | Evidence command/result | Source citation | Breaks if |
|---|---|---|---|---|
| Bundled skill resources load via `resources` grammar/caps within normal redaction/summary path. | `crates/harness-tools/src/skill_catalog.rs`; `.agent-harness/skills/{git-master,review-work,frontend-ui-ux}/SKILL.md`; `docs/extension-strategy.md`; `docs/starter-skills.md`; `docs/roadmap-v1.md`; `docs/claim-evidence-matrix.md` | `cargo test -p harness-tools --test skill_load_discovery_test -- --nocapture` → 26 passed; `cargo test -p harness-tools -- --nocapture` → passed (all harness-tools tests); `cargo test -p harness --test config_docs_reference_test config_docs_capture_v1_skill_contract_and_authoring_guide -- --nocapture` → passed; `cargo fmt --all -- --check` → passed; `cargo check --workspace` → passed. | PRD §3.5/WS8; `docs/extension-strategy.md`; `docs/starter-skills.md` | `v1_skill_activation_loads_bundled_resources_with_caps_and_redaction` breaks if bundled content loads unbounded, skips redaction, fails to append through the skill activation path, or stops marking truncated resources. |
| Symlink/path traversal/absolute-path escape across skill roots is rejected. | `crates/harness-tools/src/skill_catalog.rs`; `crates/harness-tools/tests/skill_load_discovery/03_v1_skill_contract_test.rs`; docs listed above | `cargo test -p harness-tools --test skill_load_discovery_test -- --nocapture` → 26 passed; `cargo test -p harness-tools --test native_tool_parity_matrix_test -- --nocapture` → 3 passed. | `crates/harness-tools/AGENTS.md`; PRD WS8 | `v1_skill_resources_reject_escape_paths_for_project_and_global_roots` breaks if `..` or absolute resource paths can load from project/global roots; `v1_skill_resources_reject_symlink_escape` breaks if a symlinked resource outside the skill root can be read. |

### Cross-cutting

| Criterion | Changed files | Evidence command/result | Source citation | Breaks if |
|---|---|---|---|---|
| Every touched roadmap box is updated honestly with a citation. | `docs/roadmap-v1.md`; `docs/claim-evidence-matrix.md`; this file | Complete: every checked enhancement roadmap box in the provider-auth, prompt-cache, model-resolution/prompt-parity, skill-hardening, and onboarding sections names a runnable evidence command plus this progress ledger/source citation; post-V1 non-goals remain unchecked. `cargo test -p harness --test config_docs_reference_test -- --nocapture` → passed. | `docs/roadmap-v1.md`; `docs/claim-evidence-matrix.md`; PRD §7 | Breaks if a checked roadmap box loses its evidence command/source citation or if a post-V1 item is checked without deterministic evidence. |
| `docs/config.md`, schemas, and examples document §3.5 public config keys without untested aliases. | `docs/config.md`; `configs/config.json`; `configs/harness.example.jsonc`; `crates/harness/tests/config_schema_cli/01_models_probe_generates_harness_catalog_fragment_test.rs`; `crates/harness/tests/config_schema_cli/02c_config_validate_and_provider_credentials_test.rs` | `cargo test -p harness --test config_schema_cli_test schema_cli_prints_runtime_json_schema -- --nocapture` → passed; `cargo test -p harness --test config_schema_cli_test checked_in_runtime_schema_matches_generated_schema -- --nocapture` → passed; `cargo test -p harness --test config_schema_cli_test config_validate -- --nocapture` → passed; `cargo test -p harness --test config_docs_reference_test -- --nocapture` → passed. Docs cover `cacheRetention`, `authProvider`, Codex/Copilot login methods, credential precedence, logout fallback preservation, doctor scope, and manual live signoff. | AGENTS UPDATE-TOGETHER table; PRD §3.5 | Cache/auth docs/schema coverage breaks if `cacheRetention` or `authProvider` disappears from the generated schema, shipped example config, docs-reference anchors, or auth CLI/doctor tests. |
| No deterministic test performs a real provider network call. | Provider/auth tests use mocked endpoints, local loopback/request builders, cassettes, or in-process fake auth backends; no live-provider env gate is required for §6.1/§6.2. | `cargo test --workspace --all-features` → passed; `scripts/test-lanes.sh simulation` → `simulation_secret_scan PASS`; provider/auth evidence rows above cite mocked Codex/Copilot/credential tests; doctor output reports `scope: local readiness only; no provider or MCP network probes`. | PRD §0.3/§6.3; `crates/harness-testkit/tests/AGENTS.md` live-env contract; provider/cassette AGENTS protocol | Breaks if a deterministic test reads live provider env (`HARNESS_LIVE_PROXY`, real OAuth secrets) or performs OpenAI/ChatGPT/GitHub network I/O outside mocked/cassette/env-gated lanes. |
| This progress doc records evidence and "breaks if" for every new test. | This file | Complete: each acceptance row above names changed files, evidence commands/results, source citations, and a `breaks if` regression statement for the new or extended tests. | PRD §0.1/§7 | Breaks if an acceptance row loses changed-file scope, command evidence, source citation, or a regression statement. |
| Live OAuth E2E is recorded as env-gated/manual, not faked or required. | `docs/config.md`; `docs/roadmap-v1.md`; this file | Manual signoff item only: real ChatGPT/GitHub browser/device login requires human-held accounts and is intentionally excluded from autonomous deterministic gates. Deterministic completion is backed by mocked PKCE/device/refresh/decoration tests; real live proof should be run later through explicit env-gated live signoff with credentials supplied by an operator. | PRD §0.4; `crates/harness-testkit/tests/AGENTS.md` live-env contract | Breaks if docs or tests claim live OAuth success without an env-gated/manual artifact, or if deterministic gates require real OAuth credentials. |

## Final gates

| Gate | Result/evidence |
|---|---|
| Targeted §6.1: `cargo test -p harness-providers` | PASS in `.omx/reports/pre-v1-final-gates/g010-targeted-gates.log` |
| Targeted §6.1: `cargo test -p harness` | PASS in `.omx/reports/pre-v1-final-gates/g010-targeted-gates.log` |
| Targeted §6.1: `cargo test -p harness-tools` and `native_tool_parity_matrix_test` | PASS in `.omx/reports/pre-v1-final-gates/g010-targeted-gates.log` |
| Targeted §6.1: `cargo test -p harness-tui` | PASS in `.omx/reports/pre-v1-final-gates/g010-targeted-gates.log` |
| Targeted §6.1: `RUST_TEST_THREADS=1 cargo test -p harness-testkit --test pty_e2e` | PASS in `.omx/reports/pre-v1-final-gates/g010-targeted-gates.log` |
| Targeted §6.1: `scripts/test-lanes.sh simulation` | PASS, including `simulation_secret_scan PASS`, artifact root `target/test-lanes/20260530-204417`, in `.omx/reports/pre-v1-final-gates/g010-targeted-gates.log` |
| Targeted §6.1: `cargo run -p harness -- --config configs/harness.example.jsonc doctor` | PASS: `12 passed, 0 warnings, 0 failures`; local-readiness/no-network scope and redacted provider credential summary in `.omx/reports/pre-v1-final-gates/g010-targeted-gates.log` |
| Workspace §6.2: `cargo fmt --all -- --check` | PASS in `.omx/reports/pre-v1-final-gates/g010-workspace-gates.log` |
| Workspace §6.2: `cargo check --workspace` | PASS in `.omx/reports/pre-v1-final-gates/g010-workspace-gates.log`; reconciliation rerun after auth/TUI fixes also passed in-session. |
| Workspace §6.2: `cargo test --workspace --all-features` | PASS in `.omx/reports/pre-v1-final-gates/g010-workspace-gates.log` |
| Workspace §6.2: `cargo clippy --workspace --all-targets --all-features -- -D warnings` | PASS in `.omx/reports/pre-v1-final-gates/g010-workspace-gates.log` |
| Final reconciliation rerun after browser-auth/TUI-auth/Windows-permission fixes | PASS in `.omx/reports/pre-v1-final-gates/g010-reconciliation-gates-20260531.log`: targeted auth/core/provider tests, `cargo fmt --all -- --check`, `cargo check --workspace`, `cargo test --workspace --all-features`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `scripts/test-lanes.sh simulation`, and `cargo run -p harness -- --config configs/harness.example.jsonc doctor`; log ends `ALL GATES PASSED`. |
| Post-cleaner verification after `ai-slop-cleaner` pass | PASS in `.omx/reports/pre-v1-final-gates/g010-post-cleaner-gates-20260531.log`: `cargo fmt --all -- --check`, `cargo check --workspace`, `cargo test -p harness-core auth -- --nocapture`, `cargo test -p harness auth -- --nocapture`, `cargo test -p harness-tui auth -- --nocapture`, and `cargo clippy --workspace --all-targets --all-features -- -D warnings`; log ends `POST-CLEANER GATES PASSED`. |


## Final reconciliation addendum (2026-05-30)

An independent audit found two blockers after the first final-gate run: Codex browser login only printed an authorize URL, and TUI mid-session auth only emitted an intent/banner. The reconciliation fix routed Codex browser auth through a real CLI-owned loopback listener and routed TUI `/auth`/palette intents through the same in-process auth backend used by `harness auth`, with operator-notice secret redaction. A conditional Windows credential-store gap was addressed by adding a `cfg(windows)` protected owner-only DACL path in addition to POSIX `0600`; the final cleanup pass then removed a direct Windows API/`unsafe` implementation and kept the DACL path behind a Windows-only `whoami`/`icacls` helper with SID parser coverage, preserving the workspace `unsafe_code = "deny"` contract. Evidence rerun in this session: `cargo test -p harness auth -- --nocapture`, `cargo test -p harness-tui auth -- --nocapture`, `cargo test -p harness-tui onboarding -- --nocapture`, `cargo test -p harness-core codex -- --nocapture`, `cargo test -p harness-providers codex_auth_profile -- --nocapture`, `cargo fmt --all -- --check`, and `cargo check --workspace` all passed; the fail-fast final reconciliation chain in `.omx/reports/pre-v1-final-gates/g010-reconciliation-gates-20260531.log` additionally passed workspace tests, clippy, simulation secret scan, and doctor. The `ai-slop-cleaner` report is `.omx/reports/pre-v1-final-gates/g010-ai-slop-cleaner-report.md`; post-cleaner verification passed in `.omx/reports/pre-v1-final-gates/g010-post-cleaner-gates-20260531.log`.

## Final audit-fix addendum (2026-05-31)

A second independent audit pass found four final correctness gaps in the auth/onboarding polish layer: backend auth output was not streamed into the live TUI, API-key onboarding could advance on intent rather than backend success, stored credential read errors could be masked by fallback credentials, and one deterministic snapshot contained trailing whitespace. The audit-fix pass changed `crates/harness/src/tui.rs`, `crates/harness/src/auth_cmd.rs`, `crates/harness/src/doctor.rs`, `crates/harness-tui/src/runtime.rs`, `crates/harness-tui/src/app.rs`, `crates/harness-tui/src/app/session_navigation.rs`, `crates/harness-tui/src/lib_tests.rs`, `crates/harness-tui/tests/deterministic_render_test.rs`, `crates/harness/tests/config_schema_cli/02c_config_validate_and_provider_credentials_test.rs`, and the command-palette snapshot. TUI auth now streams backend notices through `LiveUpdate::OperatorNotice`, startup/onboarding consumes live auth result updates, API-key onboarding sends the secret only through hidden stdin and advances only on `LiveUpdate::AuthBackendResult { success: true }`, `auth status` reports credential-store load failures as `presence: "error"`/`source: "credential_store_error"`, and doctor reports malformed/unreadable stored credentials as warnings rather than silently treating env/inline fallback as fully healthy.

Evidence: `.omx/reports/pre-v1-final-gates/g010-audit-fix-gates-20260531.log` records PASS for `cargo fmt --all -- --check`, `cargo check --workspace`, `cargo test -p harness-core auth -- --nocapture`, `cargo test -p harness auth -- --nocapture`, `cargo test -p harness-tui auth -- --nocapture`, `cargo test -p harness-tui onboarding -- --nocapture`, `cargo test -p harness --test config_docs_reference_test`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace --all-features`, `scripts/test-lanes.sh simulation` with `simulation_secret_scan PASS`, and `cargo run -p harness -- --config configs/harness.example.jsonc doctor`. The same log records an initial `git diff --check` trailing-whitespace failure followed by the post-fix PASS sequence: `cargo test -p harness-tui command_palette_renders_without_pty -- --nocapture`, `cargo fmt --all -- --check`, `git diff --check`, and `cargo clippy --workspace --all-targets --all-features -- -D warnings`.

The final architecture audit then found one remaining onboarding graph issue: the Copilot Enterprise screen existed but was not reachable from normal first-run provider selection. The fix added an explicit Copilot target-choice screen before the public/Enterprise device-code screens and a keyboard-flow regression, `onboarding_copilot_enterprise_is_reachable_and_redacts_domain`, that drives Start setup → GitHub Copilot → Enterprise, verifies blank Enterprise input emits no auth intent or public fallback, enters a hidden Enterprise domain, verifies `--enterprise-url <redacted>` in the status banner, verifies the backend receives the same `harness auth login github-copilot --method device --enterprise-url ...` args, and verifies success still waits for `apply_auth_backend_result(true)`. A CLI regression in `auth_login_list_and_logout_run_outside_onboarding_without_printing_secrets` also proves an explicitly empty `--enterprise-url` is rejected and does not store a public Copilot credential. Evidence: `.omx/reports/pre-v1-final-gates/g010-enterprise-onboarding-fix-gates-20260531.log` records PASS for `cargo fmt --all -- --check`, `cargo check --workspace`, `cargo test -p harness-tui onboarding -- --nocapture`, `cargo test -p harness-tui auth -- --nocapture`, `cargo test -p harness auth_login_list_and_logout_run_outside_onboarding_without_printing_secrets -- --nocapture`, `cargo test -p harness auth -- --nocapture`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `git diff --check`. The final post-audit gate chain in `.omx/reports/pre-v1-final-gates/g010-final-post-audit-gates-20260531.log` then passed `cargo fmt --all -- --check`, `cargo check --workspace`, `cargo test --workspace --all-features`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `scripts/test-lanes.sh simulation` with `simulation_secret_scan PASS`, `cargo run -p harness -- --config configs/harness.example.jsonc doctor`, and `git diff --check`.

Source citations: PRD §0.1/§0.3/§0.4 require deterministic proof, no secret leakage, and honest evidence; PRD WS6 requires first-run onboarding plus anytime auth CLI/TUI login UX; AGENTS invariants require deterministic replay/session inspection and redacted credential handling. Breaks if: TUI auth notices stop reflecting backend progress, onboarding advances without backend success, auth/doctor status hides credential-store corruption behind fallback credentials, deterministic tests require live provider credentials, or snapshots/support bundles contain API keys, OAuth tokens, refresh tokens, account ids, cookies, authorization codes, or trailing whitespace failures.
