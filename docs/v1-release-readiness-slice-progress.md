# V1 Release-Readiness Slice Progress

Append-only implementation log for `docs/v1-release-readiness-slice-prd.md`.

## 2026-05-29 Session Start

Notepad: `/tmp/ulw-20260529-115201.5XjZa9.md`.

### Scenario Contract

| Scenario | Surface | Binary pass condition | Test or lane |
|---|---|---|---|
| Documentation and roadmap integrity | `cargo test` docs-reference suites plus markdown docs | All V1 docs exist, local markdown links resolve, event variants/permission names/tool ids/provider categories/unsupported areas are source-derived and documented; every checked PRD/roadmap box has a matching evidence row. | `cargo test -p harness --test event_docs_reference_test`; new/extended docs-reference tests named in evidence rows |
| Prompt, skill, task, and permission contract | Prompt composition tests and native tool tests | All shipped profiles have agent-specific bodies and intent-gates where required; built-in skills load or fail disabled by stable id; task prompts contain the structured delegation template; child summaries are capped; prompt promises match coordinator-denied tools. | `cargo test -p harness <prompt tests>`; `cargo test -p harness-tools <skill/task/permission tests>` |
| Provider and session trust | Provider faux transport tests, headless/TUI surfacing tests, event-store/compaction tests | Provider failures map to stable categories and render actionable text; fallback policy is observable; interrupted resume, crash tail recovery, compaction preservation, fallback boundedness, large-session artifacts, and meaningful titles are proven. | `cargo test -p harness-providers <provider category tests>`; `cargo test -p harness-core <session/compaction tests>`; `scripts/test-lanes.sh perf` |
| TUI operator flow | TUI model/render tests and deterministic PTY lane | Prompt history persists across restarts and preserves drafts; permission overlays, model switcher, session search, diff hunk navigation, keyboard navigation, and flow states render deterministically; PTY happy path artifact is produced. | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty` |
| Release surface and first-run | CLI help/readme tests, doctor JSON, signoff-binary lane | Help text/README commands resolve; first-run provider/auth docs do not assume loopback only; doctor reports extension/roadmap readiness; outside-repo TUI startup and tool-enabled prompt artifacts exist. | `cargo test -p harness <cli/docs tests>`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json`; `HARNESS_BINARY_SMOKE=1 scripts/test-lanes.sh signoff-binary` |

### Initial Findings

| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Scenario contract before coding | Cross-cutting | progress log | source write | Contract recorded before production edits. | PASS | scenario rows removed or lose binary pass conditions |

### Honest Limitations

No PRD acceptance boxes are checked yet. Existing worktree already shows deleted legacy docs (`docs/legacy-parity-spec.md`, `docs/v1-agent-catalog-workspace-intelligence-prd.md`, `docs/v1-skill-contract-capability-governance-prd.md`, `skills-lock.json`) and an untracked PRD; these were observed before edits and will not be reverted.
## Evidence 2026-05-29 prompt-doc-skill batch
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Prompt bodies contain agent-specific seams and primary Intent Gate | WS2 | test | cargo test -p harness --test bootstrap_profiles_test | 27 passed, including prompt seam, no-duplicate-primary, prompt snapshot, and section registry tests | PASS | deleting a required seam anchor or Intent Gate route from a prompt body makes bootstrap_profiles_test fail |
| Named prompt-section registry exists | WS2 | test | cargo test -p harness --test bootstrap_profiles_test | dynamic_prompt_named_sections_are_addressable passed | PASS | removing a required registered_prompt_sections entry makes the registry test fail |
| V1 docs exist and thin docs cover source surfaces | WS1/WS4/WS7/WS10 | docs-reference test | cargo test -p harness --test config_docs_reference_test | 8 passed; missing docs, stale links, or missing source-surface anchors fail | PASS | removing docs/permissions.md, provider categories, native-tool bash safety wording, or stale-link repair makes config_docs_reference_test fail |
| Built-in skills git-master, review-work, frontend-ui-ux ship with V1 metadata and sections | WS3 | test | cargo test -p harness-tools --test skill_load_discovery_test | 16 passed, including shipped_v1_builtin_skills_have_quality_contract_and_catalog_metadata | PASS | deleting a built-in SKILL.md or adding a non-real review route makes skill_load_discovery_test fail |
## Evidence 2026-05-29 WS4 provider taxonomy
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Stable provider error enum and faux-response mapping cover every category | WS4 | provider tests | `cargo test -p harness-providers` | 22 provider unit tests passed, 1 live proxy test ignored by env gate, 1 schema test passed, 9 cassette/recorded tests passed; `openai_non_success_responses_map_to_stable_error_categories` and `openai_malformed_stream_and_transport_failures_have_stable_categories` assert all eight categories. | PASS | removing `ProviderErrorCategory`, a mapping arm, or category/remediation on generated errors makes the provider tests fail |
| Provider error category/remediation are durable event metadata | WS4 | coordinator test | `cargo test -p harness-core --test coord_test records_provider_error_events_and_fails_agent_turn` | 1 passed; `ProviderRequestFinished.metadata.provider_error_category` records `rate_limited` and a non-empty remediation while the turn fails. | PASS | dropping category/remediation propagation in `agent.rs` or `ProviderRequestFinishedMetadata` makes the coord test fail |
| Headless prompt and TUI expose categorized provider failures | WS4 | surface tests | `cargo test -p harness --test prompt_cli_test prompt_cli_surfaces_provider_error_categories_in_stderr_and_events`; `cargo test -p harness-tui provider_error_categories_surface_in_tui_activity_and_runtime_state` | Both passed; headless stderr and events show `missing_credentials`, `rate_limited`, and `context_window_exceeded`; TUI activity/runtime state shows the category, provider message, and remediation. | PASS | removing category text from stderr/events/TUI activity/runtime state makes the surface tests fail |
| Provider support matrix documents execution path, fallback/no-fallback policy, credentials, limits, and every category | WS4 | docs-reference test | `cargo test -p harness --test config_docs_reference_test v1_release_docs_cover_permissions_extension_privacy_migration_and_provider_support` | 1 passed; docs-reference iterates `ProviderErrorCategory::ALL` and asserts both variant names and serialized values appear in `docs/provider-support.md`. | PASS | adding/removing a category or deleting fallback/support-matrix wording without doc updates makes the docs-reference test fail |

## Now
- Moving to WS5 session/resume/compaction trust gaps.

## Evidence 2026-05-29 WS5 meaningful titles
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Session list and resume surfaces show non-empty meaningful titles | WS5 | CLI/TUI tests | `cargo test -p harness render_human_session_table_surfaces_meaningful_session_title`; `cargo test -p harness-tui resume_history_surface_uses_meaningful_session_title` | Both targeted tests passed; CLI list table renders `map chat renderers` in `run_name`, and the TUI `/resume` history overlay renders/selects that same title before emitting `ContinueSession`. LSP diagnostics for `crates/harness/src/sessions.rs` and `crates/harness-tui/src/app/tests.rs` were clean. | PASS | degrading titled sessions to `<unavailable>`, only run ids/paths, or omitting title text from the resume picker makes the tests fail |

## Evidence 2026-05-29 WS5 crash-tail recovery
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Event store repairs a truncated final JSONL write without losing complete prior events | WS5 | red/green store test + docs | Red: `cargo test -p harness-core --lib jsonl_open_repairs_truncated_final_jsonl_line_and_preserves_complete_events` failed with `InvalidJsonLine { line: 3, EOF while parsing an object }`; Green: same command passed after repair. Also ran `cargo test -p harness-core --lib replay_reports_invalid_json_lines_deterministically` and `cargo test -p harness --test config_docs_reference_test v1_release_docs_cover_permissions_extension_privacy_migration_and_provider_support`. | Crash-tail test passed; complete invalid JSON remains fail-closed; docs-reference test passed. `docs/architecture.md` and `docs/sessions-and-replay.md` now state the final-line-only repair boundary and no-provider/tool/hook execution guarantee. LSP found no errors/warnings for `store.rs` (only an inactive `#[cfg(not(target_os = "linux"))]` hint). | PASS | removing tail truncation/newline normalization, accepting terminated invalid JSON, or deleting the documented recovery boundary makes the focused tests/docs evidence fail |

## Evidence 2026-05-29 WS5 lineage docs
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Fork/clone lineage docs cover summary, artifact, restored-context, and source-cutoff semantics | WS5 | red/green docs-reference test | Red: `cargo test -p harness --test config_docs_reference_test sessions_docs_cover_lineage_source_cutoff_summary_and_artifact_semantics` failed before docs expansion on missing `fork materializes an explicitly validated stable prefix`; Green: same command passed after expanding `docs/sessions-and-replay.md`. Also ran `cargo test -p harness --test config_docs_reference_test thin_v1_docs_cover_their_source_surfaces`; LSP diagnostics for `crates/harness/tests/config_docs_reference_test.rs` were clean. | PASS | removing implementation anchors (`source_cutoff_seq`, artifact byte/digest validation, event rewrite policy) from docs or source makes the docs-reference test fail |

## Evidence 2026-05-29 WS5 bounded compaction fallback
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Deterministic compaction fallback is bounded and user-visible | WS5 | red/green coordinator + TUI tests + docs | Red: `cargo test -p harness-core --test coord_test model_backed_compaction_falls_back_for_invalid_summary_and_records_metadata` failed before surfacing metadata with `left: Null`, `right: "model_backed_deterministic_fallback"`. Green: same command passed after `CompactionWrittenEvent.summary_source` was populated. Also ran `cargo test -p harness-core --test coord_test compaction_no_loop_guards_cover_pre_prompt_overflow_and_failed_response`, `cargo test -p harness-tui compaction_written_status_surfaces_deterministic_fallback`, `cargo test -p harness --test event_docs_reference_test`, and `cargo test -p harness --test config_docs_reference_test sessions_docs_cover_lineage_source_cutoff_summary_and_artifact_semantics`. | PASS | dropping `summary_source`, hiding deterministic fallback text in the TUI status, removing no-loop guards, or deleting the documented fallback policy makes the focused tests/docs evidence fail |

## Evidence 2026-05-29 WS5 compaction preservation
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Compaction contracts are documented and checkpoints preserve file, tool, skill, todo, and plan context | WS5 | red/green core test + docs-reference guard | Red: `cargo test -p harness-core --lib compaction_preserves_file_tool_skill_todo_and_plan_context` failed before non-file tool context facts with missing `tool bash`. Green: same command passed after `add_tool_operation_fact` preserved generic tool operations. Also ran `cargo test -p harness-core --lib operational_memory_records_read_and_modified_files_from_events`, `cargo test -p harness --test config_docs_reference_test architecture_docs_cover_compaction_contracts_and_preservation_context`, and LSP diagnostics for `provider_context.rs`, `coord/tests.rs`, and `config_docs_reference_test.rs`. | PASS | removing file facts, generic tool facts, skill/todo/plan operation facts, or the threshold/recent-turn/restoration docs anchors makes the focused tests/docs evidence fail |

## Evidence 2026-05-29 WS5 resume acceptance
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Resume acceptance restores a realistic interrupted coding session and continues | WS5 | red/green coordinator test + docs-reference guard | Red: `cargo test -p harness-core --test coord_test resume_acceptance_restores_realistic_interrupted_session_and_continues` failed before the projection preserved resumed artifacts with `assertion failed: plan_after_resume.session_artifacts.contains_key(artifact_path)`. Green: same focused test passed after resume `RunStarted` kept durable historical tool calls, completed tasks, and session artifacts. Red docs guard: `cargo test -p harness --test config_docs_reference_test sessions_docs_cover_resume_acceptance_realistic_interrupted_session` failed until `docs/sessions-and-replay.md` documented the scenario anchors. Green: same docs guard passed. | PASS | clearing durable resume projection state, losing artifact/tool history, failing to restore the shell grant, omitting skill/todo/plan/tool context from the provider turn, rejecting the post-resume turn, or deleting the documented scenario anchors makes the focused tests fail |

## Evidence 2026-05-29 WS5 large-session perf artifacts
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Large-session list/reopen/search behavior is measured and writes artifacts with provenance | WS5 | perf lane + artifact | `cargo fmt --all -- --check`; `env HARNESS_PERF_ARTIFACT_DIR=/tmp/harness-perf-focused cargo test -p harness --test perf_sessions_surface_test perf_large_session_list_reopen_and_session_search_write_artifact`; `cargo test -p harness --test test_lanes_script_test test_lanes_exports_artifact_dir_for_performance_stage`; `scripts/test-lanes.sh perf` | All commands passed. Perf lane summary at `target/test-lanes/20260529-113237/summary.txt` reports `perf nextest_perf PASS command_exit_zero`. Artifact `target/test-lanes/20260529-113237/perf/stages/nextest_perf/artifacts/large-session-surfaces.json` uses schema `harness-large-session-perf-v1`, corpus 120 sessions, 6 turns/session, 3,960 events, list 85ms/120 returned, reopen 82ms for `run_perf_large_119`, search 211ms/1,560 total matches/200 returned/120 searched sessions, with `HARNESS_PERF_ARTIFACT_DIR` provenance. LSP diagnostics for `crates/harness/tests/perf_sessions_surface_test.rs` and `crates/harness/tests/test_lanes_script_test.rs` were clean. | PASS | removing the perf lane artifact dir export, deleting the large-session test, changing session reopen to non-resumable, hiding `session_search` spill artifacts, or omitting corpus/timing/provenance fields makes the focused test or perf lane fail |

## Now
- Continue WS7+ gaps after WS6 tool/permission evidence is recorded and checkmarks are synchronized.

## Evidence 2026-05-29 WS6 tool and permission depth
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Read-only and recursion-denied profiles reject edit/bash/task/MCP attempts through the coordinator | WS6 | red/green native tool test | Red: `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test restricted_profiles_reject_edit_bash_task_and_mcp_calls` failed while the new event assertion was too narrow; Green: same focused test passed after asserting the coordinator denial surfaces directly. | PASS | adding `edit`, `bash`, `task`, or `mcp.fixture.tool.call` to restricted profiles without matching permission denial makes the focused test fail |
| Prompt permission promises match shipped runtime toolsets and policy for Plan, Explore, General, and categories | WS6 | prompt/bootstrap fixture | `cargo test -p harness --test bootstrap_profiles_test` | 28 passed, including `shipped_profile_permission_promises_match_runtime_policy_and_toolsets` and the prompt snapshot. Plan production edits are denied while plan-file edits are allowed; Plan task docs expose Explore only; Explore/General/category denied tools match their prompt claims. | PASS | deleting prompt restriction anchors, broadening Plan delegation, allowing Explore write/network/redelegation tools, or allowing category `task` makes the bootstrap fixture fail |
| Native tool catalog rows agree with registry permission metadata, aliases, schema status, and replay/artifact text | WS6 | catalog parity test | Red: `cargo test -p harness-tools --test native_tool_parity_matrix_test native_tool_catalog_rows_include_permission_alias_and_replay_metadata` failed before alias and permission drift were corrected; Green: `cargo test -p harness-tools --test native_tool_parity_matrix_test` passed. | PASS | adding a tool without a doc row, dropping runtime alias notes, or documenting the wrong permission makes the parity test fail |
| Bash safety guidance and the pre-G005 AST-grep replace deferral were honest for the earlier WS6 slice | WS6 | constants/deferral test + CLI surface | Historical 2026-05-29 row: the old guard passed while `ast_grep_replace` was still deferred. It is superseded by the 2026-05-30 G005 evidence rows below, which register the tool only after edit-safety gates. | SUPERSEDED | changing `DEFAULT_SHELL_TIMEOUT_MS`, output caps, or blocked command guidance without synchronized docs still makes the current parity guard fail |

## Evidence 2026-05-29 WS7 release evidence and claim integrity
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Claim-evidence matrix maps maintained README release-claim phrases to backing rows | WS7 | red/green docs-reference test | Red: `cargo test -p harness --test config_docs_reference_test readme_release_claim_phrases_have_claim_evidence_rows` failed on missing `first source build`; Green: same command passed after adding exact phrase-backed rows. | PASS | removing a maintained phrase row, its evidence pointer, verification command, observed result, provenance, or PASS/LIMITATION status makes the drift guard fail |
| Release blocker taxonomy maps blocker categories to real lane names | WS7 | red/green lane docs test | Red: `cargo test -p harness --test test_lanes_script_test release_blocker_taxonomy_maps_categories_to_real_lanes` failed because `docs` had no declared lane; Green: same test passed after mapping docs/provider/evidence rows to script-declared lanes. | PASS | deleting a category, removing all lane names from a category, or documenting a lane not declared by `scripts/test-lanes.sh` makes the guard fail |
| Perf budget artifacts fail closed on missing/stale evidence and pass on fresh lane artifacts | WS7 | lane + script + artifact | `python3 scripts/check-perf-artifacts.py --artifact-dir /tmp/harness-ws7-missing-perf-artifacts` returned failure as expected; a synthetic stale `large-session-surfaces.json` returned failure containing `stale`; `env HARNESS_PERF_ARTIFACT_DIR=/tmp/harness-ws7-fresh-perf-artifacts cargo test -p harness --test perf_sessions_surface_test perf_large_session_list_reopen_and_session_search_write_artifact && python3 scripts/check-perf-artifacts.py --artifact-dir /tmp/harness-ws7-fresh-perf-artifacts`; `scripts/test-lanes.sh perf` | Perf lane summary `target/test-lanes/20260529-123553/summary.txt` reports `perf nextest_perf PASS` and `perf perf_artifact_freshness PASS`; artifact `target/test-lanes/20260529-123553/perf/stages/nextest_perf/artifacts/large-session-surfaces.json` records schema `harness-large-session-perf-v1`, 120 sessions, 3,960 events, list 100ms, reopen 93ms, search 233ms, and lane provenance. | PASS | deleting `large-session-surfaces.json`, aging its timestamp, changing its schema, dropping timings, or omitting lane provenance makes `perf_artifact_freshness` fail |
| Doctor reports roadmap/extension readiness separately from runtime health | WS7/G009 | red/green CLI test | Red: `cargo test -p harness --test config_schema_cli_test doctor_cli_json_reports_extension_roadmap_readiness_separately` failed before the check existed; Green: same test passed after adding `extension_roadmap_readiness` and `runtime_effects_scope: "descriptor_only"`. | PASS | removing the doctor check, merging it into runtime health, dropping `separate_from_runtime_health`, losing descriptor-only runtime-effects scope, or losing final-slice/post-V1 seam details makes the test fail |
| Outside-repo smoke runs TUI startup and tool-enabled mock path with artifacts | WS7 | env-gated signoff lane | `env HARNESS_BINARY_SMOKE=1 HARNESS_BINARY_SMOKE_ARTIFACT_DIR=/tmp/harness-binary-smoke-artifacts-pty-final-20260530001133 cargo test -p harness --test binary_smoke -- --ignored --exact`; `scripts/test-lanes.sh signoff-binary` | Focused smoke passed and wrote `/tmp/harness-binary-smoke-artifacts-pty-final-20260530001133/tui-startup.status.txt` with `success=true`. Signoff summary `target/test-lanes/20260530-000459/summary.txt` reports `signoff-binary harness_binary_smoke PASS`; artifacts under `target/test-lanes/20260530-000459/signoff-binary/stages/harness_binary_smoke/artifacts/` include `tui-startup.{stdout,stderr,status}.txt` with `success=true`, `tool-prompt.{stdout,stderr,status}.txt`, and `tool-prompt.events.jsonl` with an `edit` tool call against `demo.txt`. | PASS | dropping `HARNESS_BINARY_SMOKE_ARTIFACT_DIR`, regressing PTY-backed `tui --mock --exit-on-finish`, skipping deterministic `golden_path`, or omitting tool-call event assertions makes the smoke fail |
| No deterministic lane requires a live provider; the static guard flags live coupling | WS7 | lane + temporary guard probe | `python3 scripts/check-test-suite-gates.py`; `python3 scripts/check-test-suite-gates.py --self-test`; `scripts/test-lanes.sh quality-gates`; temporary `crates/harness/tests/live_provider_env_guard_temp_test.rs` containing `HARNESS_LIVE_PROXY` checked with `--gate live-provider-env` then deleted | Quality-gates summary `target/test-lanes/20260529-130106/summary.txt` reports `quality-gates static_test_suite_gates PASS` and `quality-gates forbidden_branding PASS`. The temporary deterministic test probe failed with `[live-provider-env] crates/harness/tests/live_provider_env_guard_temp_test.rs:1 deterministic tests must not require HARNESS_LIVE_PROXY live provider env`, proving the guard flags live coupling. | PASS | removing `live-provider-env` from `GATES`, deleting the self-test fixture, allowing oversized focused tests, or reintroducing convention debt makes the static gate or probe fail |

## Evidence 2026-05-29 WS10 CLI surface and first-run
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| CLI help text has non-empty, non-placeholder command descriptions for the V1 command tree | WS10 | CLI help test | `cargo test -p harness --test config_schema_cli_test cli_help_lists_non_placeholder_command_descriptions` | 1 passed; root, `config`, `sessions`, and `models` help command rows all expose meaningful descriptions and reject blank/TODO/TBD/placeholder text. | PASS | removing a `clap` command description from root/config/sessions/models help, or replacing it with placeholder wording, makes the focused test fail |
| README command references resolve to real V1 CLI subcommands | WS10 | README audit test | `cargo test -p harness --test config_schema_cli_test readme_command_audit_resolves_to_real_subcommands` | 1 passed; required quick-start/support commands (`config validate`, `doctor`, `prompt`, and session inspect/replay/export/tree/fork/clone) are present and every parsed `harness ...` README reference maps to the known command tree. | PASS | adding a fake `harness frobnicate` README command, removing a required documented command, or renaming a command without updating the audit makes the test fail |
| First-run provider/auth docs explain real-provider setup without assuming loopback only | WS10 | docs-reference test | `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | 1 passed after adding README and config-guide anchors for real provider first run, `baseURL`, `apiKeyEnv`, `OPENAI_API_KEY`, loopback as only one option, and the `doctor` limitation that live prompt/signoff-live proves transport/auth. | PASS | deleting the real-provider first-run section, the documented credential config keys, or the `doctor` limitation text makes the docs-reference test fail |

## Evidence 2026-05-29 WS2 model-tuning stance and seam map
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| V1 model prompt tuning stance is explicit and avoids substring prompt heuristics | WS2 | docs-reference test | `cargo test -p harness --test config_docs_reference_test model_prompt_tuning_stance_is_documented_for_v1` | 1 passed; `docs/config.md` states per-model prompt tuning is intentionally absent for V1, prompt composition remains in `dynamic_prompt.rs` plus markdown assets, model differences use explicit `variants` metadata, and any future prompt presets require golden prompt tests. | PASS | deleting the V1 model prompt tuning stance, reintroducing substring prompt selection as the documented seam, or omitting the golden-test requirement makes the focused test fail |
| Adopted reference prompt patterns map to real Harness seams or explicit deferrals | WS2 | docs-reference test | `cargo test -p harness --test config_docs_reference_test reference_prompt_patterns_map_to_harness_seams` | 1 passed; `docs/architecture.md` now maps intent-gate, delegation reminder, category prompt appends, markdown skills, disableable built-ins, and command/hook lifecycle maps to concrete Harness seams or final-slice/post-V1 deferrals. | PASS | removing a required pattern row, dropping its real seam anchor, or claiming copied source architecture instead of user-observable Harness behavior makes the focused test fail |

## Evidence 2026-05-29 WS1 documentation completeness and accuracy
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Architecture docs mention every public `EventV1` variant from source | WS1 | source-derived docs-reference test | `cargo test -p harness --test event_docs_reference_test` | 1 passed; `architecture_event_docs_match_event_v1_variants` derives variants from `crates/harness-core/src/event.rs` and confirms `docs/architecture.md` documents each public event variant. | PASS | adding a public `EventV1` variant without documenting it, or deleting a documented event name from `docs/architecture.md`, makes the event docs test fail |
| V1 permissions, extension, privacy, migration, and provider docs exist with required source-derived anchors | WS1 | docs-reference test | `cargo test -p harness --test config_docs_reference_test` | 15 passed; `v1_release_docs_cover_permissions_extension_privacy_migration_and_provider_support` checks all eight public permission names, not-a-sandbox/runtime-vs-behavioral wording, deferred extension seams, privacy/local-data topics, migration non-goals, and provider category coverage. | PASS | removing a required V1 doc, permission name, threat-model phrase, deferred seam, privacy topic, migration non-goal, or provider category makes the docs-reference suite fail |
| Thin V1 docs cover their full source surfaces and local docs links avoid deleted targets | WS1 | docs-reference/link-integrity tests | `cargo test -p harness --test config_docs_reference_test` | 15 passed; `thin_v1_docs_cover_their_source_surfaces` covers agents/categories, session/replay/lineage commands, native-tool bash/AST-grep honesty, and troubleshooting topics; `docs_do_not_reference_broken_local_markdown_targets_or_deleted_prd_artifacts` rejects broken local markdown targets and deleted PRD/lockfile artifacts. | PASS | deleting source-surface anchors from thin docs, adding a local markdown link to a missing target, or reintroducing references to the deleted PRD/lock artifacts makes the docs-reference suite fail |

## Evidence 2026-05-29 WS9 task delegation contract
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Structured delegation body is present in the `task` tool contract and schema | WS9 | red/green tool schema test | Red: `cargo test -p harness-tools --lib task_and_background_output_descriptions_prefer_explicit_retrieval` failed before `goal` and the remaining structured fields were documented. Green: same command passed after the `task` description and `prompt` schema documented `context`, `goal`, `downstream use`, `request`, `required tools`, `must-do`, and `must-not-do`. | PASS | deleting any structured field from the tool description or prompt schema makes the focused test fail |
| Child summaries are capped and structured for parent-visible sync/background results | WS9 | red/green native delegation fixture | Red: `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test task_delegation_fixture_covers_structured_prompt_lineage_and_summary_cap` failed while oversized summaries were returned raw, then while `background_output` lacked `mode`/`child_summary`. Green: same focused fixture passed after `result_summary`/`failure_summary` were redacted and capped at 1,200 chars with structured `child_summary` metadata. | PASS | removing the cap, `child_summary`, or `background_output` summary fields makes the fixture fail |
| Delegation fixture covers skill content, category prompt append, lineage, sync/background behavior, and summary capping | WS9 | deterministic native integration fixture | `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test task_delegation_fixture_covers_structured_prompt_lineage_and_summary_cap` | 1 passed; fixture provider recorded both child prompts, verifying injected `ws9-skill` content, `visual-engineering` prompt append, seven structured prompt fields, parent/child lineage for sync and background paths, background retrieval through `background_output`, and capped structured summaries. | PASS | deleting skill injection, category prompt resolution, lineage metadata, background retrieval, or summary capping makes the fixture fail |

## Evidence 2026-05-29 WS3 built-in skills and disablement
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Built-in `git-master`, `review-work`, and `frontend-ui-ux` skills have V1 frontmatter, non-empty template sections, and real Harness route references | WS3 | skill catalog + referential-integrity tests | `cargo test -p harness-tools --test skill_load_discovery_test` | 18 passed. `shipped_v1_builtin_skills_have_quality_contract_and_catalog_metadata` asserts required frontmatter, non-empty quality-template sections, loadable project-scope catalog metadata, and real review-work route anchors; `review_work_references_only_shipped_agent_catalog_routes` extracts `category=` / `subagent_type=` references and checks them against shipped AgentCatalog route constants. | PASS | deleting a built-in `SKILL.md`, hollowing a required section, removing V1 frontmatter, or adding a fake `task(category=...)` / `subagent_type=...` reference makes the skill discovery suite fail |
| Each built-in loads through `skill`, injects through `task(load_skills=[...])`, and stable-id disablement blocks both skill activation and child spawn | WS3 | tool activation + native task tests | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test task_tool_injects_shipped_builtin_skill_bodies_into_child_prompt`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test task_tool_rejects_disabled_shipped_builtin_skill_before_child_spawn` | Skill tool activation exposed distinctive body markers for all three built-ins while metadata stayed compact (`body_loaded: false`). The task injection fixture copied the shipped built-ins into a workspace, loaded all three, and observed their body markers in the child provider prompt. The disabled built-in fixture set `skills.disabled = ["skill:project:git-master"]`, failed before spawning `general`, and reported `disabled by skills.disabled`. | PASS | disabling support by stable id, removing skill activation, dropping task skill injection, leaking body content in metadata, or spawning a child despite disabled `git-master` makes the focused tests fail |
| Doctor JSON and docs expose the built-in skill contract, disabled status, capability map, and per-skill use/do-not-use guidance | WS3 | doctor surface + source-derived docs tests | `cargo test -p harness --test config_schema_cli_test doctor_cli_json_reports_resolved_route_metadata`; `cargo test -p harness --test config_schema_cli_test doctor_cli_json_reports_disabled_builtin_skill_metadata`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | Doctor JSON lists `frontend-ui-ux`, `git-master`, and `review-work` with `skill:project:*` stable ids, `status: loadable`, `source_scope: project`, and `body_loaded: false`; the disabled built-in doctor test reports `git-master` as `disabled` with reason `disabled by skills.disabled`. The docs test derives the three built-ins from the skill catalog and checks `docs/extension-strategy.md` capability rows plus `docs/starter-skills.md` use-when / do-not-use-when rows and disablement guidance. | PASS | removing a built-in from doctor readiness, deleting disabled status/reason, removing the capability-map row, or omitting per-skill use/do-not-use docs makes the focused tests fail |

## Evidence 2026-05-29 WS2 prompt-depth closeout
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| All 12 shipped prompt bodies are agent-specific and primary prompts keep a named Intent Gate | WS2 | prompt manifest fixture | `cargo test -p harness --test bootstrap_profiles_test` | Existing seam tests still assert every shipped profile's distinctive Harness anchors, primary `## Intent Gate` route text, non-duplicate primary Behavioral Guidance + Operating Loop sections, skeleton sections, runtime permission promises, and `v1_prompt_assets.json` metadata. | PASS | deleting a required seam anchor, removing a primary Intent Gate route, making two primary guidance/loop sections identical, or hollowing a required skeleton section makes `bootstrap_profiles_test` fail |
| Named dynamic prompt sections render in isolation with committed golden text | WS2 | red/green golden snapshot test | Red: `cargo test -p harness --test bootstrap_profiles_test shipped_v1_prompt_section_snapshots_match_source` failed on missing `v1_prompt_sections/base_model.txt`. Green: same test passed after generating `base_model`, `environment`, `delegation_reminder`, `project_instructions`, `skill_guidance`, and `intent_gate` snapshots. | The snapshot directory contains exactly 6 non-empty `.txt` files derived from `dynamic_prompt::registered_prompt_sections()`. LSP diagnostics for `crates/harness/src/dynamic_prompt.rs` and `crates/harness/tests/bootstrap_profiles_test.rs` were clean. | PASS | removing a registered section, changing rendered section text without updating the golden, or leaving a section snapshot missing/empty makes the focused test fail |
| Full composed prompt golden snapshots cover all shipped and hidden profiles | WS2 | red/green composed prompt fixture | Red: `cargo test -p harness --test bootstrap_profiles_test shipped_v1_full_composed_prompt_snapshots_match_source` failed on missing `v1_composed_prompts/build.txt`. Green: same test passed after generating one normalized full-prompt `.txt` snapshot per shipped/hidden profile; full `cargo test -p harness --test bootstrap_profiles_test` passed 30 tests; `cargo fmt --all -- --check` passed. | The composed snapshot directory contains exactly 15 non-empty files: `build`, `plan`, `general`, `explore`, all 8 category profiles, and hidden `title`, `summary`, `compaction`. Dynamic environment/path lines are normalized to a fixed V1 snapshot environment while preserving real prompt text. | PASS | deleting a shipped or hidden profile, dropping `title`/`summary`/`compaction` coverage, removing AGENTS/instruction composition, or changing composed prompt text without updating the golden makes the focused test fail |

## Evidence 2026-05-29 WS8 durable prompt history
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Prompt history persists across restarts and restores the draft after history recall | WS8 | red/green TUI app-model test + docs + terminal smoke | Red: `cargo test -p harness-tui prompt_history_persists_and_restores_draft_after_recall` failed before the durable constructors existed. Green: same focused test passed after adding `<session-dir>/tui/prompt-history.json` loading/saving and draft restoration when navigating past the newest history entry; `cargo test -p harness-tui startup_auto_submit_persists_prompt_history_once` guards startup-to-live handoff against duplicate persisted entries. | The focused test submits `persisted prompt`, observes `prompt-history.json`, recreates startup state from the same path, recalls history with Up, then Down restores the unsent `draft text` and cursor. Full `cargo test -p harness-tui` passed 614 unit/lib tests plus deterministic/model/PTY/keybinding suites. Manual terminal smoke launched `target/debug/harness tui --mock --session-dir /tmp/harness-tui-history-qa-20260529b`, submitted `manual history qa once`, quit through the command palette, and observed one prompt in `/tmp/harness-tui-history-qa-20260529b/tui/prompt-history.json`. `docs/config.md` documents the runtime state file location. | PASS | removing prompt-history load/save wiring, changing the state-file path, duplicating startup auto-submit history, or clearing the in-progress draft when leaving history recall makes the focused test fail |

## Evidence 2026-05-29 WS8 permission overlay clarity
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Permission overlay renders shortcuts, one-shot/session scope, and timeout countdown state | WS8 | red/green deterministic render test + snapshot | Red: `cargo test -p harness-tui --test deterministic_render_test permission_modal_preserves_draft_without_pty` failed before the overlay rendered `once=one-shot`. Green: same focused test passed after `permission_modal_metadata_line` included `scopes once=one-shot always=session` and `timeout 30s countdown`. Also ran `cargo test -p harness-tui permission_modal_snapshot_renders_request`; both snapshot variants passed after accepting the intentional metadata text wrapping. | The deterministic render output contains `Allow once`, `Allow always`, `Reject`, `once=one-shot`, `always=session`, and `countdown` while preserving the draft prompt. The committed snapshot now renders the permission call metadata with scope and timeout/countdown text. LSP diagnostics for `crates/harness-tui/src/ui_overlays.rs` and `crates/harness-tui/tests/deterministic_render_test.rs` were clean. | PASS | removing the scope labels, timeout countdown wording, shortcuts, or draft preservation makes the focused render test or permission snapshot fail |

## Evidence 2026-05-29 WS8 deterministic PTY happy path
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Scripted deterministic PTY happy-path artifact records start, prompt, permission approval, tool call, edit, resume picker, and quit | WS8 | red/green PTY lane + artifact | Red: `env RUST_TEST_THREADS=1 HARNESS_TUI_HAPPY_PATH_ARTIFACT_DIR=/tmp/harness-work/harness-tui-happy-path-artifacts cargo test -p harness --test pty_happy_path_recorded -- --ignored --exact scripted_tui_happy_path_records_start_prompt_permission_tool_edit_resume_and_quit` timed out before the scenario exited after `ready for next turn`; then timed out on stale slash/resume markers. Green: the same focused PTY test passed after the live event forwarder stopped after forwarding terminal events and the test synchronized to current UI markers. Final lane: `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`. | Signoff summary `target/test-lanes/20260529-161412/summary.txt` reports `PASS=3 FAIL=0`, including `signoff-pty harness_tui_happy_path_pty PASS`. Artifact `target/test-lanes/20260529-161412/signoff-pty/stages/harness_tui_happy_path_pty/artifacts/tui-happy-path-recording.json` has schema `harness-tui-pty-happy-path-v1`, lane `signoff-pty`, command `scripts/test-lanes.sh signoff-pty`, timestamp/env provenance, and covered steps `start`, `prompt`, `permission approval`, `tool call`, `edit`, `resume picker`, `quit`. `scenario.events.jsonl` contains `permission_requested`, `permission_resolved`, `tool_call_requested`, `edit_applied`, `tool_call_finished`, and `run_finished`; `prompt.events.jsonl` contains `user_message_submitted` for `Hello from PTY`. | PASS | removing `harness_tui_happy_path_pty` from `scripts/test-lanes.sh`, dropping the PTY test assertions/artifact writes, regressing scenario `--exit-on-finish`, or omitting the manifest provenance/covered steps makes the focused test or signoff lane fail |

## Evidence 2026-05-29 WS8 model switcher status
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Model switcher is provider-grouped, searchable, with visible fallback/error status | WS8 | red/green render test + manual TUI QA | Red: `cargo test -p harness-tui --test model_switcher_metadata_test model_switcher_renders_fallback_error_status` failed before the switcher rendered fallback/error policy text. Green: same focused test passed; `cargo test -p harness-tui --test model_switcher_metadata_test` passed 16 tests; `cargo test -p harness-tui provider_error_categories_surface_in_tui_activity_and_runtime_state` passed; `cargo fmt --all -- --check` passed; LSP diagnostics for `crates/harness-tui/src/ui_overlays.rs` and `crates/harness-tui/tests/model_switcher/palette_test.rs` were clean. Manual QA launched `cargo run -p harness -- tui --mock --session-dir /tmp/harness-model-switcher-qa-2`, opened `/model`, typed `mock`, and observed the filtered model row plus `No automatic model fallback; provider errors stay visible`. | PASS | removing provider category rows, breaking provider-title filtering, deleting `model_switcher_renders_fallback_error_status`, or moving the fallback/error status below the compact TUI viewport makes the model-switcher test or manual QA fail |

## Evidence 2026-05-29 WS8 session search filtering
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Session search supports fielded metadata filtering and fuzzy title/run-id filtering | WS8 | red/green app-model test + manual TUI QA | Red: `cargo test -p harness-tui session_history_filter_matches_visible_fields_and_fuzzy_title` failed while `gpt-5` metadata search returned no rows. Green: same focused test passed after the picker matched provider/model/profile/status/date/readiness fields and fuzzy title/run id; `cargo test -p harness-tui session_history` passed 7 lib/runtime tests plus session-history integration coverage; `cargo test -p harness-tui --test session_navigation_keybindings_test` passed 3 tests; `cargo fmt --all -- --check` passed; LSP diagnostics for `crates/harness-tui/src/app/session_navigation.rs` and `crates/harness-tui/src/lib_tests.rs` were clean. Manual QA launched `cargo run -p harness -- tui --mock`, opened `/resume`, typed `tool` and observed narrowed title/fuzzy results, then typed `model-1` and observed rows matched by model metadata. | PASS | reverting `session_history_filter_matches` to title-only substring matching, removing provider/model fields from the search set, breaking fuzzy title matching, or hiding the typed search input makes the focused test or manual QA fail |

## Evidence 2026-05-29 WS8 diff hunk navigation
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Diff review supports next/previous hunk navigation through the keybinding registry | WS8 | red/green app-model test + manual replay TUI QA | Green: `cargo test -p harness-tui diff_hunk_navigation_advances_and_retreats_between_hunks` passed after the app measured transcript diff hunk rows and jumped selection/scroll between two hunks; `cargo test -p harness-tui keymap_binds_diff_hunk_navigation_to_default_bindings` passed after primary terminal-safe bindings moved to `Alt+n`/`Alt+p` with `Alt+]`/`Alt+[` retained as alternates; `cargo fmt --all -- --check` passed; LSP diagnostics for `crates/harness-tui/src/app/tests.rs` and `crates/harness-tui/src/keybindings.rs` were clean. Manual QA created `/tmp/harness-work/ws8-diff-nav-replay`, launched `cargo run -p harness -- tui --replay /tmp/harness-work/ws8-diff-nav-replay`, rendered a two-hunk diff, resized to a compact viewport, pressed `Alt+p` to jump to the first hunk, and pressed `Alt+n` to jump back to the second hunk. | PASS | removing hunk offsets from transcript diff rendering, bypassing `Action::DiffHunkNext` / `Action::DiffHunkPrevious`, dropping the default `Alt+n`/`Alt+p` bindings, or breaking replay diff rendering makes the focused tests or manual QA fail |

## Evidence 2026-05-29 WS8 keyboard navigation and keybinding registry
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Background/subagent work and session tree/sidebar controls are keyboard-first | WS8 | app-model tests + manual replay TUI QA | `cargo test -p harness-tui keyboard_sidebar_subagent_selection_opens_child_session`; `cargo test -p harness-tui --test session_navigation_keybindings_test`; `cargo test -p harness-tui --test lineage_view_model_test lineage_tree_navigation_filters_and_folds` | The sidebar test drives default key events through `KeyMap` actions, moves operator-sidebar selection from `None` to rows 0 and 1, presses Enter, and opens `agent_child` read-only. The session-navigation keybinding suite covers `session_child_first`, `session_child_cycle`, `session_child_cycle_reverse`, and `session_parent` overrides/defaults. The lineage browser test drives keyboard selection, fold/unfold, filter typing, Ctrl+n/Ctrl+p movement, and Esc close for the session tree. Manual QA created `/tmp/harness-work/ws8-sidebar-keyboard-qa/sessions/run_ws8_sidebar_parent`, launched `cargo run -p harness -- tui --replay /tmp/harness-work/ws8-sidebar-keyboard-qa/sessions/run_ws8_sidebar_parent`, pressed the registered `]` child-cycle binding, observed the child transcript `Child session details are visible after keyboard activation.`, footer `Explore (1 of 1) ... Parent Ctrl+[  Prev [  Next ]`, and `?` rendered replay keyboard help. | PASS | removing operator-sidebar keyboard targets, bypassing `Action::MoveDown`/`Action::SubmitPrompt`, breaking child-session keybindings, or regressing lineage-browser keyboard handling makes the focused tests or manual QA fail |
| New WS8 keyboard actions are registered through configurable defaults and guarded against scattered hardcoded checks | WS8 | keybinding registry guard | `cargo test -p harness-tui ws8_keyboard_surfaces_use_registry_actions_instead_of_hardcoded_keys`; `cargo test -p harness-tui keymap_binds_child_session_navigation_to_default_bindings`; `cargo test -p harness-tui keymap_binds_diff_hunk_navigation_to_default_bindings` | The guard asserts required WS8 actions have default configurable bindings and `Action::from_str` round-trips, then source-walks the touched operator-sidebar, child-session, and diff-hunk dispatch/rendering ranges to reject `KeyCode::`/`KeyModifiers::` checks outside `keybindings.rs`. Child-session defaults remain `Ctrl+]`, `]`, `[`, and `Ctrl+[`; diff defaults remain `Alt+n`/`Alt+p` with alternate `Alt+]`/`Alt+[`. | PASS | adding a WS8 movement/session/diff key comparison outside the registry, deleting an action id, or dropping a default binding makes the guard or binding tests fail |

## Evidence 2026-05-29 WS8 deterministic flow coverage
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Approve/deny permission, resume, diff review, replay-failure, and PTY happy-path flows render visible operator states | WS8 | focused render tests + app-model tests + manual replay QA + PTY artifact | `cargo test -p harness-tui --test deterministic_render_test permission_modal_preserves_draft_without_pty`; `cargo test -p harness-tui permission_modal_allow_always_requests_durable_run_grant`; `cargo test -p harness-tui permission_modal_ctrl_n_emits_deny_intent_without_hiding_pending_permission`; `cargo test -p harness-tui --test deterministic_render_test startup_session_history_picker_renders_without_pty`; `cargo test -p harness-tui diff_hunk_navigation_advances_and_retreats_between_hunks`; `cargo test -p harness-tui --test deterministic_render_test replay_failure_state_renders_without_pty`; manual replay QA on `/tmp/harness-work/ws8-replay-failure-qa/run_failed`; PTY artifact `target/test-lanes/20260529-161412/signoff-pty/stages/harness_tui_happy_path_pty/artifacts/tui-happy-path-recording.json` | All targeted tests passed; replay QA showed `Replay · read-only · run run_failed · 2 ev` and composer text `Replay is read-only · run failed · inspect transcript · session shell pre…` with footer `? shortcuts · ctrl+tab focus · r reload · q quit`; signoff PTY artifact exists with lane name, command, timestamp, and env/provenance summary. | PASS | removing the permission scope/countdown copy, deny/allow intents, diff hunk navigation, resume picker rendering, replay-failure surfacing, or PTY happy-path artifact makes the focused tests or manual QA fail |

## Evidence 2026-05-30 final roadmap reconciliation and self-audit
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Built-in capability order and built-in state policy are documented and tested | WS3/WS7 | docs-reference test | `cargo test -p harness --test config_docs_reference_test built_in_capability_order_and_state_policy_are_documented_and_guarded` | 1 passed; `docs/extension-strategy.md` now fixes core capability order, stable-id sorted built-ins, operator-requested skill activation order, and the JSONL/artifact schema-version, migration-policy, and replay-behavior requirement for future release-blocking built-ins. | PASS | removing the order/state-policy anchors or reordering capability rows makes the docs-reference guard fail |
| Anti-stub sweep and mutation spot-check | §16.1 | command output + temporary mutation probe | `rg -n "#\[ignore" crates`; `rg -n "assert!\(true\)" crates/*/tests crates/*/src`; `rg -nU "#\[test\]\s*\n\s*(?:async\s+)?fn\s+[A-Za-z0-9_]+\s*\([^)]*\)\s*\{\s*\}" crates/*/tests crates/*/src`; temporary five-anchor mutation script | Ignore scan returned seven intentional env-gated signoff/live/native matches: `crates/harness/tests/binary_smoke.rs:22` (`HARNESS_BINARY_SMOKE=1`), `crates/harness/tests/pty_happy_path_recorded.rs:21` (`signoff-pty`), `crates/harness-providers/src/openai.rs:2760` (`HARNESS_LIVE_PROXY=1`), `crates/harness-testkit/tests/live_proxy_e2e.rs:7`, `:13`, `:19` (`HARNESS_LIVE_PROXY=1`), and `crates/harness-testkit/tests/native_visual_e2e.rs:4` (`HARNESS_NATIVE_VISUAL=1` plus display session). No ignore is a new gate-dodging stub. Assert-true and empty-test scans returned no output. Mutation spot-check observed expected failures for built-in capability policy, first-run provider/auth docs, AST-grep deferral docs, primary Intent Gate prompt, and built-in skill docs, then restored files. | PASS | adding an unclassified `#[ignore]`, weakening those tests, or making them pass after anchor removal makes the audit or mutation probe fail |
| Stale-link and roadmap accounting audit | §14/§16.2/§16.3 | source-derived parser + docs scan | `rg -n "docs/v1-agent-catalog-workspace-intelligence-prd.md|docs/legacy-parity-spec.md|docs/v1-skill-contract-capability-governance-prd.md|skills-lock.json" docs --glob !docs/v1-release-readiness-slice-prd.md --glob !docs/v1-release-readiness-slice-progress.md`; source parser over `docs/roadmap-v1.md` | Deleted-artifact reference scan returned no output outside the PRD/progress historical notes. Parser result after final flips: total roadmap boxes 357, excluded 56, included denominator 301, checked included 301, included completion 100.00%. | PASS | adding a dangling deleted-doc reference or changing excluded-set matching changes the parser result and invalidates the row |

### Historical roadmap accounting numbers (pre strict-V1 closeout)

- Total roadmap boxes: 357
- Excluded final-slice or post-V1 boxes: 56
- Included denominator: 301
- Checked boxes inside included denominator: 301
- Included pre-V1 completion percentage: 100.00%
- Result: ≥80% threshold satisfied. The percentage is above the 85–90% target range because this slice deepened additional in-scope workstreams rather than adding exclusions.

### Historical exclusions from the earlier slice denominator

These rows capture the earlier readiness-slice baseline. The strict-V1 closeout rows below supersede any item that G004/G005/G006/G008 later implemented or reclassified with evidence.

#### AST-grep replace native tool
- Line 531: [ ] AST-grep replace is a first-class native tool.
- Line 583: [ ] AST-grep replace; see Native tool baseline.

#### Command and hook seam implementation
- Line 277: [ ] V1 decides whether markdown-defined slash commands, distinct from the unsupported upstream config `command` area, are first-class; if so, it defines the command file schema, `$ARGUMENTS` substitution, and safe command interpolation policy.
- Line 281: [ ] If command interpolation is supported, it is permission-checked and replay safe; command rendering never executes during replay.
- Line 283: [ ] Hook phases have a lifecycle map with status labels: native, fallback, intentionally unsupported, and post-V1.
- Line 285: [ ] Minimal V1 hook seams are limited to message received, context transform, provider params, tool preflight, tool result, agent turn finish, session idle, and compaction request.
- Line 288: [ ] Rules/context injection uses explicit source files, glob matching, provenance text, and session-scoped priority/consume semantics.
- Line 290: [ ] Hooks cannot bypass coordinator-owned permissions, event append authority, or replay side-effect boundaries.
- Line 589: [ ] A small slash-command/hook seam for built-in lifecycle behavior, distinct from arbitrary executable plugins; see Command and hook depth plus Extension and plugin strategy.
- Line 604: [ ] Built-in lifecycle command hooks are migrated onto the same hook seam planned for future extensions.
- Line 606: [ ] Hook phases are explicitly modeled for message received, provider params, context transform, tool preflight, tool result, agent turn finish, session idle, and compaction request.

#### External compatibility skill-root adapters
- Line 231: [x] Skill discovery documents and tests full compatibility/built-in precedence across built-in scopes and imported external editor/assistant/agent roots before V1 ships; see strict V1 skill precedence evidence.
- Line 234: [x] External editor, assistant, and agent compatibility skill roots are tracked as adapter work: `.external-editor/skills/*/SKILL.md`, `.assistant/skills/*/SKILL.md`, `.agents/skills/*/SKILL.md`, and user-level equivalents. Harness-owned roots stay first unless an explicit compatibility mode says otherwise.

#### Native screenshot gap and G007 TUI signoff manifest rows
- Line 365: [ ] Every V1 user-visible TUI change has deterministic PTY or snapshot coverage, plus native visual signoff when terminal rendering fidelity matters; native screenshot artifacts remain env-gated until `HARNESS_NATIVE_VISUAL=1` and `DISPLAY` are available.
- Line 449: [x] UI signoff records startup screen, command palette, session picker, and diff review coverage through deterministic snapshot or PTY evidence; reference-image comparison is not required for this PRD.
- Line 697: [x] TUI signoff manifest and PTY artifacts cover normal prompt, permission/question, provider/tool failure, and resume flows.
- Line 788: [x] The scripted operator happy path and TUI signoff checklist are recorded through the signoff manifest and `signoff-pty` lane.

#### Non-V1 product areas
- Line 609: [x] External executable/script plugins are deferred until command mediation, sandboxing, and replay-safe manifests are proven. Superseded by G008 final reconciliation evidence.
- Line 611: [x] Active upstream plugin compatibility is explicitly post-V1. Superseded by G008 final reconciliation evidence.

#### Post-V1 roadmap sections
- Line 77: [ ] Harden the extension manifest and built-in capability system.
- Line 78: [ ] Add richer built-in skills and skill bundles beyond the small V1 candidate set.
- Line 80: [ ] Expand subagents only after the `AgentCatalog`, prompt snapshots, and permission fixtures are stable.
- Line 82: [ ] Turn Team Mode from primitive event tools into an optional orchestrated layer if it proves useful.
- Line 84: [ ] Add skill-embedded MCP and OAuth only after ordinary MCP, skills, and extension-state contracts are boring.
- Line 86: [ ] Add browser/media/desktop automation as installable capabilities, not core release blockers.
- Line 88: [ ] Revisit upstream plugin compatibility only after Harness-native extension seams have their own conformance suite.
- Line 627: [ ] Declared team registry support exists.
- Line 628: [ ] Team worktree support exists.
- Line 629: [ ] Team tmux visualization exists.
- Line 630: [ ] Team mailbox artifacts and resume semantics are documented and tested.
- Line 631: [x] Full Team Mode is treated as post-V1 unless deliberately pulled into the release scope. Superseded by G008 final reconciliation evidence.
- Line 636: [ ] Ralph loop / ultrawork loop.
- Line 637: [ ] Todo enforcer or autonomous idle continuation loop.
- Line 638: [ ] Prometheus / Metis / Momus / Atlas orchestration stack.
- Line 639: [ ] Full specialist persona catalog.
- Line 640: [ ] Skill-embedded MCP lifecycle.
- Line 641: [ ] MCP OAuth lifecycle.
- Line 642: [ ] Browser automation / Playwright skill bundle.
- Line 643: [ ] Media analysis / `look_at` / multimodal-looker.
- Line 644: [ ] Interactive tmux terminal tool.
- Line 645: [ ] Arbitrary upstream plugin loading.
- Line 646: [ ] Upstream server/share/auth/account product surfaces.
- Line 647: [ ] Desktop, web, mobile, or PWA clients.
- Line 648: [ ] IDE integration, editor-selection sync, or editor-side diff control.
- Line 649: [ ] GitHub Action, Slack, Discord, Telegram, OpenClaw, or other remote collaboration bots.
- Line 651: [ ] Cloud, enterprise, billing, analytics, telemetry, or hosted API surfaces.
- Line 652: [ ] JS/QuickJS/WASM extension marketplace or broad extension runtime.
- Line 653: [ ] Beads, Agent Mail, RCH, swarm, or external issue/validation-broker coordination systems.
- Line 655: [ ] Math-heavy adaptive optimization controllers, shadow dual execution, online policy evaluation, or VOI planners.
- Line 657: [ ] Broad non-OpenAI-compatible provider transports beyond the implemented execution path.

#### Typed extension manifest seam
- Line 599: [x] A typed extension manifest seam exists for optional tools, hooks, commands, prompts, MCP bundles, diagnostics, and provider decorators. Superseded by G006 descriptor-only manifest evidence.
- Line 601: [x] Extension tool descriptors declare public permission names, but extension-provided tools are not registered or executed in V1 and no runtime permission path exists yet. Superseded by G009 descriptor-only truth evidence.
- Line 603: [x] Replay support for extension manifests is limited to static descriptor/config metadata; it does not render extension tool events or load extension code. Superseded by G009 descriptor-only truth evidence.
- Line 753: [x] A typed extension manifest seam defines optional tools, hooks, commands, prompts, MCP bundles, diagnostics, provider decorators, capability ids, disablement state, and replay-safe descriptor metadata. Superseded by G009 descriptor-only truth evidence.
- Line 756: [x] Extension-provided behavior is not registered, executed, permissioned, or replay-rendered in V1; future runtime support must go through coordinator-owned permissions, event append authority, artifact redaction, and replay side-effect boundaries. Superseded by G009 descriptor-only truth evidence.
- Line 815: [x] Define the typed extension manifest seam before migrating lifecycle command hooks onto it or marking the extension strategy guide complete. Superseded by G006 descriptor-only manifest evidence.

### Roadmap reconciliation table

| Line | Exact checkbox text | WS evidence | Verify command or artifact | Observed result | Flipped yes/no | §3.3 exclusion category |
|---:|---|---|---|---|---|---|
| 40 | Rust-native runtime and crate seams instead of copying the upstream app architecture. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 42 | Event log and replay invariants are core product behavior. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 43 | Hashline editing is first-class rather than an optional editing style. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 44 | Simple task subagents are part of the core workflow. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 45 | Category routes exist only for delegation, not as main engine modes. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 46 | Doctor/readiness/evidence-gate foundations exist as product surfaces; V1 hardening remains tracked below rather than treated as maintainer-only scripts. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 48 | Extension seams are designed before arbitrary plugin compatibility is promised. Orchestration-inspired V1 inclusions are intentionally narrow. Checked items in this list mean the current mechanism exists; unchecked items are V1 hardening work that must still land before the roadmap can treat the surface as release-ready: | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 55 | Intent-gate and prompt-rigor ideas. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 56 | Read-only `explore` as a subagent. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 57 | Category delegation names and routing concepts. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 58 | Markdown skill loading as a runtime mechanism. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 59 | Skill progressive-disclosure behavior is documented and tested. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 60 | Candidate built-in skills such as `git-master`, `review-work`, and `frontend-ui-ux` ship with V1-quality bodies, docs, disablement, and tests. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 62 | Stronger doctor checks, prompt snapshots, and evidence gates cover prompt, skill, task-route, and asset readiness. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 64 | AST-grep search, model-visible session tools, and a dedicated `background_cancel` land as practical tool-surface improvements with docs and parity tests. Everything else from the heavy-orchestration references is post-V1 by default unless this document explicitly moves it into the release scope. That includes full specialist-agent catalogs, autonomous continuation loops, Team Mode, skill-embedded MCP/OAuth, browser or media automation, remote collaboration bots, and broad plugin compatibility. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 77 | Harden the extension manifest and built-in capability system. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 78 | Add richer built-in skills and skill bundles beyond the small V1 candidate set. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 80 | Expand subagents only after the `AgentCatalog`, prompt snapshots, and permission fixtures are stable. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 82 | Turn Team Mode from primitive event tools into an optional orchestrated layer if it proves useful. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 84 | Add skill-embedded MCP and OAuth only after ordinary MCP, skills, and extension-state contracts are boring. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 86 | Add browser/media/desktop automation as installable capabilities, not core release blockers. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 88 | Revisit upstream plugin compatibility only after Harness-native extension seams have their own conformance suite. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 93 | The vanilla CLI/TUI reference material under `inspirations/` is the primary source for CLI, TUI, sessions, MCP, plugins, permissions, providers, and app-level product expectations. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 96 | Optional visual reference material is comparison context only, not runtime authority. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 98 | `inspirations/codex/` is the Codex CLI reference for install simplicity, sandbox-conscious Rust architecture, TUI snapshot rigor, and provider/tool execution ergonomics. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 101 | The heavy-orchestration reference under `inspirations/` is available. Use it for candidate skills, agents, hooks, task/team workflows, and MCP ideas, but do not treat complete agent-OS parity as a V1 goal. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 104 | A workflow-layer reference under `inspirations/` is available for pairing a base harness with setup, doctor, real execution smoke tests, durable plans, and optional tmux/team runtime. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 107 | The TypeScript baseline under `inspirations/` is available for package seams, provider abstraction, configurable keybindings, interactive testing practice, and supply-chain hardening. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 110 | A Rust performance/security reference under `inspirations/` covers single-binary release posture, structured concurrency, capability-gated extensions, session indexing, evidence-gated claims, and crash-resilient persistence. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 114 | The extension-first reference under `inspirations/` is available: keep core changes small, ship useful builtin extensions, and leave heavier features as installable packages. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 117 | A product-polish reference under `inspirations/` covers for review diffs, session sidebars, search, status surfaces, mobile/desktop clients, and user interface experiments pending upstream. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 123 | V1 targets a vanilla local-coding operator experience rather than full orchestration parity. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 125 | The runtime remains Rust-native and event-sourced; replay must stay side-effect free. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 127 | Hashline editing stays the normal file-changing path. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 128 | `build` is the default implementation agent. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 129 | `plan` is a selectable primary agent alongside `build`. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 130 | `explore` is a read-only subagent profile used through `task(subagent_type = "explore")`, not a main engine view. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 132 | Public docs describe the V1 stance clearly from the README and config guide. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 134 | Docs remove or update stale references to missing runtime assets such as `.agent-harness/native-agents/*.toml` and `.agent-harness/agents/operator.md`. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 145 | Use the vanilla reference's agent, skill, project-instruction, and command directory layouts as references for markdown-defined agents, skills, and commands. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 148 | Use Codex's base-instruction and core-skill design as the reference for instruction precedence, preamble rigor, and progressive disclosure. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 150 | Use orchestration prompt libraries as references for explicit scope guards, output contracts, role-specific tool restrictions, and lifecycle hook maps. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 152 | Use the extension-first and TypeScript baselines as references for extension-first design, disableable builtin capabilities, compaction safety, prompt presets, and release evidence gates. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 154 | Each adopted reference pattern names the Harness seam that owns it before implementation starts. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 156 | Reference behavior is copied as user-observable behavior, not as source architecture, package layout, or brand-specific terminology. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 161 | Runtime profiles resolve from `configs/harness.example.jsonc` into coordinator `AgentProfile` values. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 163 | The dynamic prompt builder composes base/model prompt, environment, delegation reminder, project instructions, and skill guidance. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 165 | `build` and `plan` prompt asset files exist under `.agent-harness/agents/`. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 168 | `build.md` has a source-controlled prompt body, not only frontmatter. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 169 | `plan.md` has a source-controlled prompt body, not only frontmatter. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 170 | Every primary prompt uses a shared skeleton: identity, goal, use when, do not use when, scope guard, tool/permission posture, operating loop, ask gate, failure recovery, output contract, and verification gate. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 173 | Every subagent prompt uses the same skeleton, with stronger output contracts and clearer stop conditions than primary agents. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 175 | Prompt bodies declare what is enforced by permissions versus what is only behavioral guidance. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 177 | Prompt precedence is documented and tested: system/developer/user, AGENTS.md, runtime agent prompt, config instructions, loaded skills, and task delegation context. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 180 | Prompt bodies for primary agents, subagents, and category routes are near-exact adaptations of the relevant reference prompt bodies, with only branding, unsupported agent-OS workflows, and features not present or not planned for Harness removed. Any retained reference behavior must map to an explicit Harness runtime seam, permission policy, tool, documentation contract, or roadmap item. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 185 | Primary prompts include an intent-gate pattern before tool use for ambiguous requests: state the interpreted intent, then route to explain, investigate, implement, plan, or ask exactly one blocking question. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 188 | Dynamic prompt sections are named modules with golden tests for each section and for full composed prompts. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 190 | Model-specific prompt tuning is either intentionally absent for V1 or represented as explicit prompt presets with tests; substring heuristics do not become the only long-term seam. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 193 | Prompt golden tests cover `build`, `plan`, `general`, `explore`, all category routes, and hidden title/summary/compaction profiles. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 198 | `task(subagent_type = ...)` can spawn named subagent profiles. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 199 | `task(category = ...)` maps category names to ordinary non-primary profiles. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 200 | `task(run_in_background = true)` schedules background child sessions and `background_output` retrieves results. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 202 | Plan can delegate only to `explore` through profile-aware task description filtering and parent-child policy enforcement. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 204 | `general` has a real prompt body defining when it should handle multistep work, how much context to return, and when to refuse work that belongs to primary Build. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 207 | `explore` has a real prompt body defining read-only behavior, search strategy, output contract, and stop condition. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 209 | `explore` returns structured findings such as files, relationships, answer, and next steps, rather than freeform summaries. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 211 | Category routes have category-specific prompt appends for their domains, especially `visual-engineering`, `ultrabrain`, `deep`, `quick`, and `writing`. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 213 | Category route descriptions include use-when and do-not-use-when guidance so parent agents can choose correctly. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 215 | The task tool contract recommends or enforces a structured delegation body: context, goal, downstream use, request, required tools, must-do, and must-not-do. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 217 | Child task summaries are capped and structured so parent context stays lean. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 218 | Category route model, variant, prompt append, tools, permissions, hidden status, and fallback are centralized behind an `AgentCatalog`-style seam. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 220 | Fallback from an unknown category to `general` is visible in task output and doctor/readiness diagnostics. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 225 | Skills are discovered from configured project/global roots. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 226 | `skill` can load a discovered skill into the current turn. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 227 | `task(load_skills = [...])` injects loaded skill content into child prompts. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 228 | Skill loading supports allow, ask, and deny permission modes. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 229 | Skill discovery documents implemented V1 precedence across configured project/workspace roots, git-root walking, and user/XDG global roots. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 231 | Skill discovery documents and tests full compatibility/built-in precedence across built-in scopes and imported external editor/assistant/agent roots before V1 ships. | Strict V1 skill precedence rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test config_docs_capture_v1_skill_contract_and_authoring_guide` | Harness-owned roots, built-in stable ids, configured global roots, ignored-by-default compatibility roots, explicit compatibility import order, and duplicate shadow diagnostics are now documented and tested. | yes | n/a |
| 234 | External editor, assistant, and agent compatibility skill roots are tracked as adapter work: `.external-editor/skills/*/SKILL.md`, `.assistant/skills/*/SKILL.md`, `.agents/skills/*/SKILL.md`, and user-level equivalents. Harness-owned roots stay first unless an explicit compatibility mode says otherwise. | Strict V1 skill precedence rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test config_docs_capture_v1_skill_contract_and_authoring_guide` | Compatibility roots remain ignored unless explicitly configured, and explicit imports cannot shadow Harness-owned or other non-compatibility roots. | yes | n/a |
| 238 | Skill frontmatter has a V1 schema for name, description, argument hint, allowed/expected tools, target agent/category, and deferred MCP/resource metadata; skill loading permission modes are config/catalog metadata, not frontmatter. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 242 | The skill authoring guide documents a quality template: purpose, use when, do not use when, execution policy, steps, tool usage, escalation/stop conditions, final checklist, and advanced notes. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 245 | Skill loading follows progressive disclosure: metadata in catalog and SKILL.md on activation; bundled references/assets remain deferred metadata/follow-up, not runtime-loaded in this slice. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 248 | Skill prompt injection order and conflict resolution with agent prompts and AGENTS.md is documented. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 250 | Built-in skills are disableable by stable ids before V1 adds more of them. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 251 | Built-in skill candidates are reviewed against the V1 stance before being checked: `git-master`, `review-work`, and `frontend-ui-ux` are useful; browser, Team Mode, and skill-embedded MCP bundles remain post-V1 unless re-scoped. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 257 | V1 names which shipped behaviors are core runtime behavior and which are disableable built-in capabilities. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 259 | Disableable built-ins have stable ids, default states, config shape, doctor visibility, and tests. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 261 | Built-in capability order is intentional and tested when ordering affects prompt assembly, tool registration, permission checks, or compaction. | WS3/WS7 built-in capability closeout | `cargo test -p harness --test config_docs_reference_test built_in_capability_order_and_state_policy_are_documented_and_guarded` | 1 passed; docs guard enforces capability order and JSONL/artifact schema/migration/replay policy | yes | n/a |
| 263 | New built-in capabilities use the public Harness interfaces; they do not reach around coordinator permissions, event storage, or tool registry seams. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 265 | Any JSONL or artifact state written by built-ins has a documented schema, migration policy, and replay behavior before it is release-blocking. | WS3/WS7 built-in capability closeout | `cargo test -p harness --test config_docs_reference_test built_in_capability_order_and_state_policy_are_documented_and_guarded` | 1 passed; docs guard enforces capability order and JSONL/artifact schema/migration/replay policy | yes | n/a |
| 267 | Compaction has explicit V1 contracts for threshold policy, retained recent turns, file/tool context preservation, todo/plan bridging, and post-compaction restoration hints. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 270 | Compaction failures have a bounded fallback policy with user-visible status; repeated failures do not silently erase context or loop forever. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 272 | Provider/model prompt presets, if added, are thin tuning layers over the base prompt skeleton, not duplicate full system prompts. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 277 | V1 decides whether markdown-defined slash commands, distinct from the unsupported upstream config `command` area, are first-class; if so, it defines the command file schema, `$ARGUMENTS` substitution, and safe command interpolation policy. | §3.3 exclusion: Command and hook seam implementation | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Command and hook seam implementation |
| 281 | If command interpolation is supported, it is permission-checked and replay safe; command rendering never executes during replay. | §3.3 exclusion: Command and hook seam implementation | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Command and hook seam implementation |
| 283 | Hook phases have a lifecycle map with status labels: native, fallback, intentionally unsupported, and post-V1. | §3.3 exclusion: Command and hook seam implementation | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Command and hook seam implementation |
| 285 | Minimal V1 hook seams are limited to message received, context transform, provider params, tool preflight, tool result, agent turn finish, session idle, and compaction request. | §3.3 exclusion: Command and hook seam implementation | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Command and hook seam implementation |
| 288 | Rules/context injection uses explicit source files, glob matching, provenance text, and session-scoped priority/consume semantics. | §3.3 exclusion: Command and hook seam implementation | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Command and hook seam implementation |
| 290 | Hooks cannot bypass coordinator-owned permissions, event append authority, or replay side-effect boundaries. | §3.3 exclusion: Command and hook seam implementation | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Command and hook seam implementation |
| 295 | Permission policy supports profile overrides, defaults, and selector rules. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 296 | Plan's codebase-edit restrictions and category recursion limits are enforced by configuration and permission checks, not prompt text alone. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 298 | Tool allow/deny posture is visible for every resolved profile in doctor JSON. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 299 | Read-only subagent restrictions are covered by tests that attempt edit, bash, task, and MCP calls where relevant. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 301 | Tool schemas and prompt descriptions agree on exact ids, aliases, permissions, and replay behavior. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 303 | Bash timeout, output cap, and blocked-command guidance are stated in both tool docs and agent prompt guidance. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 305 | Permission docs state the V1 threat model clearly: permissions are an operator approval layer, not a sandbox, and dangerous approvals can still affect the local workspace. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 311 | A drift test fails when `.agent-harness/agents/*.md` assets referenced by docs/config are missing, empty, or frontmatter-only where a body is required. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 313 | A generated prompt snapshot exists for every shipped profile and category. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 314 | Prompt skeleton adherence is checked by a fixture that asserts every shipped profile includes identity, goal, use-when, do-not-use-when, scope guard, tool/permission posture, operating loop, ask gate, failure recovery, output contract, and verification gate sections. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 318 | A task delegation fixture proves skill content, category prompt append, parent/child lineage, sync/background behavior, and summary capping. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 320 | A permission fixture proves prompt promises match runtime enforcement for plan, explore, general, and category routes. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 322 | Doctor reports prompt asset status, skill catalog status, task route status, and profile tool/permission posture separately from provider health. | WS2/WS9 prompt evidence rows | `cargo test -p harness --test bootstrap_profiles_test`; `cargo test -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` | prompt golden/fixture and delegation tests passed in recorded evidence rows | yes | n/a |
| 332 | The README has one clear install, configure, and run path for a new user. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 333 | `docs/config.md` and checked-in schemas/examples agree on every public key. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 334 | `docs/architecture.md` describes all V1 runtime invariants and public events accurately. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 336 | `docs/testing.md` names the required V1 verification lanes and artifact expectations. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 338 | `cargo fmt --all -- --check` passes. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 339 | `cargo check --workspace` passes. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 340 | `cargo test --workspace --all-features` passes, or any live-only exclusions are explicitly documented. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 342 | `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes. | Final verification suite | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | Passed on 2026-05-30 after final binary-smoke and closeout-audit edits; command exited 0 with `Finished dev profile`. | yes | n/a |
| 344 | `cargo run -p harness -- --config configs/harness.example.jsonc config validate` passes. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 346 | `cargo run -p harness -- --config configs/harness.example.jsonc doctor` gives actionable output for missing provider, model, MCP, tool, and agent setup. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 349 | A manual TUI happy path has been recorded for start, prompt, permission, tool call, edit, resume, and quit. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 351 | Release-facing speed, provider breadth, compatibility, or parity claims are backed by current evidence artifacts, not README assertions or inspiration claims. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 354 | The V1 closeout bundle includes artifact roots for the release-blocking lanes, with `summary.txt`, `env.txt`, per-stage `command.txt`, `stdout.txt`, `stderr.txt`, `status.txt`, and `verification.txt` where the lane runner emits them. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 361 | A canonical lane runner exists for deterministic, integration, signoff, and stress evidence. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 363 | The testing guide documents PTY, live, native visual, simulation, and stress lanes as separate provenance classes. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 365 | Every V1 user-visible TUI change has deterministic PTY or snapshot coverage, plus native visual signoff when terminal rendering fidelity matters. | §3.3 exclusion: Native visual signoff | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Native visual signoff |
| 367 | V1 defines release smoke tests for `harness --help`, `harness --version`, config validation, doctor, and one real or explicitly mocked provider call from outside the repository. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 370 | Release smoke includes one outside-repository TUI startup and one tool-enabled prompt path, so V1 evidence proves more than config preflight or `doctor` success. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 373 | V1 defines which checks are release blockers and which are local development aids. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 375 | V1 defines startup/readiness, TUI render, session resume, and binary size budgets before making performance claims. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 377 | Performance claims cite current artifacts with run provenance; stale or partial artifacts do not support release-facing claims. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 379 | Provider/model compatibility claims are backed by fixture or live-gated evidence for the named transport, not by provider catalog metadata alone. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 381 | Prompt, permission, compaction, and built-in-capability tests use faux/mock providers by default; real-provider tests are live-gated and never required for deterministic lanes. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 384 | Feature-specific fixtures exist for prompt assembly, task delegation, permission decisions, compaction summaries, and extension/built-in state. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 389 | V1 has one recommended install path for normal users. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 390 | V1 has one source-build path for contributors. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 391 | The released artifact supports `harness --help` and `harness --version` from outside the repository. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 393 | The first-run path explains how to create or copy a minimal `harness.jsonc`. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 394 | The first-run path explains provider/auth setup without assuming the local loopback provider already exists. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 396 | The first-run path ends with an exact first prompt and visible success signal, so a new user can tell the provider, tools, and session store worked. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 398 | `doctor` is documented as an install/config readiness check. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 399 | A real execution smoke test is documented separately from `doctor`, because a green readiness report does not prove the selected provider can complete a model call. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 402 | V1 install docs explain where sessions, config, skills, and artifacts live. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 403 | V1 has a troubleshooting page for auth, provider base URL, missing tools, session resume, terminal rendering, and permission prompts. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 410 | The CLI entrypoint exists. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 411 | The interactive TUI can be launched from the CLI. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 412 | Headless prompt execution exists. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 413 | Scenario/headless run flows exist. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 414 | Config validation exists. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 415 | JSON schema output exists. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 416 | Doctor diagnostics exist. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 417 | Session list, inspect, replay, continue, export, tree, fork, and clone commands exist. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 419 | Model catalog generation/probing commands exist. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 420 | CLI help text has been reviewed as a complete V1 user surface. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 421 | CLI command names and docs are audited against the README quick start. | WS10 CLI/first-run rows | `cargo test -p harness --test config_schema_cli_test`; `cargo test -p harness --test config_docs_reference_test first_run_provider_auth_docs_do_not_assume_loopback_only` | CLI help/README audit and first-run provider/auth docs passed | yes | n/a |
| 425 | Ratatui startup/live/replay/scenario modes exist. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 426 | Live sessions render a transcript-first shell. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 427 | Replay mode is read-only. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 428 | Permission and question overlays exist. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 429 | Diff rendering exists for edit review. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 430 | Operator sidebar and secondary surfaces exist. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 431 | Keybinding override plumbing exists. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 432 | Slash commands exist for model/status/toggles/resume/new/tree/fork/clone. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 433 | Prompt `@` mentions can suggest workspace files, agents, and MCP resources. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 434 | Startup is prompt-first by default, with session browsing secondary rather than the initial focus. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 436 | Prompt history is durable across sessions. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 437 | Prompt history navigation preserves drafts and cursor intent. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 438 | Command palette metadata is centralized and reused by slash commands, help, and keybinding surfaces. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 440 | Permission overlays show shortcuts, scope, and timeout/countdown state clearly. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 442 | Model switching shows provider-grouped search and visible fallback/error status. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 444 | Session search supports visible fielded or fuzzy filtering. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 445 | Subagent/background work is keyboard-navigable from the operator surface. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 446 | Diff review supports next/previous hunk navigation. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 447 | Approve/deny, diff review, resume, and replay failure states have visible operator flows covered by deterministic PTY or snapshot evidence. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 449 | UI signoff records startup screen, command palette, session picker, and diff review coverage through deterministic snapshot or PTY evidence; reference-image comparison is not required for this PRD. | G009 correction of false completion | `cargo test -p harness-tui --test tui_signoff_manifest_test`; `cargo test -p harness-tui --test deterministic_render_test` | Manifest maps startup, command palette, session picker/resume, and diff review to deterministic owner tests and PTY stages without requiring reference-image assets. | yes | n/a |
| 452 | New keybindings are registered through configurable keybinding defaults, not hardcoded checks scattered through TUI code. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 454 | Session tree/sidebar navigation has keyboard-first controls before any pointer-only polish is considered complete. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 459 | Events are the source of truth. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 460 | Replay derives state from event logs without executing tools or providers. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 461 | Session lineage, tree, fork, and clone surfaces exist. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 462 | Background child-task wakeup events are modeled. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 463 | Tool summaries and artifacts are persisted with redaction/capping policy. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 464 | Model-visible session tools exist: `session_list`, `session_read`, `session_search`, and `session_info`. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 466 | Resume behavior has a documented V1 acceptance test covering a realistic interrupted session. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 468 | Session resume/list performance is measured against a large enough local session corpus before V1 claims fast long-session behavior. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 470 | Crash-resilient session write behavior is documented and tested at the event-store boundary. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 472 | Compaction summaries preserve enough file, tool, skill, todo, and plan context for a resumed session to continue without guessing. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 474 | Branch/fork/clone session flows document how summaries, artifacts, and restored context behave across lineage. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 476 | Session list/resume surfaces show meaningful generated or editable titles, not only paths or opaque run ids. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 478 | A redacted support export or bug-report bundle captures enough session events, artifacts, doctor output, and provider/config summary to debug V1 user failures without leaking secrets. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 484 | Mock provider support exists for deterministic runs. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 485 | OpenAI-compatible provider transport exists. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 486 | Config supports provider/model definitions. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 487 | Model variants are supported in config examples. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 488 | Generated provider catalog maintenance exists. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 489 | Provider errors are surfaced with enough context for non-expert users. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 490 | Runtime model fallback policy is defined for V1. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 491 | The V1 provider support statement is explicit: OpenAI-compatible execution first, broader catalog metadata as reference unless implemented. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 493 | Provider errors use stable, user-actionable categories such as missing credentials, invalid credentials, rate limit, context-window overflow, unsupported tool call, malformed stream, and transport failure. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 499 | Runtime config uses `harness.json` / `harness.jsonc` as the canonical public shape. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 501 | TUI config uses `tui.json` / `tui.jsonc` as the canonical public shape. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 502 | Generated runtime and TUI schemas are checked in. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 503 | `configs/harness.example.jsonc` is the canonical runtime example. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 504 | `configs/tui.example.jsonc` is the canonical TUI example. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 505 | Unsupported upstream product areas are rejected or accepted only when inactive: `server`, `command`, `plugin`, `share`, `autoupdate`, and `enterprise`. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 508 | Config-backed MCP servers are part of the runtime tool registry. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 509 | Doctor reports the complete resolved agent list, including primary agents, subagents, hidden profiles, and category routes. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 511 | Doctor reports stale/missing built-in asset references. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 512 | Doctor reports extension/roadmap readiness separately from runtime health. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 516 | Workspace read/list/glob/grep tools exist. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 517 | Hashline edit tooling exists. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 518 | Bash/shell execution exists behind permission and safety controls. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 519 | Web fetch/search/code search tools exist. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 520 | LSP diagnostics/symbol/reference/rename tools exist. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 521 | `question` exists. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 522 | `skill` exists. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 523 | `todoread` and `todowrite` exist. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 524 | `task` exists as the canonical child-delegation tool. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 525 | `background_output` exists. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 526 | `batch` exists. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 527 | Config-backed MCP tools exist. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 528 | `background_cancel` exists as a dedicated user-facing tool instead of only cancellation through `background_output` arguments. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 530 | AST-grep search is a first-class read-only native tool. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 531 | AST-grep replace is a first-class native tool. | G009 correction of false completion | `cargo test -p harness-tools --test native_ast_grep_replace_test`; `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness-core --test coord_ast_grep_auth_test -- --nocapture` | Dry-run/apply replace tool ships with `EditFs`/`edit`, diff artifacts, path/byte-range validation, truncated-apply refusal, and coordinator path-scoped list-valued `paths` denial-before-execution coverage plus allowed-list-path execution coverage. | yes | n/a |
| 532 | Model-visible session tools are first-class native tools. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 533 | Native tool docs include a concise V1 tool catalog. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 534 | Native tool parity tests cover the full V1 tool catalog. | WS6 tool and permission rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | native tool parity, bash safety, permission threat model, and AST-grep deferral passed | yes | n/a |
| 540 | `build` exists as the default implementation lane. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 541 | `plan` exists as a planning lane with codebase-edit restrictions and a controlled handoff back to Build. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 543 | Stricter delivery remains prompt and tool guidance without changing scheduler semantics. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 545 | Primary-agent picker/docs make clear that primary agents are operator modes, not search helpers. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 547 | Primary-agent prompt bodies are reviewed for V1 quality rather than only frontmatter/runtime synthesis. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 552 | `general` exists as a subagent profile. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 553 | `explore` exists as a read-only local code-search subagent profile. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 554 | Category routes exist for `visual-engineering`, `artistry`, `ultrabrain`, `deep`, `quick`, `unspecified-low`, `unspecified-high`, and `writing`. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 556 | Category profiles are intended for `task(category = "...")` delegation and deny recursive task delegation by default. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 558 | `task(subagent_type = ...)` and `task(category = ...)` behavior is documented in a user-facing V1 guide. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 560 | `task` has a clear contract for sync vs background execution, cancellation, continuation, and skill loading. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 562 | Subagent output is summarized in a way that keeps parent context lean. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 563 | Category route model/variant/fallback resolution is centralized in an `AgentCatalog`-style seam. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 572 | Hashline editing. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 573 | Simple subagents through `task`. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 574 | Category routing through `task(category = ...)`. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 575 | Markdown skill loading. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 576 | Config-backed MCP registration. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 577 | Stricter delivery remains prompt and tool guidance, not an extra primary agent. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 578 | Built-in `git-master` skill; see Skill depth. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 579 | Built-in `review-work` skill; see Skill depth. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 580 | Built-in `frontend-ui-ux` or equivalent visual-engineering skill; see Skill depth. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 582 | AST-grep search; see Native tool baseline. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 583 | AST-grep replace; see Native tool baseline. | G005 AST-grep replace safety gate | `cargo test -p harness-tools --test native_ast_grep_replace_test`; `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness-core --test permission_policy_supports_native_tool_permission_kinds_test` | Native tool catalog and permission mapping now include `ast_grep_replace` as an edit-permission structural rewrite tool that defaults to dry-run. | yes | n/a |
| 584 | Model-visible session tools; see Sessions and replay plus Native tool baseline. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 586 | Dedicated `background_cancel` tool; see Native tool baseline. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 587 | Doctor checks for built-in skills, category routes, and missing assets; see Prompt-system evidence plus Config and doctor. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 589 | A small slash-command/hook seam for built-in lifecycle behavior, distinct from arbitrary executable plugins; see Command and hook depth plus Extension and plugin strategy. | §3.3 exclusion: Command and hook seam implementation | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Command and hook seam implementation |
| 597 | Config-backed MCP gives a safe external-tool integration path today. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 598 | Skills provide a markdown-based instruction extension path today. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 599 | A typed extension manifest seam exists for optional tools, hooks, commands, prompts, MCP bundles, diagnostics, and provider decorators. | G006 extension-manifest-seam | `cargo test -p harness-core --test extension_manifest_test`; `cargo test -p harness --test config_docs_reference_test extension_strategy_test::extension_strategy_documents_descriptor_only_manifest_seam` | Descriptor-only manifest seam shipped with schema/parser/docs evidence; checkbox is checked. | yes | n/a |
| 601 | Extension tool descriptors declare public permission names, but extension-provided tools are not registered or executed in V1 and no runtime permission path exists yet. | G009 correction of false completion | `cargo test -p harness-core --test extension_manifest_test`; `cargo test -p harness --test config_docs_reference_test extension_strategy_test::extension_strategy_documents_descriptor_only_manifest_seam` | Descriptor validation requires public permission names only as metadata. Extension-provided tools are not registered or executed in V1; no runtime permission path exists yet. | yes | n/a |
| 603 | Replay support for extension manifests is limited to static descriptor/config metadata; it does not render extension tool events or load extension code. | G009 correction of false completion | `cargo test -p harness-core --test extension_manifest_test`; `cargo test -p harness --test config_docs_reference_test extension_strategy_test::extension_strategy_documents_descriptor_only_manifest_seam` | Replay support is descriptor/config metadata only and does not render extension tool events. Docs forbid manifest discovery/loading during replay. | yes | n/a |
| 604 | Built-in lifecycle command hooks are migrated onto the same hook seam planned for future extensions. | §3.3 exclusion: Command and hook seam implementation | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Command and hook seam implementation |
| 606 | Hook phases are explicitly modeled for message received, provider params, context transform, tool preflight, tool result, agent turn finish, session idle, and compaction request. | §3.3 exclusion: Command and hook seam implementation | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Command and hook seam implementation |
| 609 | External executable/script plugins are deferred until command mediation, sandboxing, and replay-safe manifests are proven. | G008 final reconciliation policy row | `cargo test -p harness --test config_docs_reference_test extension_strategy_documents_descriptor_only_manifest_seam`; `cargo test -p harness-core --test extension_manifest_test` | Roadmap policy checkbox is checked because docs/tests prove executable plugin behavior is not a V1 runtime host: extension strategy labels plugin hooks post-V1 and `ExtensionManifestV1` rejects executable host fields. | yes | n/a |
| 611 | Active upstream plugin compatibility is explicitly post-V1. | G008 final reconciliation policy row | `cargo test -p harness --test config_docs_reference_test extension_strategy_documents_descriptor_only_manifest_seam`; `cargo test -p harness-core --test extension_manifest_test` | Roadmap policy checkbox is checked because upstream compatibility remains post-V1 while the V1 descriptor-only manifest seam is guarded by parser/schema/docs tests. | yes | n/a |
| 612 | Built-in extension-like features are disableable by stable ids before any external plugin runtime is introduced. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 614 | Extension strategy follows the extension-first and TypeScript baselines: keep core changes small, keep authority explicit, and require conformance evidence for every new extension surface. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 620 | Event-sourced team primitives exist for create/status/message/task/shutdown/delete as primitive event tools, not as full Team Mode. | WS6/team primitive evidence rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; doctor/team docs rows | primitive team tool/docs evidence passed; post-V1 Team Mode boxes excluded | yes | n/a |
| 623 | `team_list` exists. | WS6/team primitive evidence rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; doctor/team docs rows | primitive team tool/docs evidence passed; post-V1 Team Mode boxes excluded | yes | n/a |
| 624 | Team doctor checks exist. | WS6/team primitive evidence rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; doctor/team docs rows | primitive team tool/docs evidence passed; post-V1 Team Mode boxes excluded | yes | n/a |
| 625 | Team docs clearly label the current scope as a V1 primitive or post-V1 optional layer. | WS6/team primitive evidence rows | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; doctor/team docs rows | primitive team tool/docs evidence passed; post-V1 Team Mode boxes excluded | yes | n/a |
| 627 | Declared team registry support exists. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 628 | Team worktree support exists. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 629 | Team tmux visualization exists. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 630 | Team mailbox artifacts and resume semantics are documented and tested. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 631 | Full Team Mode is treated as post-V1 unless deliberately pulled into the release scope. | G008 final reconciliation policy row | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | Roadmap policy checkbox is checked because V1 exposes only primitive team tools and docs/catalog rows explicitly exclude full Team Mode registry, worktree, tmux, and mailbox behavior. | yes | n/a |
| 636 | Ralph loop / ultrawork loop. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 637 | Todo enforcer or autonomous idle continuation loop. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 638 | Prometheus / Metis / Momus / Atlas orchestration stack. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 639 | Full specialist persona catalog. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 640 | Skill-embedded MCP lifecycle. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 641 | MCP OAuth lifecycle. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 642 | Browser automation / Playwright skill bundle. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 643 | Media analysis / `look_at` / multimodal-looker. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 644 | Interactive tmux terminal tool. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 645 | Arbitrary upstream plugin loading. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 646 | Upstream server/share/auth/account product surfaces. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 647 | Desktop, web, mobile, or PWA clients. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 648 | IDE integration, editor-selection sync, or editor-side diff control. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 649 | GitHub Action, Slack, Discord, Telegram, OpenClaw, or other remote collaboration bots. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 651 | Cloud, enterprise, billing, analytics, telemetry, or hosted API surfaces. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 652 | JS/QuickJS/WASM extension marketplace or broad extension runtime. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 653 | Beads, Agent Mail, RCH, swarm, or external issue/validation-broker coordination systems. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 655 | Math-heavy adaptive optimization controllers, shadow dual execution, online policy evaluation, or VOI planners. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 657 | Broad non-OpenAI-compatible provider transports beyond the implemented execution path. | §3.3 exclusion: Post-V1 roadmap sections | source-derived roadmap parser matched fixed exclusion groups | Excluded from denominator; checkbox remains unchecked unless it was already a non-release non-goal statement | no | Post-V1 roadmap sections |
| 661 | Do not add a second build system such as Bazel beside the Cargo workspace. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 662 | Do not copy another harness architecture mechanically; extract behavior and reimplement it through Harness modules and event contracts. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 664 | Do not use desktop/cloud/team features as V1 release blockers. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 665 | Do not use source-brand parity claims as release claims without current Harness evidence. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 667 | Do not add broad compatibility shims that bypass canonical Harness tool ids, permissions, or replay safety. | Cross-workstream evidence rows | `cargo test --workspace --all-features`; final release-readiness lanes | source-derived roadmap item remains checked with matching progress evidence | yes | n/a |
| 680 | A V1 claim-to-evidence matrix maps every release-facing claim to a deterministic test, manual artifact, fixture, command output, or explicit documented limitation. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 683 | A release blocker taxonomy classifies open work as correctness, safety, UX, docs, provider, performance, or evidence work so checked foundations are not confused with V1-quality surfaces. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 686 | Release-facing performance, startup, binary-size, provider, and parity claims cite current artifacts with run provenance and fail closed when evidence is stale or partial. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 689 | The V1 closeout bundle includes an operator-readable summary that ties README/config/docs claims back to the evidence matrix. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 694 | A scripted operator happy path covers install, config, provider readiness, TUI startup, one tool-enabled prompt, permission approval/denial, edit review, diff inspection, resume, replay, doctor, and support export. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 697 | TUI visual signoff artifacts cover at least one normal prompt flow, one permission/question flow, one provider/tool failure flow, and one resume flow. | G007 TUI signoff manifest + PTY lane | `env RUST_TEST_THREADS=1 cargo test -p harness-testkit --test pty_e2e`; `scripts/test-lanes.sh signoff-pty --artifact-dir target/test-lanes/ultragoal-g007-signoff-pty` | Manifest and PTY artifacts cover startup/prompt, command palette, session resume, permission/question, provider/tool failure, and diff review; signoff summary reports PASS=3 FAIL=0. | yes | n/a |
| 699 | The TUI has release-quality operator surfaces for model/provider status, permission scope, tool progress, diff review, session navigation, and recoverable failure states. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 702 | TUI polish inspired by source-reference terminal tools is scoped to terminal-first V1 surfaces: prompt history, session sidebar/search, diff review, model/status clarity, and question/permission overlays; desktop/mobile/PWA polish remains post-V1. | WS8/WS7 TUI and evidence rows | `cargo test -p harness-tui`; `RUST_TEST_THREADS=1 scripts/test-lanes.sh signoff-pty`; `scripts/test-lanes.sh signoff-binary` | deterministic TUI tests and signoff artifacts passed in recorded evidence | yes | n/a |
| 709 | A V1 provider support matrix states the supported execution path, known limits, model fallback policy, credential expectations, and named error categories. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 712 | Provider errors have stable user-actionable categories and TUI/headless recovery text for missing credentials, invalid credentials, rate limits, context overflow, unsupported tool calls, malformed streams, and transport failures. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 716 | A permission threat model explains that permissions are an operator approval layer rather than a sandbox, names the mutable surfaces, and links prompt promises to runtime enforcement fixtures. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 719 | Privacy and local-data notes explain what can leave the machine, where sessions and artifacts live, how redaction works, and which cloud/telemetry features are absent unless added later. | WS4 provider taxonomy rows | `cargo test -p harness-providers`; provider/TUI/headless focused tests | provider categories, remediation, and surfacing evidence passed | yes | n/a |
| 725 | Resume acceptance evidence covers an interrupted realistic coding session with tool artifacts, permission state, summaries, and restored context. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 727 | Large-session list/resume/search behavior is measured before V1 makes fast long-session claims. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 729 | Crash-resilient session write behavior is tested at the event-store boundary and documented for support/debugging. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 731 | Compaction preservation tests prove file, tool, skill, todo, and plan context survive summary generation well enough for a resumed session to continue without guessing. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 734 | Session list, search, tree, fork, and clone surfaces expose meaningful generated or editable titles instead of only paths or opaque run ids. | WS5 session/resume/compaction rows | `cargo test -p harness-core --test coord_test`; `scripts/test-lanes.sh perf`; `scripts/test-lanes.sh simulation` | resume, crash-tail, compaction, lineage, and perf/simulation evidence passed | yes | n/a |
| 739 | The minimal V1 built-in skill set is named explicitly and each skill has a real body, use-when/do-not-use-when guidance, docs, disablement behavior, and tests before it is advertised as shipped. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 742 | README and docs do not advertise `git-master`, `review-work`, `frontend-ui-ux`, or equivalent skills as release-ready until the evidence matrix links them to V1-quality bodies and tests. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 745 | Prompt bodies and category profiles distinguish template scaffolding from agent-specific operating guidance, with golden snapshots for the shipped prompt set. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 748 | Intent-gate behavior is covered by prompt fixtures for ambiguous requests, investigation requests, implementation requests, and planning requests. | WS3 built-in skill rows | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test built_in_skill_docs_and_capability_map_cover_catalog_stable_ids` | built-in skills, stable ids, disablement, docs, and doctor evidence passed | yes | n/a |
| 753 | A typed extension manifest seam defines optional tools, hooks, commands, prompts, MCP bundles, diagnostics, provider decorators, capability ids, disablement state, and replay-safe descriptor metadata. | G009 correction of false completion | `cargo test -p harness-core --test extension_manifest_test`; `cargo test -p harness --test config_docs_reference_test extension_strategy_test::extension_strategy_documents_descriptor_only_manifest_seam` | Descriptor-only manifest seam ships schema/parser/docs for all descriptor classes plus static replay-safe metadata, not extension tool event rendering. | yes | n/a |
| 756 | Extension-provided behavior is not registered, executed, permissioned, or replay-rendered in V1; future runtime support must go through coordinator-owned permissions, event append authority, artifact redaction, and replay side-effect boundaries. | G009 correction of false completion | `cargo test -p harness-core --test extension_manifest_test`; `cargo test -p harness --test config_docs_reference_test extension_strategy_test::extension_strategy_documents_descriptor_only_manifest_seam` | Manifest behavior is descriptor-only. Docs/tests forbid runtime registration, execution, runtime permission paths, extension event replay rendering, external code loading, or session mutation. | yes | n/a |
| 758 | Built-in extension-like features use the public Harness interfaces and stable ids before external plugin compatibility is considered. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 760 | Migration notes explain which source-inspiration, desktop/mobile, browser/media, and plugin surfaces are unsupported by design for V1. | WS1/WS3/WS7 extension rows | `cargo test -p harness --test config_docs_reference_test`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json` | extension strategy/doctor readiness evidence passed | yes | n/a |
| 766 | README quick start is accurate and minimal. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 767 | Config guide is accurate for V1. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 768 | Architecture guide is accurate for V1. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 769 | Testing guide is accurate for V1. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 770 | Native tool catalog exists. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 771 | Agent and subagent guide exists. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 772 | Permissions guide exists. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 773 | Sessions and replay guide exists. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 774 | Extension strategy guide exists, clearly marking post-V1 plugin work. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 775 | Privacy and local-data notes explain what leaves the machine, where sessions and artifacts are stored, how redaction works, and that telemetry/cloud features are absent unless explicitly introduced later. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 778 | Migration notes explain which source-inspiration areas are unsupported by design. | WS1 documentation rows | `cargo test -p harness --test event_docs_reference_test`; `cargo test -p harness --test config_docs_reference_test` | docs-reference and local link integrity evidence passed | yes | n/a |
| 782 | Freeze V1 scope and add the release blocker taxonomy plus claim-to-evidence matrix before adding new user-facing release claims. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 784 | Clean documentation and asset drift first, because stale prompt/agent references make every later checklist item ambiguous. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 786 | Lock install, config, provider, `doctor`, and one prompt smoke from outside the repository, because V1 starts with a user being able to run the binary. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 788 | Add the scripted operator happy path and TUI visual signoff checklist before treating the TUI as release-ready. | G007 TUI signoff manifest + existing happy path | `scripts/test-lanes.sh signoff-pty --artifact-dir target/test-lanes/ultragoal-g007-signoff-pty`; `cargo test -p harness-tui --test deterministic_render_test` | Existing happy-path recording remains green, and the new manifest is the checklist for startup, command palette, session picker/resume, permission/question, provider/tool failure, and diff review. | yes | n/a |
| 790 | Make startup prompt-first and improve prompt history, because this is the highest-frequency vanilla local-coding interaction. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 792 | Centralize command/keybinding metadata before expanding keybindings, slash commands, or help text. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 794 | Improve permission modal clarity before adding more powerful tools, so new capabilities inherit a clear approval surface. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 796 | Harden provider error categories, fallback policy, and support-matrix docs before claiming provider breadth beyond the implemented execution path. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 798 | Lock resume, large-session, crash-write, and compaction preservation evidence before making session durability or long-session performance claims. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 800 | Add the permission threat model and privacy/local-data notes before adding more extension-like or externally integrated capabilities. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 802 | Add prompt bodies, prompt snapshots, and task/permission fixtures before expanding subagent or skill catalogs. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 804 | Add an `AgentCatalog`-style resolution seam before relying on category fallbacks, hidden profiles, or category-specific prompt appends. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 806 | Add model-visible session tools after session/replay acceptance tests are documented. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 808 | Add dedicated `background_cancel` after sync/background task contracts are documented. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 810 | Add AST-grep search after the native tool catalog shape and parity-test harness are stable, then update the catalog and matrix for AST-grep. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 813 | Add V1 built-in skills after skill schema, precedence, disablement, and progressive disclosure are documented. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 815 | Define the typed extension manifest seam before migrating lifecycle command hooks onto it or marking the extension strategy guide complete. | G006 extension-manifest-seam | `cargo test -p harness-core --test extension_manifest_test`; `cargo test -p harness --test config_docs_reference_test extension_strategy_test::extension_strategy_documents_descriptor_only_manifest_seam` | Descriptor-only manifest seam shipped before future command-hook migration; checkbox is checked. | yes | n/a |
| 817 | Add doctor readiness checks once the prompt, skill, tool, and agent catalog contracts are stable enough for diagnostics to enforce. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |
| 819 | Keep Team Mode primitive-only for V1 unless the V1 release scope is deliberately re-opened; otherwise document it as a post-V1 optional layer. | WS7 release evidence rows | `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh perf`; claim-evidence docs tests | release blocker taxonomy, claim matrix, budgets, and lane artifacts passed | yes | n/a |

### Honest limitations (§16.4)

- Historical note superseded by G006: the typed extension manifest now ships as descriptor-only schema/validation/replay metadata; command-hook execution, runtime extension loading, and plugin-host behavior remain post-V1.
- Historical note superseded by G005: `ast_grep_replace` now ships only behind edit permission with dry-run/apply safety gates; `ast_grep_search` remains the read-only structural search tool.
- Native screenshot signoff remains env-gated; G009 keeps reference-image comparison out of scope while preserving the deterministic/PTY flow manifest, and the native screenshot checkbox remains a documented gap until `HARNESS_NATIVE_VISUAL=1` plus `DISPLAY` artifacts exist.
- External editor/assistant/agent runtime adapters remain excluded; default ignoring and explicitly configured skill-root discovery precedence are now covered.
- Binary-size and production-class performance claims remain documented limitations; local perf artifacts are fresh and fail-closed, but no release claim should imply production hardware ratification.
- Live provider and native visual signoff lanes are provenance classes, not deterministic blockers for this slice unless their environment gates are explicitly supplied.

## Evidence 2026-05-30 corrected binary smoke closeout
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Binary signoff TUI startup artifact is PTY-backed and succeeds | WS7/WS10 | focused smoke + signoff lane artifact | `cargo fmt --all -- --check`; `env HARNESS_BINARY_SMOKE=1 HARNESS_BINARY_SMOKE_ARTIFACT_DIR=/tmp/harness-binary-smoke-artifacts-pty-final-20260530001133 cargo test -p harness --test binary_smoke -- --ignored --exact`; `scripts/test-lanes.sh signoff-binary` | Focused smoke passed; `/tmp/harness-binary-smoke-artifacts-pty-final-20260530001133/tui-startup.status.txt` contains `success=true`. Signoff summary `target/test-lanes/20260530-000459/summary.txt` reports `PASS=1 FAIL=0`; `target/test-lanes/20260530-000459/signoff-binary/stages/harness_binary_smoke/artifacts/tui-startup.status.txt` and `tool-prompt.status.txt` both contain `success=true`. | PASS | launching TUI startup without a PTY, accepting raw-mode failure, omitting TUI startup artifacts, or letting tool prompt artifacts fail makes the focused smoke or signoff lane fail |

## Evidence 2026-05-30 final progress-log audit and completion statement
| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Final progress-log audit confirms checked PRD boxes and flipped roadmap boxes have evidence | §18 | source-derived closeout audit | `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `python3 scripts/check-forbidden-branding.py --root .`; `python3 scripts/check-test-suite-gates.py`; `cargo test -p harness --test config_docs_reference_test`; `cargo check -p harness --all-features`; `env HARNESS_BINARY_SMOKE=1 HARNESS_BINARY_SMOKE_ARTIFACT_DIR=/tmp/harness-binary-smoke-artifacts-pty-confirm-20260530001352 cargo test -p harness --test binary_smoke -- --ignored --exact`; `scripts/test-lanes.sh signoff-binary`; source-derived roadmap parser recorded above | PRD acceptance boxes are all checked with matching progress rows; final roadmap parser result remains 357 total boxes, 56 excluded by fixed §3.3 groups, 301 included denominator, 301 included checked, 100.00% included pre-V1 completion. The progress log records direct workspace clippy evidence, each excluded roadmap checkbox, honest limitations, final smoke artifacts, and classified ignored tests. The historical slice was complete for the scoped PRD at that time; then-remaining typed extension manifest/runtime seams, AST-grep replace, native visual/image signoff, external compatibility adapters, production-class perf/binary-size claims, and live-provider/native-visual provenance were explicitly documented limitations or post-V1/final-slice exclusions. The strict-V1 closeout evidence below supersedes the AST-grep replace limitation. | PASS | checking a PRD/roadmap box without an evidence row, adding stale or unverifiable evidence, changing the roadmap excluded set, or removing honest limitation rows invalidates closeout |

Final closeout statement: all in-scope §4–§14 acceptance criteria are checked with evidence, §15/§16 audits are green, and the measured included pre-V1 roadmap completion is 100.00% (301/301 included boxes).

## Evidence 2026-05-30 strict V1 roadmap closeout start

This section records the current strict-V1 closeout run from `.omx/plans/pre-v1-roadmap-closeout-consensus.md` after the readiness docs were restored. The durable execution ledger is `.omx/ultragoal/ledger.jsonl`; the pre-roadmap-edit unchecked baseline is `.omx/ultragoal/evidence/G001-prep-classification/roadmap-classification-baseline.md` and was generated from `rg -n '^[- ]*\[ \]' docs/roadmap-v1.md` before any roadmap checkbox edits in this run.

### Strict-V1 closeout scenario contract

| Scenario | Evidence pointer | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|
| Readiness docs exist and active references are non-dangling | `docs/v1-release-readiness-slice-prd.md`; `docs/v1-release-readiness-slice-progress.md`; `docs/roadmap-v1.md`; `docs/budgets.md` | `cargo test -p harness --test config_docs_reference_test readiness_closeout_docs_are_current_and_back_roadmap_claims` | Restored readiness PRD/progress docs; roadmap and budgets point to an existing current progress log. | PASS | deleting either readiness doc or removing the roadmap/budget progress reference makes the docs-reference test fail |
| Current strict-V1 unchecked roadmap map exists before checkbox edits | `.omx/ultragoal/evidence/G001-prep-classification/roadmap-unchecked-rg.txt`; `.omx/ultragoal/evidence/G001-prep-classification/roadmap-classification-baseline.md` | `rg -n '^[- ]*\[ \]' docs/roadmap-v1.md` | Baseline captured 56 unchecked roadmap items and classified each by owner, evidence requirement, and do-not-flip condition before implementation/roadmap edits. | PASS | changing roadmap unchecked state without refreshing the baseline/progress row invalidates this closeout gate |
| Checked release-blocker rows remain evidence-backed | `docs/claim-evidence-matrix.md`; this progress log | `cargo test -p harness --test config_docs_reference_test readiness_closeout_docs_are_current_and_back_roadmap_claims` | The release-blocker paragraph still requires current command output or artifact roots before checking roadmap boxes, and this log plus the claim matrix carry evidence for checked blocker rows. | PASS | checking a release-blocker roadmap row without adding progress or claim-matrix evidence makes the docs-reference test fail |

### Release-blocker checked rows backed by this progress log or the claim matrix

- The README has one clear install, configure, and run path for a new user.
- `docs/config.md` and checked-in schemas/examples agree on every public key.
- `docs/architecture.md` describes all V1 runtime invariants and public events accurately.
- `docs/testing.md` names the required V1 verification lanes and artifact expectations.
- `cargo fmt --all -- --check` passes.
- `cargo check --workspace` passes.
- `cargo test --workspace --all-features` passes, or any live-only exclusions are explicitly documented.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes.
- `cargo run -p harness -- --config configs/harness.example.jsonc config validate` passes.
- `cargo run -p harness -- --config configs/harness.example.jsonc doctor` gives actionable output for missing provider, model, MCP, tool, and agent setup.
- A manual TUI happy path has been recorded for start, prompt, permission, tool call, edit, resume, and quit.
- Release-facing speed, provider breadth, compatibility, or parity claims are backed by current evidence artifacts, not README assertions or inspiration claims.
- The V1 closeout bundle includes artifact roots for the release-blocking lanes, with `summary.txt`, `env.txt`, per-stage `command.txt`, `stdout.txt`, `stderr.txt`, `status.txt`, and `verification.txt` where the lane runner emits them.

## Evidence 2026-05-30 strict V1 skill-root adapter policy

| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| External editor/assistant/agent skill roots are adapter-deferred and ignored by default | G003 skill-root-policy | red/green discovery test + docs-reference guard | `cargo test -p harness-tools --test skill_load_discovery_test skill_discovery_ignores_external_adapter_roots_unless_explicitly_configured`; `cargo test -p harness --test config_docs_reference_test config_docs_capture_v1_skill_contract_and_authoring_guide` | Harness-owned `.agent-harness/skills`, `.harness/skills`, and configured `~/.config/agent-harness/skills` roots remain default; `.external-editor/skills`, `.assistant/skills`, `.agents/skills`, and user equivalents load only when explicitly configured in `skills.project_roots` or `skills.global_roots`. | PASS | adding external adapter roots to defaults, removing the explicit-config test, or deleting adapter-deferred docs makes the focused tests fail |
| Full skill discovery precedence covers Harness-owned roots, built-in skill ids, configured global roots, and explicit compatibility imports | V1 skill precedence closeout | red/green discovery tests + docs-reference guard | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test config_docs_capture_v1_skill_contract_and_authoring_guide` | `skill_load_discovery_test` reported 23 passed. It proves `.agent-harness/skills` beats `.harness/skills` and global roots, imported `.external-editor/skills`, `.assistant/skills`, and `.agents/skills` cannot shadow Harness-owned/non-compat roots even when listed earlier, project compatibility roots beat global compatibility roots when no higher-precedence duplicate exists, duplicates are reported as `shadowed`, and shipped `git-master`, `review-work`, and `frontend-ui-ux` resolve to `skill:project:*` over global/imported duplicates. The docs guard checks the public precedence wording in `docs/config.md` and `docs/starter-skills.md`; roadmap line 231 is checked with this evidence. | PASS | reordering compatibility roots before Harness-owned/non-compat roots, dropping duplicate `shadowed` diagnostics, letting global/imported duplicates win over shipped built-ins, or deleting the public precedence docs makes the focused tests fail |

## Evidence 2026-05-30 strict V1 command/hook lifecycle map

| Requirement | Workstream | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Markdown command files/interpolation have a strict-V1 stance | G004 command-hook-lifecycle-map | docs-reference guard | `cargo test -p harness --test config_docs_reference_test extension_strategy_documents_command_hook_lifecycle_map` | Strict V1 keeps markdown command files, `$ARGUMENTS` substitution, rules/context injection, and command interpolation intentionally unsupported; first-party TUI slash actions remain native UI actions and no command rendering executes during replay. | PASS | advertising executable markdown command interpolation or deleting the unsupported/replay anchors makes the docs-reference test fail |
| Hook lifecycle phases are mapped to native/unsupported/post-V1 statuses | G004 command-hook-lifecycle-map | source-derived docs-reference guard | `cargo test -p harness --test config_docs_reference_test extension_strategy_documents_command_hook_lifecycle_map` | The lifecycle map includes every `HookLifecycleEvent::as_str()` value from `crates/harness-core/src/config.rs` as `native` and labels markdown command/context/plugin seams as `intentionally_unsupported` or `post_v1`. | PASS | adding a hook enum value without a lifecycle-map row, or using a status outside native/fallback/intentionally_unsupported/post_v1, makes the guard fail |
| Hooks remain coordinator/replay safe | G004 command-hook-lifecycle-map | coord tests | `cargo test -p harness-core --test coord_test lifecycle_hooks_cover_provider_subagent_and_permission_events`; `cargo test -p harness-core --test coord_test critical_hook_failure_fails_closed_and_records_metadata`; `cargo test -p harness-core --test coord_test replay_suppresses_hooks_but_preserves_hook_events` | Hook tests cover provider/subagent/permission events, critical failure fail-closed metadata, and replay suppression/preservation. Roadmap command/hook boxes are checked with this evidence; external plugin/typed manifest runtime remains separate. | PASS | allowing hooks to grant permissions, bypass event append, or execute during replay makes the focused coord tests fail |


## Evidence 2026-05-30 strict V1 AST-grep replace safety gate

| Requirement | Goal | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| `ast_grep_replace` is registered only as an edit-permission native tool | G005 ast-grep-replace-gate | permission/catalog tests | `cargo test -p harness-core --test permission_policy_supports_native_tool_permission_kinds_test`; `cargo test -p harness-tools --test native_tool_parity_matrix_test` | `permission_kind_for_tool("ast_grep_replace") == EditFs`; catalog row documents public `edit`, mutating behavior, dry-run/apply diff artifacts, and runtime registry parity. | PASS | mapping the tool to `codesearch`/read-only, removing the catalog row, or dropping strict schema/parity coverage makes the focused tests fail |
| Dry-run/apply behavior is path-safe and artifact-backed | G005 ast-grep-replace-gate | native tool test + ADR | `cargo test -p harness-tools --test native_ast_grep_replace_test` | Dry-run leaves files unchanged and writes a diff artifact; apply mutates only workspace files after byte-range/current-content validation through atomic writes; traversal, unknown fields, unsupported language, invalid patterns, missing adapter, and truncated apply are rejected. | PASS | allowing adapter direct mutation, applying a truncated match set, accepting traversal/unknown args, or omitting diff artifacts makes the focused test fail |
| Coordinator edit policy blocks replace before adapter execution | G009 correction of false completion | coordinator auth tests | `cargo test -p harness-core --test coord_ast_grep_auth_test -- --nocapture` | A worker with `ast_grep_replace` in its toolset and a path-scoped deny rule for `src/` receives `PermissionDenied` for `paths: ["src/lib.rs"]`; no `ToolCallStarted` event is emitted. A separate allowed `paths: ["docs/readme.md"]` call reaches `ToolCallStarted`/`ToolCallFinished`. | PASS | moving permission checks after adapter execution, ignoring list-valued `paths`, or changing `ast_grep_replace` capability away from `EditFs` makes the focused tests fail |

## Evidence 2026-05-30 strict V1 typed extension manifest seam

| Requirement | Goal | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| `ExtensionManifestV1` is a typed descriptor/schema for all strict-V1 descriptor classes | G006 extension-manifest-seam | core parser/schema test | `cargo test -p harness-core extension_manifest` | Parser/schema covers stable extension ids, capability ids, disablement defaults, tools, hooks, commands, prompts, MCP bundles, diagnostics, provider decorators, public tool permissions, and replay metadata; `configs/extension-manifest.v1.schema.json` matches the generated schema. | PASS | deleting a descriptor class, changing schema without regenerating the checked-in schema, accepting duplicate capability ids, or omitting tool permissions makes the focused test fail |
| Manifest parsing remains descriptor-only and cannot become a plugin host silently | G009 correction of false completion | negative parser test + doctor JSON | `cargo test -p harness-core extension_manifest`; `cargo test -p harness --test config_schema_cli_test doctor_cli_json_reports_extension_roadmap_readiness_separately` | Unknown executable fields such as tool `command` are rejected; runtime effects report `runtime_effects_scope: "descriptor_only"`, `registers_tools=false`, `executes_commands=false`, `launches_mcp=false`, `invokes_provider_decorators=false`, `loads_external_code=false`, and `mutates_sessions=false`; doctor reports `typed_extension_manifest.status=shipped_descriptor_only`. | PASS | adding manifest runtime discovery/loading/execution, accepting executable payload fields, reporting a runtime permission path, or reporting the seam as a runtime plugin host makes the focused tests fail |
| Replay renders extension metadata from static descriptor fields only | G006 extension-manifest-seam | replay metadata test + docs guard | `cargo test -p harness-core extension_manifest`; `cargo test -p harness --test config_docs_reference_test extension_strategy_documents_descriptor_only_manifest_seam` | `replay_metadata()` exposes extension id, capability ids, disabled capabilities, descriptor counts, and replay label without loading extension code; docs state replay never discovers manifests, registers tools, executes commands, launches MCP, invokes decorators, or mutates sessions. | PASS | allowing dynamic replay text, deleting replay-side-effect docs, or making replay depend on extension package loading makes the focused tests fail |

## Evidence 2026-05-30 strict V1 TUI signoff manifest

| Requirement | Goal | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| TUI signoff manifest names all strict-V1 operator flows without requiring reference-image comparison | G009 correction of false completion | manifest + deterministic render snapshots | `cargo test -p harness-tui --test tui_signoff_manifest_test`; `cargo test -p harness-tui --test deterministic_render_test`; `cargo test -p harness --test config_docs_reference_test` | `docs/tui-signoff-manifest.v1.json` uses schema `harness-tui-signoff-manifest-v1` and declares startup, command palette, session picker/resume, permission/question, provider/tool failure, and diff review through deterministic owner tests and PTY stages. New deterministic snapshots cover command palette, session history picker, permission draft overlay, question prompt, replay read-only, replay failure, and tool/diff/failure rows. Reference-image comparison is explicitly not required. | PASS | deleting a required flow, removing owner tests, reintroducing required reference assets, or changing render output without accepting snapshots makes the focused test fail |
| PTY signoff records manifest provenance and the happy path artifact still covers start/prompt/permission/tool/edit/resume/quit | G007 tui-signoff-evidence | PTY tests + lane artifact | `env RUST_TEST_THREADS=1 cargo test -p harness-testkit --test pty_e2e`; `env RUST_TEST_THREADS=1 HARNESS_TUI_PTY_SIGNOFF=1 cargo test -p harness-tui --test pty_e2e`; `scripts/test-lanes.sh signoff-pty --artifact-dir target/test-lanes/ultragoal-g007-signoff-pty` | Focused PTY tests passed; `target/pty-visual-artifacts/tui-signoff-manifest.v1.json` and `.md` were written by the harness-testkit PTY test. Signoff summary `target/test-lanes/ultragoal-g007-signoff-pty/summary.txt` reports `PASS=3 FAIL=0`, including `harness_testkit_pty_e2e`, `harness_tui_pty_e2e`, and `harness_tui_happy_path_pty`; happy-path artifacts include `tui-happy-path-recording.json`, `prompt.events.jsonl`, and `scenario.events.jsonl`. | PASS | removing the manifest artifact write, dropping the happy-path stage, failing to set `RUST_TEST_THREADS=1`, or regressing PTY startup/palette/resume behavior makes the focused test or signoff lane fail |
| Native visual screenshot evidence is explicit, env-gated, and not silently claimed from PTY evidence | G007 tui-signoff-evidence | native gap guard + docs | `cargo test -p harness-testkit --test native_visual_e2e`; `printf 'HARNESS_NATIVE_VISUAL=%s DISPLAY=%s\n' "${HARNESS_NATIVE_VISUAL-}" "${DISPLAY-}"` | Non-ignored native tests passed with `native_visual_fails_closed_without_env` and metadata coverage; local env had no `HARNESS_NATIVE_VISUAL` or `DISPLAY`, so `signoff-native` was not claimed. Roadmap line 367 remains unchecked for native screenshot signoff; roadmap lines for required flow artifacts and the visual checklist are checked with manifest/PTY evidence, while reference-image comparison remains out of scope. | PASS | treating missing `HARNESS_NATIVE_VISUAL=1`/`DISPLAY` as a native screenshot pass, deleting `native_visual_fails_closed_without_env`, or checking the native screenshot line without artifacts invalidates the evidence |

## Evidence 2026-05-30 strict V1 final reconciliation

| Requirement | Goal | Evidence type | Verification | Observed result | Status | breaks if |
|---|---|---|---|---|---|---|
| Plugin and upstream-compatibility policy boxes are checked only as post-V1 deferrals | G008 final-reconciliation-gates | docs-reference + descriptor-only parser tests | `cargo test -p harness --test config_docs_reference_test extension_strategy_documents_descriptor_only_manifest_seam`; `cargo test -p harness-core --test extension_manifest_test` | `docs/extension-strategy.md` labels executable plugin hooks and upstream compatibility as post-V1, while `ExtensionManifestV1` rejects executable host behavior. The roadmap deferral-policy boxes are checked; explicit implementation boxes such as arbitrary upstream plugin loading remain unchecked. | PASS | checking a plugin runtime/compatibility implementation box, accepting executable manifest fields, or deleting the post-V1 docs anchors invalidates the row |
| Full Team Mode is recorded as post-V1 while V1 keeps primitive team tools | G008 final-reconciliation-gates | native tool catalog parity + docs-reference guard | `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness --test config_docs_reference_test` | `docs/native-tool-catalog.md` states `team_list` is only a primitive projection reader and full Team Mode worktrees/tmux/mailbox/registry/file claims are out of V1 scope. The roadmap policy box is checked; implementation boxes for registry, worktree, tmux, and mailbox support remain unchecked. | PASS | adding Team Mode implementation claims without matching runtime/tests or deleting the primitive-only catalog row invalidates the row |
| Remaining unchecked roadmap boxes are intentional post-V1 or native-visual gaps | G008 final-reconciliation-gates | source-derived roadmap scan + claim matrix rows | `rg -n '^\s*[-*]\s*\[ \]' docs/roadmap-v1.md`; `cargo test -p harness --test config_docs_reference_test` | The remaining unchecked list is limited to post-V1 direction/explicit post-V1 items, Team Mode implementation boxes, and env-gated native visual signoff. External skill-root compatibility discovery precedence is now checked with strict V1 skill precedence evidence. `docs/claim-evidence-matrix.md` records matching PASS rows or limitations for the checked strict-V1 claims. | PASS | flipping an unchecked post-V1/native-visual box without current artifact evidence, or adding a checked box with no matrix/progress row, invalidates final reconciliation |
| Post-cleaner verification passed after strict-V1 closeout edits | G008 final-reconciliation-gates | local quality-gate command output | `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `python3 scripts/check-forbidden-branding.py --root .`; `python3 scripts/check-test-suite-gates.py`; `cargo test -p harness --test config_docs_reference_test`; `cargo test -p harness-core --test extension_manifest_test`; `cargo test -p harness-tools --test native_ast_grep_replace_test`; `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness-tui --test deterministic_render_test`; `cargo test -p harness-tui --test tui_signoff_manifest_test`; `env RUST_TEST_THREADS=1 cargo test -p harness-testkit --test pty_e2e`; `cargo test -p harness-testkit --test native_visual_e2e`; `cargo check -p harness --all-features`; `cargo run -p harness -- --config configs/harness.example.jsonc config validate`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor` | All listed post-cleaner commands passed locally. `native_visual_e2e` reported 2 passed and 1 ignored because managed native visual signoff remains env-gated. `doctor` reported 12 passed, 0 warnings, 0 failures. | PASS | any listed command fails, or native visual is claimed without `HARNESS_NATIVE_VISUAL=1`/`DISPLAY`, invalidates final quality-gate evidence |
