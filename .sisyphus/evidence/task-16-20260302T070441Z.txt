Task 16 evidence (Wave 3)

Implemented:
- headless scenario runner subcommand: `harness run --scenario golden_path`
- interactive scenario variant: `harness run --scenario golden_path_interactive`
- deterministic mode (`--deterministic`) using FakeClock + UUIDv5 run_id
- `--session-dir`, `--out`, and `--print-run-dir`
- deterministic SHA-256 test: run golden path twice, compare JSONL digest equality

Verification:
- `cargo test -p harness` => PASS
- `HARNESS_DETERMINISTIC=1 cargo run -p harness -- run --scenario golden_path --deterministic` => exits 0

Observed deterministic run output path:
- `.agent-harness/sessions/run_212128dfbe7a59f3975f8f70b1c3407d/events.jsonl`
