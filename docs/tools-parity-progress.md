# Harness Tools Parity Progress

Progress ledger for the Harness tools parity work described in
[`docs/tools-parity-prd.md`](./tools-parity-prd.md).

The table shape follows PRD §9. Every row records a dated unit of work with
source evidence and dogfood artifact provenance. Do not rewrite historical rows;
add superseding rows when status changes.

| Date | Commit | Workstream | Harness source | Harness source | Tests | Dogfood evidence | Status | Notes |
|---|---|---|---|---|---|---|---|---|
| 2026-07-01 | `uncommitted` | Support-export hidden-reasoning redaction and PRD status correction | No new Harness source behavior changed; this row closes Oracle blocker `ses_0e56c47d6ffehVQf5TID3yexTQ` by keeping provider reasoning deltas local to session events and out of support exports. | `crates/harness/src/sessions/export.rs`; `crates/harness/src/sessions.rs`; `docs/tools-parity-prd.md`; `docs/tools-parity-progress.md`; regenerated support bundles under `/tmp/harness-umans-live-20260701-ultrawork` after this fix. | LSP diagnostics clean on changed Rust files; `cargo fmt --all -- --check`; focused regression `cargo nextest run -p harness support_export_omits_provider_reasoning_delta_events -- --nocapture`; targeted CLI export tests `cargo nextest run -p harness --test replay_sessions_cli_test sessions_export -- --nocapture`; `cargo build -p harness`; `GIT_MASTER=1 git diff --check`; `python3 scripts/check-forbidden-branding.py`; `python3 scripts/check-test-suite-gates.py --gate conventions`. | Freshly regenerated GLM/Kimi support bundles no longer contain raw `provider_reasoning_delta` text or the `provider_reasoning_delta` event type while preserving the prior live provider/tool proof from GLM run `prompt_098dba541a3d52c1946c8145d2cead30` and Kimi run `prompt_f1aa1981931c581f98054bcb4417ec10`; scans report raw API key `0`, `.umans.env` `0`, `sk-live-secret` `0`, reasoning-delta event payloads `0`, replay reasoning count `0`, and clean redaction/secret-scan statuses for both bundles. | ready_for_oracle | Support exports now drop hidden provider reasoning delta events and remove their replay count trace before default redaction and fail-closed secret scanning. The remaining `UMANS_AI_CODING_PLAN_API_KEY` occurrence is non-secret provider env-var metadata, not the raw key. The PRD status header now reflects implemented/evidence-backed work instead of claiming the document is planning-only. |
| 2026-07-01 | `uncommitted` | Superseding fresh Umans live tool-use and redaction evidence | No new Harness source behavior changed; this row supersedes the previous live-evidence rows with fresh GLM/Kimi provider execution proof and stricter support-export redaction checks. | `docs/tools-parity-progress.md`; live evidence under `/tmp/harness-umans-live-20260701-ultrawork`; clean redaction workspace under `/tmp/harness-umans-live-20260701-clean`; deterministic lane artifacts under `target/test-lanes/20260630-live-umans-*`. | `/home/urbanbreach/Projects/agent-harness/target/debug/harness --config /tmp/harness-umans-live-20260701-ultrawork/harness.jsonc config validate`; same config `doctor --json`; live GLM prompt with `umans-ai-coding-plan/umans-glm-5.2`; live Kimi prompt with `umans-ai-coding-plan/umans-kimi-k2.7`; `sessions inspect` and `sessions export`; `jq -Rr 'fromjson?'` extraction of provider-started/tool-requested/tool-finished events; secret scans for the raw API key, `.umans.env`, `sk-live-secret`, and `UMANS_AI_CODING_PLAN_API_KEY`; existing final lanes remain `target/test-lanes/20260630-live-umans-fast-final/summary.txt` (`PASS=3 FAIL=0`), `target/test-lanes/20260630-live-umans-quality-final4/summary.txt` (`PASS=2 FAIL=0`), and `target/test-lanes/20260630-live-umans-all-deterministic-final2/summary.txt` (`PASS=18 FAIL=0`). | Live provider/tool proof: GLM run `prompt_098dba541a3d52c1946c8145d2cead30` and Kimi run `prompt_f1aa1981931c581f98054bcb4417ec10` in `/tmp/harness-umans-live-20260701-ultrawork`; both emitted `provider_request_started` for the expected model, `tool_call_requested` for `glob` plus two `read` calls, and successful `tool_call_finished` events for all three calls. Evidence extracts are `evidence-provider-started.tsv`, `evidence-tool-requested.tsv`, and `evidence-tool-finished.tsv`; stream output includes the alpha canary line for both models. Support bundles `glm-support-bundle.json` and `kimi-support-bundle.json` report `secret_scan_status.status=clean`, `secret_finding_count=0`, and no raw key, `.umans.env`, or fake sentinel hits; remaining `UMANS_AI_CODING_PLAN_API_KEY` text is provider-summary metadata at `support.provider_summary.providers.0.api_key_env.0`, not credential material. Stricter clean workspace `/tmp/harness-umans-live-20260701-clean` keeps config and key files outside the snapshotted workspace: full workspace scan found raw key `0`, `.umans.env` `0`, fake sentinel `0`, and env-var-name `0`; Kimi clean run `prompt_690c2a4dffe95ec6852ea0d724e2e4a9` repeated successful `glob`/`read` tool use and clean support export. | verified | Clean GLM reruns `prompt_0324c5af1d00585e8689138a66cce8ad` and `prompt_2aa2ac9777175a4f9127e50f8ab32f1f` timed out while queued before provider execution, so they are retained only as failed-run/export/redaction evidence; GLM provider/tool proof comes from the successful ultrawork run above. The raw credential was not printed into repo/docs/support artifacts. |
| 2026-06-30 | `uncommitted` | Final post-review signoff for Umans live dogfood and snapshot secret-file hardening | Same scope as the following two rows; this row records post-review closure after the reasoning-delta docs caveat was narrowed. | `crates/harness-core/src/coord/snapshot.rs`; `crates/harness-core/src/coord/revert.rs`; `crates/harness-core/src/coord/tests/workspace_snapshot_secret_tests.rs`; `crates/harness-core/src/coord/tests.rs`; `docs/architecture.md`; `docs/sessions-and-replay.md`; `docs/tools-parity-prd.md`; `docs/tools-parity-progress.md` | LSP diagnostics clean on changed Rust files; `cargo fmt --all -- --check`; `cargo build -p harness`; `cargo nextest run -p harness-core`; `cargo nextest run -p harness-core --test coord_test -- --nocapture`; focused snapshot/revert regressions; `cargo nextest run -p harness --test event_docs_reference_test -- --nocapture`; `cargo nextest run -p harness --test config_docs_reference_test -- --nocapture`; `cargo nextest run -p harness --test config_schema_cli_test -- --nocapture`; `cargo nextest run -p harness --test replay_sessions_cli_test sessions_export -- --nocapture`; `cargo nextest run -p harness-core --test native_metadata_replay_test -- --nocapture`; `python3 scripts/check-forbidden-branding.py`; `python3 scripts/check-test-suite-gates.py --gate conventions`; `scripts/test-lanes.sh fast --artifact-dir target/test-lanes/20260630-live-umans-fast-final`; `scripts/test-lanes.sh quality-gates --artifact-dir target/test-lanes/20260630-live-umans-quality-final4`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic --artifact-dir target/test-lanes/20260630-live-umans-all-deterministic-final2`. | Fresh artifacts: `target/test-lanes/20260630-live-umans-fast-final/summary.txt` (`PASS=3 FAIL=0`), `target/test-lanes/20260630-live-umans-quality-final4/summary.txt` (`PASS=2 FAIL=0`), and `target/test-lanes/20260630-live-umans-all-deterministic-final2/summary.txt` (`PASS=18 FAIL=0`). CLI/tmux surface QA inspected clean GLM `prompt_37ded366dff75b958d85883b182aed03` and Kimi `prompt_c9aff705cab05b6fad3fbc74673fc761` sessions, and the missing-run-id path failed as expected. Fresh support exports `/tmp/glm-support-export-final-refresh2.json` and `/tmp/kimi-support-export-final-refresh2.json` contain the expected canary content and no blocked credential markers. | verified | Targeted reviewer and Oracle rechecks passed after narrowing `docs/tools-parity-prd.md` and `docs/architecture.md`: live event logs may include provider reasoning delta events as local evidence, while raw provider payloads, credentials, and hidden prompt/config instruction secrets remain excluded from public/support material. |
| 2026-06-30 | `uncommitted` | Superseding Umans live signoff artifact | Same live Umans dogfood and snapshot secret-file hardening scope as the immediately following row. | Same source set as the immediately following row. | Supersedes the immediately following row's stale `quality-final2` path with the final post-fix lane: `scripts/test-lanes.sh quality-gates --artifact-dir target/test-lanes/20260630-live-umans-quality-final4`. The focused red/green snapshot/revert tests and support-export secret scans remain as recorded below. | Fresh quality artifact: `target/test-lanes/20260630-live-umans-quality-final4/summary.txt`. Clean live evidence remains `/tmp/harness-umans-live-20260630-clean`. | verified_pending_review | Supersedes the next row only for the final quality-gates artifact path; the detailed live run IDs, red/green regressions, dotenv omission behavior, and raw-credential caveat remain unchanged. |
| 2026-06-30 | `uncommitted` | Credential-enabled Umans live dogfood plus snapshot secret-file hardening | No new Harness source behavior changed in this row; it supersedes the earlier external-credential blocker by exercising the Harness live provider/tool surface. | `crates/harness-core/src/coord/snapshot.rs`; `crates/harness-core/src/coord/revert.rs`; `crates/harness-core/src/coord/tests/workspace_snapshot_secret_tests.rs`; `crates/harness-core/src/coord/tests/workspace_snapshot_tests.rs`; `crates/harness-core/src/coord/tests.rs`; `docs/architecture.md`; `docs/sessions-and-replay.md`; `docs/tools-parity-prd.md`; `docs/tools-parity-progress.md`; clean temp evidence under `/tmp/harness-umans-live-20260630-clean` | RED: `cargo nextest run -p harness-core snapshot_omits_dotenv_files_from_artifacts -- --nocapture` failed because `.umans.env` was captured in snapshot artifacts. RED: `cargo nextest run -p harness-core revert_ignores_dotenv_files_already_in_snapshot_artifact -- --nocapture` failed because a legacy snapshot entry restored `.umans.env`. GREEN: both regressions after snapshot/revert ignore dotenv-style files; `cargo nextest run -p harness-core revert_ignores_dotenv_files_missing_from_snapshot -- --nocapture`; `cargo nextest run -p harness-core revert_restores_workspace_from_snapshot -- --nocapture`; LSP diagnostics clean on changed Rust files; `cargo build -p harness`; `cargo fmt --all -- --check`; `cargo nextest run -p harness --test event_docs_reference_test -- --nocapture`; `cargo nextest run -p harness --test config_docs_reference_test -- --nocapture`; `cargo nextest run -p harness --test replay_sessions_cli_test sessions_export -- --nocapture`; `python3 scripts/check-forbidden-branding.py`; `python3 scripts/check-test-suite-gates.py --gate conventions`; `scripts/test-lanes.sh quality-gates --artifact-dir target/test-lanes/20260630-live-umans-quality-final2`. | Live credentialed workspace: `/tmp/harness-umans-live-20260630-clean`. `doctor-live.json` reports credential presence via environment while retaining `provider_execution_proof=false`/`readiness_scope=local_readiness_only` because doctor remains offline. GLM run `prompt_37ded366dff75b958d85883b182aed03` used `glob` then `read` and returned both canary paths plus `alpha-canary: GLM and Kimi should read this exact line.` Kimi run `prompt_c9aff705cab05b6fad3fbc74673fc761` used `read` then `glob` and returned the same canary line. `glm-live.json`/`kimi-live.json`, session inspection JSON, support bundles, and `.agent-harness` session artifacts were scanned for dotenv filename, dotenv assignment shapes, and secret literal markers with result `secret_material_not_found`; the public env var name appears only as config-key metadata in `harness.jsonc`-derived evidence. Live session event logs contain provider reasoning delta events and remain local evidence, not public support material. | ready_for_oracle | Supersedes rows 12, 13, and 15 for live Umans scope. A prior temp workspace exposed a real Harness bug: workspace snapshots captured `.umans.env` contents before tool batches. The fix omits dotenv-style files from snapshot and revert scans, including legacy snapshot entries, preserving ordinary snapshot/revert behavior while keeping credential material out of session artifacts/support evidence. |
| 2026-06-30 | `uncommitted` | Final acceptance candidate — PRD checklist closure and post-checklist lanes | Same full PRD/source set as the immediately following Oracle follow-up row; this row adds PRD §11 closure after Oracle found the final checklist still open. | `docs/tools-parity-prd.md`; `docs/tools-parity-progress.md`; `crates/harness/tests/prompt_cli/02_prompt_cli_model_variant_and_thinking_test.rs`; temp Umans evidence under `/tmp/harness-umans-dogfood-20260630` | Final post-checklist lane set: `scripts/test-lanes.sh fast --artifact-dir target/test-lanes/20260630-final-fast`; `scripts/test-lanes.sh quality-gates --artifact-dir target/test-lanes/20260630-final-quality`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic --artifact-dir target/test-lanes/20260630-final-all-deterministic`. Focused checks remain the same as the following row: LSP diagnostics clean on the changed Rust test, `cargo fmt --all -- --check`, focused model-override regression, conventions gate, and forbidden-branding gate. | Final artifact targets: `target/test-lanes/20260630-final-fast/summary.txt`, `target/test-lanes/20260630-final-quality/summary.txt`, and `target/test-lanes/20260630-final-all-deterministic/summary.txt`. The Umans dogfood caveat remains unchanged: provider/model routing, metadata, failed-run inspection, and support export are proven; live model response/tool-call/`glob`/`read` dogfood is not claimed without `UMANS_AI_CODING_PLAN_API_KEY`. | ready_for_oracle | Supersedes the following row's stale-lane/checklist state. PRD §11 is now checked with an explicit no-live-Umans-claim caveat, and the named lane artifacts are the evidence set to be produced after this final docs edit before Oracle re-review. |
| 2026-06-30 | `uncommitted` | Final full-task Oracle follow-up — PRD dispositions, Umans blocker scope, fresh lanes | Oracle review `ses_0e66f8b7affe1WNHgOup26mXWq` rechecked the full PRD and found remaining closure blockers in PRD §0.5 and §8; no new Harness source behavior changed in this documentation/evidence disposition row. | `docs/tools-parity-prd.md`; `docs/tools-parity-progress.md`; `crates/harness/tests/prompt_cli/02_prompt_cli_model_variant_and_thinking_test.rs`; temp Umans evidence under `/tmp/harness-umans-dogfood-20260630` | Focused post-fix checks: LSP diagnostics clean on `crates/harness/tests/prompt_cli/02_prompt_cli_model_variant_and_thinking_test.rs`; `cargo fmt --all -- --check`; `cargo nextest run -p harness --test prompt_cli_test prompt_cli_model_override_records_selected_model_in_run_metadata -- --nocapture`; `python3 scripts/check-test-suite-gates.py --gate conventions`. Fresh required lanes after the convention fix: `scripts/test-lanes.sh fast --artifact-dir target/test-lanes/20260630-continuation-fast` (`PASS=3 FAIL=0`); `scripts/test-lanes.sh quality-gates --artifact-dir target/test-lanes/20260630-continuation-quality` (`PASS=2 FAIL=0`); `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic --artifact-dir target/test-lanes/20260630-continuation-all-deterministic` (`PASS=18 FAIL=0`). | Fresh lane artifacts: `target/test-lanes/20260630-continuation-fast/summary.txt`, `target/test-lanes/20260630-continuation-quality/summary.txt`, and `target/test-lanes/20260630-continuation-all-deterministic/summary.txt`. Umans temp dogfood remains provider-boundary-only: `/tmp/harness-umans-dogfood-20260630/doctor.json` reports `provider_execution_proof=false`, `readiness_scope=local_readiness_only`, and missing `UMANS_AI_CODING_PLAN_API_KEY`; fresh GLM/Kimi failed sessions and support bundles prove model routing/metadata/export redaction but no model responses, no tool calls, and no live `glob`/`read`. | blocked_external_pending_oracle | Supersedes the prior `verified_pending_oracle` metadata row for final task scope. The metadata bug is fixed and green, grep/schema blockers have fresh passing evidence, and PRD §8 is now dispositioned. Final completion still cannot honestly claim live Umans tool-use dogfood without credentials; if Oracle requires real GLM/Kimi model responses and tool calls, the next action is to provide `UMANS_AI_CODING_PLAN_API_KEY` and rerun the prompt dogfood. |
| 2026-06-30 | `uncommitted` | §8 deferred decision disposition — reject narrow unknown-tool repair for this PRD | P1.4 invalid-call references had already been inspected from `inspirations/packages/src/tool/tool.ts`, `inspirations/shuvcode/packages/src/tool/tool.ts`, `inspirations/shuvcode/packages/src/tool/invalid.ts`, and `inspirations/shuvcode/packages/src/tool/batch.ts`; this row records the explicit PRD §8 disposition Oracle found missing. | `docs/tools-parity-prd.md`; `docs/tools-parity-progress.md`; `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/src/control_plane.rs`; `crates/harness-tools/src/arg_parse.rs`; `crates/harness-tools/tests/native_malformed_args_recovery_test.rs` | Existing P1.4 evidence remains the behavior lock: `cargo nextest run -p harness-tools --test native_malformed_args_recovery_test`; current continuation lanes above also pass after the PRD update and convention fix. | Deterministic malformed-call dogfood remains strict and recoverable: malformed native arguments return schema-rewrite guidance plus serde detail. No live unknown-tool auto-repair dogfood is claimed because narrow repair is rejected, not promoted. | rejected | Unknown and malformed tool calls remain strict fail-closed with deterministic recovery text. Harness keeps the explicit `invalid` tool response but does not add a permissive catch-all executor, provider-intent guessing, or silent tool-name rewrite without future live evidence that such repair improves recovery without hiding provider bugs. |
| 2026-06-30 | `uncommitted` | Umans dogfood blocker remediation — prompt `--model` run metadata | PRD §7 dogfood requirements and existing Harness prompt/session metadata surfaces; no Harness source behavior changed because this fixes Harness runtime metadata for provider-selected prompt runs. | `crates/harness/src/prompt.rs`; `crates/harness-core/src/coord/agent_turn_runtime.rs`; `crates/harness-core/src/proj/session_catalog_projection.rs`; `crates/harness/tests/prompt_cli/02_prompt_cli_model_variant_and_thinking_test.rs`; `docs/tools-parity-progress.md` | RED: `cargo nextest run -p harness --test prompt_cli_test prompt_cli_model_override_records_selected_model_in_run_metadata -- --nocapture` failed with `meta.json` model `gpt-4o-mini` while provider request used `gpt-4.1`; GREEN: same focused regression; `cargo fmt --all -- --check`; `cargo nextest run -p harness --test prompt_cli_test`; `cargo nextest run -p harness-core --test recorded_runtime_context_meta_test`; LSP diagnostics clean on changed Rust files; `cargo build -p harness`. | Fresh temp dogfood workspace: `/tmp/harness-umans-dogfood-20260630`. Rebuilt `target/debug/harness`, then reran `prompt --model umans-ai-coding-plan/umans-glm-5.2` and `prompt --model umans-ai-coding-plan/umans-kimi-k2.7` from that workspace. Fresh sessions: GLM `/tmp/harness-umans-dogfood-20260630/.agent-harness/sessions/prompt_e6d2f440ffed5548b19bb123c630180f`, Kimi `/tmp/harness-umans-dogfood-20260630/.agent-harness/sessions/prompt_a54e2c99056e540880d4e9296f40bcdf`. `meta.json` and `sessions inspect` now report GLM `provider_model = umans-ai-coding-plan/umans-glm-5.2` and Kimi `provider_model = umans-ai-coding-plan/umans-kimi-k2.7`; events reached the provider boundary and cancelled with `missing_credentials`. Support bundles exported to `glm-rerun-support-bundle.json` and `kimi-rerun-support-bundle.json`. | verified_pending_oracle | Supersedes the earlier stale temp `sessions/` evidence that exposed GLM `meta.json` defaulting to Kimi. Live completion remains blocked by `UMANS_AI_CODING_PLAN_API_KEY` missing; the current evidence proves provider-boundary model selection and persisted run metadata through the real prompt surface. |
| 2026-06-30 | `uncommitted` | Final Oracle blocker remediation — grep text post-ledger signoff | Same current core grep source as the next row: `inspirations/packages/core/src/tool/grep.ts`. This row supersedes the next row with post-ledger static evidence only. | Same grep text remediation set as the next row: `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/src/fs_grep.rs`; `crates/harness-tools/tests/native_execution_surface/06_native_provider_tool_defs_schema_test.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `docs/test-suite-conventions-baseline.json`; `docs/tools-parity-progress.md` | Same focused red/green grep text tests as the next row, plus post-ledger `scripts/test-lanes.sh quality-gates`. | Fresh post-ledger quality artifact: `target/test-lanes/20260630-155200/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260630-154838/summary.txt` (`PASS=18 FAIL=0`). | verified_pending_oracle | Supersedes the immediately following grep provider-visible text row for evidence freshness. Completion remains blocked until skeptical Oracle returns green. |
| 2026-06-30 | `uncommitted` | Final Oracle blocker remediation — grep provider-visible text | Rechecked current core grep definition under `inspirations/packages/core/src/tool/grep.ts` after skeptical Oracle found Harness still model-advertised hidden `grep.literal` despite the provider-visible schema omitting it. | `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/src/fs_grep.rs`; `crates/harness-tools/tests/native_execution_surface/06_native_provider_tool_defs_schema_test.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `docs/test-suite-conventions-baseline.json`; `docs/tools-parity-progress.md` | RED: `cargo nextest run -p harness-tools --test native_execution_surface_test provider_tool_defs_do_not_advertise_runtime_only_grep_controls -- --nocapture` failed because the provider description advertised `literal: true`; GREEN: `cargo nextest run -p harness-tools --test native_execution_surface_test provider_tool_defs_do_not_advertise_runtime_only_grep_controls -- --nocapture`; `cargo nextest run -p harness-tools --lib collect_grep_matches_invalid_regex_suggests_escaping_regex_metacharacters -- --nocapture`; `cargo nextest run -p harness-tools --test native_execution_surface_test -- --nocapture`; `cargo nextest run -p harness-tools --test native_tool_schema_guidance_test -- --nocapture`; `UPDATE_PROVIDER_TOOL_PAYLOAD_SNAPSHOTS=1 cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test -- --nocapture`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test -- --nocapture`; `cargo nextest run -p harness-providers --test provider_schema_compatibility_test -- --nocapture`; `cargo fmt --all -- --check`; `python3 scripts/check-test-suite-gates.py --gate conventions`; `scripts/test-lanes.sh quality-gates`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | Fresh quality artifact: `target/test-lanes/20260630-154803/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260630-154838/summary.txt` (`PASS=18 FAIL=0`). Stale deterministic artifact `target/test-lanes/20260630-154359/summary.txt` is superseded because it failed static conventions before the obsolete debt hash was removed. | verified_pending_oracle | Supersedes the immediately following provider-visible filesystem schema row after Oracle review `ses_0e6dddf7dffepPaiWSO5XLe1vK` returned `<promise>NOT_VERIFIED</promise>` for model-advertised runtime-only grep controls. Provider-visible `grep` now advertises only the current core fields (`pattern`, `path`, `include`, `limit`) in schema and description. Runtime compatibility still accepts internal `literal`/`context`, but neither field appears in provider schema, provider description, or invalid-regex repair guidance. Completion remains blocked until skeptical Oracle returns green. |
| 2026-06-30 | `uncommitted` | Final Oracle blocker remediation — provider-visible filesystem schema gates | Rechecked current core filesystem definitions under `inspirations/packages/core/src/tool/read.ts`, `glob.ts`, `grep.ts`, `edit.ts`, and `write.ts` after skeptical Oracle found the previous signoff used adapted/legacy provider-visible schema names. | `crates/harness-tools/src/native_tools/args.rs`; `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/src/hashline_edit.rs`; `crates/harness-tools/src/file_write.rs`; `crates/harness-tools/src/lib.rs`; `crates/harness-tools/tests/native_execution_surface/06_native_provider_tool_defs_schema_test.rs`; `crates/harness-tools/tests/native_tool_schema_guidance_test.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `crates/harness-core/tests/coord_auth_test.rs`; `crates/harness-core/tests/common/mod.rs`; `docs/tools-parity-progress.md` | RED: `cargo nextest run -p harness-tools --test native_execution_surface_test provider_tool_defs_match_current_harness_core_filesystem_input_schemas --test-threads 1 -- --nocapture` failed on `read` advertising `filePath`; GREEN: `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-tools --test native_tool_schema_guidance_test`; `cargo nextest run -p harness-tools read_tool_schema_advertises_one_indexed_positive_paging`; `UPDATE_PROVIDER_TOOL_PAYLOAD_SNAPSHOTS=1 cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_schema_compatibility_test`; `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo fmt --all -- --check`; `RUST_TEST_THREADS=1 cargo nextest run -p harness-core --test coord_auth_test -- --nocapture`; `scripts/test-lanes.sh quality-gates`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | Fresh quality artifacts: `target/test-lanes/20260630-150953/summary.txt` and post-ledger `target/test-lanes/20260630-151935/summary.txt` (both `PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260630-151029/summary.txt` (`PASS=18 FAIL=0`). Earlier stale lane artifacts `target/test-lanes/20260630-145256/summary.txt` and `target/test-lanes/20260630-150619/summary.txt` are explicitly superseded because they failed forbidden-branding and static no-sleeps before cleanup. | verified_pending_oracle | Supersedes the immediately following final blocker row. Provider-visible `read`, `edit`, and `write` now advertise and require `path`, while `filePath` remains a hidden runtime alias. Provider-visible `glob` and `grep` now advertise `limit`. Runtime-only compatibility fields (`read.hashlineAnchors`, hashline edit payloads, delete/rename, `grep.literal`, and `grep.context`) remain unadvertised. The coord-auth redaction regression now denies the pending permission explicitly, and the shared wait helper remains `tokio::task::yield_now().await` so the no-sleeps gate stays green. Final completion remains blocked until skeptical Oracle returns green. |
| 2026-06-30 | `uncommitted` | Final Oracle blocker remediation — current core filesystem provider schemas | Rechecked current core filesystem definitions under `inspirations/packages/core/src/tool/read.ts`, `glob.ts`, `grep.ts`, `edit.ts`, and `write.ts` after skeptical Oracle found the previous signoff used adapted/legacy provider-visible schema names. | `crates/harness-tools/src/native_tools/args.rs`; `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/src/hashline_edit.rs`; `crates/harness-tools/src/file_write.rs`; `crates/harness-tools/src/lib.rs`; `crates/harness-tools/tests/native_execution_surface/01_native_execution_surface_tools_execute_through_test.rs`; `crates/harness-tools/tests/native_execution_surface/03_native_write_apply_patch_exact_edit_test.rs`; `crates/harness-tools/tests/native_execution_surface/05_native_baseline_shape_compat_test.rs`; `crates/harness-tools/tests/native_execution_surface/06_native_provider_tool_defs_schema_test.rs`; `crates/harness-tools/tests/native_tool_schema_guidance_test.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `crates/harness-core/tests/common/mod.rs`; `docs/tools-parity-progress.md` | RED: `cargo nextest run -p harness-tools --test native_execution_surface_test provider_tool_defs_match_current_harness_core_filesystem_input_schemas --test-threads 1 -- --nocapture` failed on `read` advertising `filePath`; GREEN: `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-tools --test native_tool_schema_guidance_test`; `cargo nextest run -p harness-tools read_tool_schema_advertises_one_indexed_positive_paging`; `UPDATE_PROVIDER_TOOL_PAYLOAD_SNAPSHOTS=1 cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_schema_compatibility_test`; `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo fmt --all -- --check`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `cargo nextest run -p harness-tools --test single_surface_live_test single_surface_tools_execute_under_example_config`; `cargo nextest run -p harness-tools --test native_read_truncation_presentation_test`; `cargo nextest run -p harness-providers request_media_serialization`; `cargo nextest run -p harness-providers request_serialization`; `cargo nextest run -p harness-tools --test native_web_search_test`; `cargo nextest run -p harness-tools --test native_web_fetch_test`; `cargo run -p harness -- --config configs/harness.example.jsonc config validate`; focused blocker checks: `python3 scripts/check-forbidden-branding.py`, `RUST_TEST_THREADS=1 cargo nextest run -p harness-core --test coord_auth_test -- --nocapture`, and `cargo nextest run -p harness-tools --test native_execution_surface_test provider_tool_defs_match_current_core_filesystem_input_schemas -- --nocapture` | Full lane artifacts pending this row's final rerun. Earlier stale lane artifact `target/test-lanes/20260630-145256/summary.txt` is explicitly superseded because it failed forbidden-branding before the test-name cleanup. | implemented_pending_final_gates | Supersedes the immediately preceding final continuation signoff row after a later Oracle returned `<promise>NOT_VERIFIED</promise>`. Provider-visible `read`, `edit`, and `write` now advertise and require `path`, while `filePath` remains a hidden runtime alias. Provider-visible `glob` and `grep` now advertise `limit`. Runtime-only compatibility fields (`read.hashlineAnchors`, hashline edit payloads, delete/rename, and `grep.literal`) remain unadvertised. The coord-auth verification helper now waits with a real bounded sleep so permission timeout tests observe the async timeout task instead of racing a tight yield loop. Final completion remains blocked until fresh full lanes and skeptical Oracle return green. |
| 2026-06-30 | `uncommitted` | Final continuation signoff — parity sweep, coord auth timeout, fresh gates | Rechecked the overlapping Harness tool and provider references under `inspirations/harness`, with special attention to provider-visible schemas, media lowering, filesystem tools, shell/web tools, and task/control tools. | Current parity branch across `crates/harness-tools`, `crates/harness-providers`, `crates/harness-core`, `crates/harness`, `configs/harness.example.jsonc`, and this ledger. | `RUST_TEST_THREADS=1 cargo nextest run -p harness-core --test coord_auth_test shell_permission_summary_redacts_command_secrets --test-threads 1 -- --exact --nocapture`; `RUST_TEST_THREADS=1 cargo nextest run -p harness-core --test coord_auth_test`; `cargo nextest run -p harness-core --test permission_policy_supports_native_tool_permission_kinds_test`; `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_schema_compatibility_test`; `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `cargo nextest run -p harness-tools --test single_surface_live_test single_surface_tools_execute_under_example_config`; `cargo nextest run -p harness-tools --test native_read_truncation_presentation_test`; `cargo nextest run -p harness-providers request_media_serialization`; `cargo nextest run -p harness-providers request_serialization`; `cargo nextest run -p harness-tools --test native_web_search_test`; `cargo nextest run -p harness-tools --test native_web_fetch_test`; `cargo run -p harness -- --config configs/harness.example.jsonc config validate`; `cargo run -p harness -- --config configs/harness.example.jsonc doctor --json`; Oracle review `ses_0e72bfd7bffeZj9HXq2Lv0OJYR` | Fresh quality artifact: `target/test-lanes/20260630-135600/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260630-135559/summary.txt` (all deterministic lanes passed, including `signoff-pty`). Doctor JSON reported `native_tool_catalog` pass with 33 native tool catalog entries. | verified | Supersedes the prior verified fs.read blocker row with a fresh continuation pass after the user requested more work. The previously reported coord auth timeout no longer reproduces: the timeout path denies permission, calls `reject_pending_permission`, and emits failed `ToolCallFinished`; focused and full `coord_auth_test` runs pass. Fresh skeptical Oracle returned `<promise>VERIFIED</promise>` and classified advisory sweep items (`read.hashlineAnchors`, grep `literal/context`, edit hashline mode, and write path aliases) as runtime-only compatibility extensions or hidden schemas, not provider-visible parity blockers. |
| 2026-06-30 | `uncommitted` | Final Oracle blocker remediation — fs.read spill artifact hashline anchors | Rechecked against `inspirations/packages/src/tool/read.ts` and Harness hashline edit invariants after Oracle found spill artifacts hashed truncated visible text instead of the source line. | `crates/harness-tools/src/fs_read.rs`; `crates/harness-tools/src/fs_read/tests.rs`; `crates/harness-tools/tests/native_read_truncation_presentation_test.rs`; `docs/tools-parity-progress.md` | LSP diagnostics clean on changed Rust files; `cargo fmt --all -- --check`; `cargo check -p harness-tools`; `cargo nextest run -p harness-tools fs_read_spill_artifact_hashes_source_line_when_visible_line_is_truncated`; `cargo nextest run -p harness-tools --lib fs_read`; `cargo nextest run -p harness-tools --test native_read_truncation_presentation_test`; `scripts/test-lanes.sh quality-gates`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic`; Oracle review `ses_0e750d941ffexrT7h2LxhU31cO` | Fresh quality artifact: `target/test-lanes/20260630-131353/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260630-131154/summary.txt` (`PASS=18 FAIL=0`). | verified | Supersedes the previous final-blocker row after Oracle returned `<promise>NOT_VERIFIED</promise>` for `fs_read` spill artifacts. Unit and public-registry regressions now prove streaming artifact output mirrors inline window rendering: hashline anchors are computed from the source line while the displayed artifact text remains the truncated visible line. Final skeptical Oracle review returned `<promise>VERIFIED</promise>`. |
| 2026-06-30 | `uncommitted` | Final Oracle blocker remediation — read/glob/media parity and deterministic fallout | Rechecked against `inspirations/packages/src/tool/read.ts`, `inspirations/packages/src/tool/glob.ts`, `inspirations/packages/llm/src/protocols/shared.ts`, `inspirations/packages/llm/src/protocols/openai-chat.ts`, and `inspirations/packages/llm/src/protocols/openai-responses.ts`. | `crates/harness-tools/src/fs_read.rs`; `crates/harness-tools/src/fs_read/window.rs`; `crates/harness-tools/src/fs_read/render.rs`; `crates/harness-tools/src/fs_read/tests.rs`; `crates/harness-tools/src/fs_glob.rs`; `crates/harness-tools/tests/native_read_truncation_presentation_test.rs`; `crates/harness-tools/tests/native_agent_spawn_and_batch_preserve_lineage_permissions_and_order/06_task_tool_rejects_reentry_to_sibling_test.rs`; `crates/harness-providers/Cargo.toml`; `crates/harness-providers/src/openai/request.rs`; `crates/harness-providers/src/openai/tests.rs`; `crates/harness-providers/src/openai/tests/request_serialization_test.rs`; `crates/harness-providers/src/openai/tests/request_media_serialization_test.rs`; `docs/test-suite-conventions-baseline.json`; `docs/tools-parity-progress.md` | LSP diagnostics clean on changed Rust files; `python3 scripts/check-test-suite-gates.py --gate conventions`; `cargo fmt --all -- --check`; `cargo check -p harness-tools -p harness-providers`; `cargo nextest run -p harness-tools read_byte_cap_spills_many_lines_to_artifact`; `cargo nextest run -p harness-tools batch_rejects_more_than_25_calls_preserving_input_order`; `cargo nextest run -p harness-tools fs_read_rejects_offset_beyond_end_of_file`; `cargo nextest run -p harness-tools fs_read_truncates_lines_longer_than_baseline_limit`; `cargo nextest run -p harness-tools collect_glob_matches_applies_slash_pattern_relative_to_search_base`; `cargo nextest run -p harness-providers request_media_serialization`; `cargo nextest run -p harness-providers request_serialization`; `scripts/test-lanes.sh quality-gates`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | Fresh quality artifact: `target/test-lanes/20260630-122321/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260630-122357/summary.txt` (`PASS=18 FAIL=0`). The stale failed deterministic artifact `target/test-lanes/20260630-120405/summary.txt` is superseded. | verified_pending_oracle | Supersedes the immediately preceding Chat-only row and the stale failed deterministic rerun. Fixed read past-EOF rejection, per-line 2000-character visible truncation, base-relative glob matching with workspace-relative output, OpenAI image media validation/skipping for Chat and Responses, and the two deterministic-suite fallout cases. |
| 2026-06-30 | `uncommitted` | Final Oracle blocker remediation — OpenAI Chat tool-result media batching | Rechecked against `inspirations/packages/llm/src/protocols/openai-chat.ts`, especially `lowerMessages` pending-image buffering for consecutive tool messages. | `crates/harness-providers/src/openai/request.rs`; `crates/harness-providers/src/openai/tests/request_serialization_test.rs`; `docs/tools-parity-progress.md` | LSP diagnostics clean on changed Rust provider files; `cargo fmt --all -- --check`; `cargo check -p harness-providers`; `cargo nextest run -p harness-providers openai_chat_request_batches_consecutive_tool_result_images`; `cargo nextest run -p harness-providers request_serialization`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_schema_compatibility_test`; `scripts/test-lanes.sh quality-gates`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | Fresh quality artifact: `target/test-lanes/20260630-110512/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260630-110553/summary.txt` (`PASS=18 FAIL=0`). | verified_pending_oracle | Supersedes the immediately preceding gate-only row. Chat Completions now emits consecutive image-bearing tool results as `tool`, `tool`, then one batched `user` image message, matching the local reference lowering path instead of interleaving image user messages between tool outputs. |
| 2026-06-30 | `uncommitted` | Final gate re-run after source-branding and convention-baseline cleanup | Same local reference set as the next blocker-remediation row; no tool-surface behavior changed in this cleanup step. | `crates/harness-tools/tests/native_execution_surface/05_native_baseline_shape_compat_test.rs`; `docs/test-suite-conventions-baseline.json`; `docs/tools-parity-progress.md` | `python3 scripts/check-test-suite-gates.py --gate conventions`; `python3 scripts/check-forbidden-branding.py`; `scripts/test-lanes.sh quality-gates`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | Fresh quality artifact: `target/test-lanes/20260630-103841/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260630-103910/summary.txt` (`PASS=18 FAIL=0`). | verified_pending_oracle | Supersedes the failed `target/test-lanes/20260630-102544/summary.txt` rerun. The provider/tool parity fixes from the next row remain the behavioral change; this row records that the final static and deterministic gates are green again before the fresh Oracle review. |
| 2026-06-30 | `uncommitted` | Final Oracle blocker remediation — read media/provider lowering and Harness-shaped output | Rechecked against `inspirations/packages/src/tool/read.ts`, `inspirations/packages/core/src/session/runner/to-llm-message.ts`, `inspirations/packages/llm/src/schema/messages.ts`, `inspirations/packages/llm/src/protocols/openai-chat.ts`, `inspirations/packages/llm/src/protocols/openai-responses.ts`, `inspirations/packages/src/tool/grep.ts`, `inspirations/packages/src/tool/glob.ts`, and `inspirations/packages/src/tool/registry.ts`. | `crates/harness-core/src/tool.rs`; `crates/harness-core/src/agent/provider_boundary.rs`; `crates/harness-core/src/coord/agent_turn_phases.rs`; `crates/harness-tools/src/fs_read.rs`; `crates/harness-tools/src/fs_read/tests.rs`; `crates/harness-tools/src/fs_grep.rs`; `crates/harness-tools/src/fs_glob.rs`; `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/src/tool_catalog/baseline_parity.rs`; `crates/harness-tools/tests/fixtures/tools_parity_inventory.v1.json`; `crates/harness-providers/src/openai/request.rs`; `crates/harness-providers/src/openai/tests/request_serialization_test.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `docs/tools-parity-progress.md` | LSP diagnostics clean on the changed Rust parity files; `cargo fmt --all -- --check`; `cargo check -p harness-core -p harness-tools -p harness-providers`; `cargo nextest run -p harness-tools --lib fs_read`; `cargo nextest run -p harness-providers request_serialization`; `cargo nextest run -p harness-core tool_result_message_content`; `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_schema_compatibility_test`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `cargo nextest run -p harness-tools --test single_surface_live_test single_surface_tools_execute_under_example_config`; `cargo nextest run -p harness-tools --test native_large_output_dogfood_test read_grep_and_bash_spill_large_outputs_with_model_guidance`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | CLI surface QA ran in tmux: `cargo run -p harness -- --help`, missing-config `config validate` negative path, and `cargo run -p harness -- run --scenario golden_path --deterministic --session-dir /tmp/harness-parity-smoke-sessions-final --print-run-dir`, which printed `/tmp/harness-parity-smoke-sessions-final/run_739a106bd3025d55b363355d1b87beb2`; tmux session was killed after capture. Full deterministic artifact: `target/test-lanes/20260630-094923/summary.txt` (`quality-gates`, `simulation`, `fast`, `integration`, `pty_support`, and `signoff-pty` all PASS). | verified_pending_oracle | Supersedes the previous final Oracle blocker row for read/search/toolcall parity. `read` now preserves Harness-shaped XML text/directory output, media reads return image/PDF success text plus structured attachments, OpenAI Chat lowers image attachments to image messages, and OpenAI Responses lowers image attachments to `input_image` while skipping PDF bytes to match Harness's image-only `function_call_output` schema. Provider-visible mutation tools now follow Harness registry gating (`apply_patch` vs `edit`/`write`), grep/glob model output matches Harness grouping/empty/truncation presentation, and `lsp.rename` is recorded as Harness-only rather than adapted Harness `lsp`. Completion remains blocked on fresh Oracle verification. |
| 2026-06-30 | `uncommitted` | Final Oracle blocker remediation — provider descriptions and exact edit post-mutation parity | Rechecked against `inspirations/packages/src/tool/read.ts`, `edit.ts`, and `write.ts` after skeptical Oracle found provider-visible description/schema mismatches and missing exact-edit create/format/LSP behavior. | `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/src/hashline_edit.rs`; `crates/harness-tools/src/exact_edit.rs`; `crates/harness-tools/src/file_write.rs`; `crates/harness-tools/tests/native_execution_surface/01_native_execution_surface_tools_execute_through_test.rs`; `crates/harness-tools/tests/native_execution_surface/03_native_write_apply_patch_exact_edit_test.rs`; `crates/harness-tools/tests/native_tool_schema_guidance_test.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `docs/tools-parity-progress.md` | `UPDATE_PROVIDER_TOOL_PAYLOAD_SNAPSHOTS=1 cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-tools --test native_tool_schema_guidance_test`; `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo nextest run -p harness-tools append_lsp_diagnostics_reports_current_and_limited_project_files`; `cargo nextest run -p harness-tools --test single_surface_live_test single_surface_tools_execute_under_example_config`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | Focused native surface now covers provider-visible `edit` description/schema consistency, runtime hashline compatibility outside provider schema, `oldString=""` missing-file creation, rejection of empty `oldString` on existing files, exact-edit structured diagnostics, and exact-edit formatter warning fields. Provider snapshots were refreshed after description/schema changes and then rerun without the update flag. Fresh deterministic artifact: `target/test-lanes/20260629-221109/summary.txt` (`PASS=18 FAIL=0 DRY_RUN=0 SKIP=0`). | verified_pending_oracle | Supersedes the 2026-06-29 final parity acceptance row. `read` and `edit` descriptions no longer advertise hidden `hashlineAnchors`, `LINE#HASH`, or `delete=true` fields to providers. Exact `edit` now matches Harness create behavior for missing files and runs the same formatter plus LSP diagnostic flow as `write` after mutation. Final completion remains blocked until skeptical Oracle returns `VERIFIED`. |
| 2026-06-29 | `uncommitted` | Final parity acceptance — provider-visible schemas, exact edit fallback, write formatter/LSP diagnostics | Re-read local Harness references for overlapping built-ins and mutation behavior: `inspirations/packages/src/tool/read.ts`, `grep.ts`, `bash.ts`, `edit.ts`, and `write.ts`; kept Harness-only compatibility aliases runtime-only where needed. | `crates/harness-core/src/coord.rs`; `crates/harness-core/src/coord/formatter/mod.rs`; `crates/harness-core/src/coord/formatter/runner.rs`; `crates/harness-tools/src/file_write.rs`; `crates/harness-tools/src/exact_edit.rs`; `crates/harness-tools/src/exact_edit_match.rs`; `crates/harness-tools/src/exact_edit_match/candidates.rs`; `crates/harness-tools/src/exact_edit_match/candidates/text.rs`; `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/src/native_tools/args.rs`; `crates/harness-tools/src/hashline_edit.rs`; `crates/harness-tools/tests/native_execution_surface/01_native_execution_surface_tools_execute_through_test.rs`; `crates/harness-tools/tests/native_execution_surface/03_native_write_apply_patch_exact_edit_test.rs`; `crates/harness-tools/tests/native_tool_schema_guidance_test.rs`; `crates/harness-tools/tests/fixtures/tools_parity_inventory.v1.json`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `crates/harness/tests/dogfood_harness_jsonc_tool_parity_test.rs`; `docs/tools-parity-progress.md` | `UPDATE_PROVIDER_TOOL_PAYLOAD_SNAPSHOTS=1 cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo nextest run -p harness-tools --test native_tool_schema_guidance_test`; `cargo nextest run -p harness-tools append_lsp_diagnostics_reports_current_and_limited_project_files`; `cargo nextest run -p harness-tools --test single_surface_live_test single_surface_tools_execute_under_example_config`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | Fresh deterministic artifact: `target/test-lanes/20260629-212401/summary.txt` (`mode=all-deterministic`, `PASS=18 FAIL=0 DRY_RUN=0 SKIP=0`). Focused live native surface evidence includes `single_surface_tools_execute_under_example_config`, which exercises the real example-config tool surface. Provider snapshots were refreshed and then rerun without the update flag. | verified_pending_oracle | Supersedes the latest schema/edit/write blocker rows. Provider-visible schemas now advertise Harness-shaped `read`, `grep`, `bash`, `write`, and exact `edit` arguments while preserving Harness runtime aliases and hashline internals outside provider-facing schemas. Exact edit now uses ruleset-style fallback matching. `write` creates or overwrites files, runs the shared formatter best-effort, then appends current-file and bounded workspace LSP diagnostics. Final completion remains blocked until skeptical Oracle returns `VERIFIED`. |
| 2026-06-29 | `uncommitted` | Final security-review blocker remediation — Linux procfs parent-chain hardening | Rechecked against ruleset-compatible bash/toolcall parity after security review found `/proc/<ppid>/environ` could expose the harness process environment despite child env sanitization. | `crates/harness-tools/Cargo.toml`; `crates/harness-tools/src/shell_run.rs`; `docs/tools-parity-progress.md` | RED: `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --lib invocation_hides_parent_environment` failed before the sanitized intermediate parent; RED: `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --lib invocation_hides_grandparent_environment` failed because `awk` could walk from the sanitized shell parent to the harness grandparent and print inherited env. GREEN: both command filters now pass after shell execution marks the harness process `NotDumpable` on Linux via safe `rustix::process::set_dumpable_behavior`, while commands still run under a sanitized intermediate parent; `cargo fmt --all -- --check`; `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --lib shell_run`; `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --lib shell_safety`; `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --test native_execution_surface_test`; `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --test single_surface_live_test single_surface_tools_execute_under_example_config`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `scripts/test-lanes.sh quality-gates`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | Linux regressions execute `awk` programs that construct `/proc/<ppid>/environ` and `/proc/<grandparent>/environ` at runtime without literal path tokens, with `SECRET_TOKEN` present in the parent test process. Tests assert `HOME=` and `SECRET_TOKEN=` are absent from display text, structured JSON, and artifacts for direct and wrapper shell surfaces. Fresh quality artifact: `target/test-lanes/20260629-193213/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260629-193216/summary.txt` (`PASS=18 FAIL=0`). | verified_pending_oracle | Supersedes the sanitized-shell-environment row. This closes same-UID procfs environment disclosure by combining child env clearing, a sanitized intermediate parent, non-login bash wrapper execution, and Linux `NotDumpable` hardening for the harness process. |
| 2026-06-29 | `uncommitted` | Final security-review blocker remediation — sanitized shell environment | Rechecked against ruleset-compatible bash/toolcall parity after follow-up security review found non-blocked tools could read inherited environment via `awk ENVIRON`. | `crates/harness-tools/src/shell_run.rs`; `docs/tools-parity-progress.md` | RED: `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --lib invocation_scrubs_inherited_environment` failed because direct and wrapper `awk ENVIRON` output contained inherited environment. GREEN: the same command now passes after `TokioShellCommandRunner` clears the subprocess environment, restores only safe basics (`PATH`, `TERM`, temp and locale variables), and wrapper bash uses non-login `-c`; `cargo fmt --all -- --check`; `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --lib shell_run`; `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --lib shell_safety`; `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --test native_execution_surface_test`; `SECRET_TOKEN=redacted-test-secret cargo nextest run -p harness-tools --test single_surface_live_test single_surface_tools_execute_under_example_config`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `scripts/test-lanes.sh quality-gates`; `SECRET_TOKEN=redacted-test-secret scripts/test-lanes.sh all-deterministic` | Real-runner regression executes `awk 'BEGIN { for (k in ENVIRON) print k "=" ENVIRON[k] }'` through direct and wrapper shell surfaces with `SECRET_TOKEN` present in the parent process, then asserts `HOME=` and `SECRET_TOKEN=` are absent from display text, structured JSON, and artifacts. Fresh quality artifact: `target/test-lanes/20260629-190623/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260629-190626/summary.txt` (`PASS=18 FAIL=0`). | verified_pending_oracle | Supersedes the environment-inspection-builtin row. This closes the inherited-secret leak class at the subprocess boundary instead of relying on a command-name blocklist. |
| 2026-06-29 | `uncommitted` | Final security-review blocker remediation — environment inspection builtins | Rechecked against ruleset-compatible bash/toolcall parity after security review found inherited-environment dumps via shell builtins. | `crates/harness-tools/src/shell_safety.rs`; `crates/harness-tools/src/shell_run.rs`; `docs/tools-parity-progress.md` | RED: `cargo nextest run -p harness-tools --lib validate_bash_command_rejects_environment_inspection_builtins` failed because `export` was accepted; RED: `cargo nextest run -p harness-tools --lib environment_inspection_before_secret_output` failed because fake `SECRET_TOKEN=redacted-test-secret` reached shell result `display_text`. GREEN: both commands now pass; `cargo fmt --all -- --check`; `cargo nextest run -p harness-tools --lib shell_safety`; `cargo nextest run -p harness-tools --lib shell_run`; `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-tools --test single_surface_live_test single_surface_tools_execute_under_example_config`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `scripts/test-lanes.sh quality-gates`; `scripts/test-lanes.sh all-deterministic` | Regression now covers wrapper and direct shell invocations for `export`, `set`, `declare -px`, and `typeset -px`. The fake runner returns a sentinel secret if invoked; tests assert the command is blocked, the runner is never called, the error omits the sentinel, and no display/structured/artifact output is produced. Fresh quality artifact: `target/test-lanes/20260629-184406/summary.txt` (`PASS=2 FAIL=0`). Fresh deterministic artifact: `target/test-lanes/20260629-184408/summary.txt` (`PASS=18 FAIL=0`). | verified_pending_oracle | Supersedes the immediately preceding `verified_pending_oracle` row for bash security. Environment-dump blocking now covers shell builtins as well as `env`/`printenv`, so inherited API keys cannot be printed through these builtins before shell execution. |
| 2026-06-29 | `uncommitted` | Final post-fix verification — promoted tools, shell safety, provider schemas | Re-read `inspirations/packages/core/src/tool/builtins.ts`, `packages/core/src/tool/bash.ts`, `packages/core/src/tool/apply-patch.ts`, `packages/src/tool/write.ts`, and `packages/src/tool/edit.ts`. | `crates/harness-tools/src/file_write.rs`; `crates/harness-tools/src/apply_patch_tool.rs`; `crates/harness-tools/src/hashline_edit.rs`; `crates/harness-tools/src/shell_safety.rs`; `crates/harness-core/src/perm/shell.rs`; `crates/harness-core/src/coord/permission.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `docs/tools-parity-progress.md` | `cargo fmt --all -- --check`; `python3 scripts/check-forbidden-branding.py`; `python3 scripts/check-test-suite-gates.py`; `cargo nextest run -p harness-tools --lib shell_safety`; `cargo nextest run -p harness-core --lib permission_rule_request_selectors_extract_edit_file_alias`; `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-tools --test single_surface_live_test single_surface_tools_execute_under_example_config`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `scripts/test-lanes.sh all-deterministic`; `scripts/test-lanes.sh quality-gates` | Fresh deterministic artifact: `target/test-lanes/20260629-180852/summary.txt` (`all-deterministic`, all lanes PASS). Fresh quality artifact: `target/test-lanes/20260629-180956/summary.txt` (`quality-gates` PASS). Dogfood prompt path with real `harness.jsonc` passed both scripted provider scenarios. Tmux binary surface ran `target/debug/harness --config configs/harness.example.jsonc doctor --json` and exited `0`, showing promoted `write` catalog metadata with `baseline_mapping_status: parity_ready`. | verified_pending_oracle | Supersedes the final post-review blocker rows. Static gates initially caught a forbidden source-brand term in the `write` schema alias description and a missing arrange/act/assert convention in the new permission selector regression; both were fixed, the provider payload snapshot was refreshed, and all post-fix gates were rerun before review/Oracle. |
| 2026-06-29 | `uncommitted` | Final post-review blocker remediation — shell glob/symlink safety | ruleset-compatible bash/toolcall parity rechecked after final security review identified wildcard-expanded symlink operands as a validator/runtime mismatch. | `crates/harness-tools/src/shell_safety.rs`; `docs/tools-parity-progress.md` | `cargo nextest run -p harness-tools --lib validate_bash_command_rejects`; `cargo nextest run -p harness-tools --lib shell_safety`; `cargo nextest run -p harness-tools --test native_execution_surface_test`; `scripts/test-lanes.sh all-deterministic` | Deterministic shell-safety dogfood now covers `cat l*`, `ls le*`, and `bash le*` when the wildcard would expand to a symlink escaping the workspace, plus parameter-expanded executable attempts (`p${UNSET}ython3 -c ...`). Fresh all-deterministic artifact: `target/test-lanes/20260629-155620`, `PASS=18 FAIL=0`. | implemented | Supersedes the brace-expansion row. Path-sensitive shell operands with glob metacharacters are rejected before spawning because the validator cannot prove bash-expanded targets stay within the workspace; models should use native `glob`/`grep` for filesystem discovery. |
| 2026-06-29 | `uncommitted` | Final post-review blocker remediation — brace expansion safety | ruleset-compatible bash/toolcall parity rechecked against local reference execution semantics and final code-quality review findings. | `crates/harness-tools/src/shell_safety.rs`; `docs/tools-parity-progress.md` | `cargo nextest run -p harness-tools --lib validate_bash_command_rejects_brace_expansion_bypasses`; `cargo nextest run -p harness-tools --lib shell_safety` | Deterministic shell-safety dogfood now covers brace-expansion bypass attempts that hide executable names, interpreter eval modes, secret-dump commands, and path expansion (`{/bin/printf,%s\\n,ok}`, `{python3,-c,print(1)}`, `{printenv,PATH}`, `ls {~/secret,}`). | implemented | Supersedes the wrapper/path remediation row for shell execution. Unquoted bash brace expansion is rejected before spawning, so validation cannot see one raw token while runtime expands it into a different executable/argument vector. |
| 2026-06-29 | `uncommitted` | Final post-review blocker remediation — shell wrappers, executable paths, promoted-tool snapshots | ruleset-compatible toolcall parity rechecked against local reference built-ins and the final goal review findings. | `crates/harness-tools/src/shell_safety.rs`; `crates/harness-providers/tests/provider_tool_payload_snapshot_test.rs`; `crates/harness-providers/tests/support/provider_tool_payload_snapshot_support.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `docs/tools-parity-progress.md` | `cargo nextest run -p harness-tools --lib validate_bash_command_rejects_shell_wrapper_bypasses`; `cargo nextest run -p harness-tools --lib validate_bash_command_rejects_executable_path_escapes`; `cargo nextest run -p harness-tools --lib shell_safety`; `UPDATE_PROVIDER_TOOL_PAYLOAD_SNAPSHOTS=1 cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test` | Deterministic shell-safety dogfood now also covers wrapper/eval bypass attempts (`command bash -c`, `eval "python3 -c ..."`, `command printenv`) and executable-position escapes (`/tmp/evil`, `../outside/tool`, `$SHELL -c ...`). Provider payload snapshots now require promoted `write` and `apply_patch` in the build profile. | implemented | Supersedes the immediately preceding post-review row. Shell wrapper builtins are blocked before execution, executable-position paths/expansions are workspace-validated, and provider snapshot invariants now fail if promoted public tools drop out of provider payload coverage. |
| 2026-06-29 | `uncommitted` | Post-review shell safety blocker remediation | ruleset-compatible bash/toolcall parity work rechecked against local reference built-ins; security review focused on interpreter command/script modes and workspace path containment. | `crates/harness-tools/src/shell_safety.rs`; `crates/harness-core/src/perm/shell.rs`; `docs/tools-parity-progress.md` | `cargo nextest run -p harness-tools --lib shell_safety`; `cargo nextest run -p harness-core perm::shell::tests::scan_shell_command_extracts_bare_file_operand_paths`; `cargo nextest run -p harness-core --test coord_auth_test`; `cargo nextest run -p harness-core --test coord_auth_apply_patch_permission_test`; `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `scripts/test-lanes.sh all-deterministic` | Deterministic shell-safety dogfood now covers `python3 - <<'PY'`, `python3 <<'PY'`, direct `python3 -`, `bash < script.sh`, Node `--eval`/`--print`/`-p`, allowed workspace script output redirection, and bare external-symlink operands for `cat`, `bash`, and `node`. | implemented | Supersedes the final-review blockers found after the §8 promotion row. Interpreter stdin/eval modes are blocked before execution, interpreter script operands are path-checked consistently in safety and permission metadata, and existing workspace-safe output redirection remains allowed. |
| 2026-06-29 | `uncommitted` | §8 promotion decision — `write` and `apply_patch` | `inspirations/packages/core/src/tool/builtins.ts` plus the local Harness `write` and `apply_patch` tool definitions. | `crates/harness-tools/src/file_write.rs`; `crates/harness-tools/src/apply_patch_tool.rs`; `crates/harness-tools/src/hashline_edit.rs`; `crates/harness-core/src/coord/permission.rs`; `docs/tools-parity-prd.md`; `docs/native-tool-catalog.md`; `crates/harness-tools/tests/native_execution_surface/03_native_write_apply_patch_exact_edit_test.rs`; `crates/harness-core/tests/coord_auth_test.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json` | `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-core --test coord_auth_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `scripts/test-lanes.sh all-deterministic` | Deterministic native surface dogfood covers `write` create/overwrite, `apply_patch` add/update/delete and preflight failures, provider-safe schema export, exact edit literal behavior, and path-scoped patch permission denial. | implemented | Supersedes §8's prior default deferral for `write` and `apply_patch`. The tools are promoted explicitly for ruleset-compatible toolcall parity, remain under `EditFs` permission/replay/redaction invariants, and are public catalog entries rather than stealth aliases. |
| 2026-06-29 | `uncommitted` | P1.1/P1.5 overlapping built-in Harness-shape compatibility | Harness source re-read from `inspirations/packages/core/src/tool/bash.ts`, `glob.ts`, `grep.ts`, and `websearch.ts`; built-in list from `inspirations/packages/core/src/tool/builtins.ts`. | `crates/harness-tools/src/native_tools/args.rs`; `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/tests/native_execution_surface/05_native_harness_shape_compat_test.rs`; `crates/harness-tools/tests/native_web_search_test.rs`; `crates/harness-tools/tests/native_tool_schema_guidance_test.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json` | `cargo nextest run -p harness-tools --test native_execution_surface_test`; `cargo nextest run -p harness-tools --test native_web_search_test`; `cargo nextest run -p harness-tools --test native_tool_schema_guidance_test`; `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers --test provider_schema_compatibility_test` | Deterministic native tool-surface dogfood: `native_execution_surface_test` proves `bash` accepts Harness's `{ command }` shape without Harness-only `description`, and `glob`/`grep` accept and forward `limit`. `native_web_search_test` proves `websearch` exposes and enforces Harness-bounded controls (`numResults`, `livecrawl`, `type`, `contextMaxCharacters`) before network execution. CLI surface checks ran `harness --help`, config validation happy path, missing-config negative path, and `doctor --json` catalog inspection. | completed_deterministic | Tightened overlapping built-in schemas without weakening strict serde boundaries: `bash.description` is optional, `glob.limit`/`grep.limit` forward to the underlying search tools, and `websearch` now rejects non-Harness control values before backend calls. Provider payload snapshot fixture was regenerated because the model-visible `websearch` schema digest intentionally changed. No live websearch/provider execution is claimed. |
| 2026-06-28 | `9c9667dc` | Baseline observation | `inspirations/harness` `67aec2212010d67775c35e696d8b8b54902eb338` v1.17.11 (2026-06-25); `inspirations/shuvcode` `464339aefb3f998289c2b3aa406724c10d2d1647` v1.2.27-4 (2026-03-20) | `docs/native-tool-catalog.md`; `crates/harness-tools/src/native_tools.rs`; `crates/harness-tools/src/native_tools/args.rs`; `crates/harness-core/src/agent/provider_boundary.rs`; `crates/harness-core/src/tool.rs` | `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test` | `cargo run -p harness -- --config harness.jsonc doctor --json` captures native catalog metadata | baseline_observed | 32 canonical native tool IDs observed. shuvcode local ref is ~2.5 months behind origin/integration; Harness local ref is 3 days behind origin/dev. Both are usable as anchors; upstream shuvcode truncation/cli behavior may need explicit comparison if P2.1 claims change. |
| 2026-06-28 | `9c9667dc` | P0-P2 planning | PRD primary source anchors; crate `AGENTS.md` files | This ledger; `/tmp/harness/ulw-harness-tools-parity-notepad.md` | N/A (planning) | Scenario contract S1–S12 recorded | planning_complete | Plan agent produced wave-ordered task graph: W0 evidence harness, W1 P0.1 inventory, W2 P0.2 provider snapshots, W3 P0.3 schema compatibility, W4 P1.1/1.2/1.3 parallel, W5 P1.4/1.5 parallel, W6 P2.1 TUI display, W7 final acceptance. §8 deferred items remain out of scope. |
| 2026-06-28 | `9c9667dc` | P0.1 Create parity inventory | Harness built-ins from `inspirations/shuvcode/packages/src/tool/registry.ts` (commit `464339ae`): bash, read, glob, grep, edit, write(Deferred), task, webfetch, websearch, codesearch, skill, apply_patch(Deferred), lsp(Experimental), batch(Experimental), todo, question, invalid; `inspirations/packages/core/src/tool/tool.ts` (commit `67aec22`) for definition shape. | `crates/harness-tools/src/tool_catalog.rs`; `crates/harness-tools/src/tool_catalog/harness_parity.rs`; `crates/harness-tools/tests/support/tools_parity_inventory.rs`; `crates/harness-tools/tests/fixtures/tools_parity_inventory.v1.json` | `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test` | S1: `cargo run -p harness -- --config harness.jsonc doctor --json` saved to `target/tools-parity/P0.1/doctor.json` | parity_ready | Inventory covers 31 Harness native tools + Harness-only deferred `write`/`apply_patch`. `baseline_mapping_status` and `baseline_equivalent_id` added to `NativeToolCatalogEntry`; fixture is drift-tested against registry, docs, and doctor JSON. Test regenerates doctor artifact on demand via `cargo run -p harness`. |
| 2026-06-28 | `9c9667dc` | P0.2 Real provider payload snapshots | Harness request serialization: `inspirations/packages/src/session/llm/native-request.ts` and provider transforms in `inspirations/packages/src/provider/transform.ts` (commit `67aec22`); registry inclusion/filtering from `inspirations/shuvcode/packages/src/tool/registry.ts` (commit `464339ae`). | `crates/harness-providers/tests/provider_tool_payload_snapshot_test.rs`; `crates/harness-providers/tests/provider_tool_payload_snapshot_support/`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `crates/harness-providers/Cargo.toml` | `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers` | S1 + S9: mocked prompt with event-log inspection saved to `target/tools-parity/P0.2/events.jsonl` and `summary.txt` | parity_ready | Snapshots generated from real registries for build, plan, category, MCP fixture profiles. Covers dotted IDs (`lsp.rename`, `github.issue`, `shell.run`, `mcp.docs.rs.tool.call`), sanitized function names, description/schema/request digests, Chat+Responses paths, redacted bearer token. Dev-deps `harness-core` and `harness-tools` added to `harness-providers` for test registry construction. |
| 2026-06-28 | `9c9667dc` | P0.3 Provider-family schema compatibility | Harness provider transforms: `inspirations/packages/src/provider/transform.ts` (commit `67aec22`); MCP conversion: `inspirations/packages/src/mcp/catalog.ts` (commit `67aec22`); registry inclusion from `inspirations/shuvcode/packages/src/tool/registry.ts` (commit `464339ae`). | `crates/harness-providers/src/openai/request.rs`; `crates/harness-providers/tests/provider_schema_compatibility_test.rs` (to add); `crates/harness-core/src/agent/provider_boundary.rs` | `cargo nextest run -p harness-providers --test provider_schema_compatibility_test` (to add); `cargo nextest run -p harness-providers` | S7: recorded/live provider run per claimed family or explicit unchecked row | needs_work | Re-read Harness provider transforms and MCP catalog. Will add test proving strict OpenAI-compatible, Gemini-like, Kimi-like, and MCP schema cases using real Harness ToolDefs; unsupported combos must fail with actionable diagnostics. Provider-specific policy stays in provider adapter boundary. |
| 2026-06-28 | `uncommitted` | P0.1 Create parity inventory | `inspirations/shuvcode/packages/src/tool/registry.ts` at `464339aefb3f998289c2b3aa406724c10d2d1647`; §8 deferred tools kept as `deferred_decision` only. | `crates/harness-tools/src/tool_catalog.rs`; `crates/harness-tools/src/tool_catalog/harness_parity.rs`; `crates/harness-tools/tests/native_tool_parity_matrix_test.rs`; `crates/harness-tools/tests/support/tools_parity_inventory.rs`; `crates/harness-tools/tests/fixtures/tools_parity_inventory.v1.json`; `target/tools-parity/P0.1/doctor.json` | `cargo check -p harness-tools --tests`; `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo nextest run -p harness-tools` | S1 artifact: `target/tools-parity/P0.1/doctor.json` contains `native_tool_catalog` with `catalog_source = harness_tools::tool_catalog`, `tool_count = 31`, and tool rows including Harness mapping fields. | completed | Inventory fixture covers all 31 current Harness native catalog rows plus Harness-only `write` and `apply_patch` deferred rows. Drift tests now fail on missing native rows, missing Harness built-ins, invalid statuses, P0/P1 `needs_work`, provider-name drift, alias drift, permission drift, schema drift, and mapping drift. `lsp`/`lsp.rename` remain P2 `needs_work`; no P0/P1 rows remain `needs_work`. |
| 2026-06-28 | `uncommitted` | P0.2 Real provider payload snapshots | Harness request serialization references re-read from `inspirations/packages/src/session/llm/native-request.ts` and `inspirations/packages/src/provider/transform.ts` at `67aec2212010d67775c35e696d8b8b54902eb338`. | `crates/harness-providers/tests/provider_tool_payload_snapshot_test.rs`; `crates/harness-providers/tests/provider_tool_payload_snapshot_support/mod.rs`; `crates/harness-providers/tests/provider_tool_payload_snapshot_support/openai.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `crates/harness-providers/Cargo.toml`; `target/tools-parity/P0.2/events.jsonl`; `target/tools-parity/P0.2/summary.txt` | `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-providers` | S1/S9 offline artifact: `target/tools-parity/P0.2/events.jsonl` records fixture generation and passing provider tests; `summary.txt` records scope and redaction limits. | completed | Snapshot fixture is generated from real coordinator registries for build, plan, category, and MCP fixture profiles. It covers canonical IDs, dotted-ID sanitization (`shell.run`, `github.issue`, `lsp.rename`, `mcp.docs.rs.tool.call`), description/schema digests, OpenAI Chat and Responses request digests, redacted bearer-token markers, and stable `CompletionRequest` digests. No live provider evidence is claimed. |
| 2026-06-28 | `uncommitted` | P0.3 Provider-family schema compatibility | Harness provider transforms re-read from `inspirations/packages/src/provider/transform.ts` at `67aec2212010d67775c35e696d8b8b54902eb338`; MCP catalog re-read from `inspirations/packages/src/mcp/catalog.ts` at `67aec2212010d67775c35e696d8b8b54902eb338`; shuvcode registry re-read from `inspirations/shuvcode/packages/src/tool/registry.ts` at `464339aefb3f998289c2b3aa406724c10d2d1647`. | `crates/harness-providers/src/schema_compat/mod.rs`; `crates/harness-providers/src/schema_compat/gemini.rs`; `crates/harness-providers/src/schema_compat/kimi.rs`; `crates/harness-providers/src/openai.rs`; `crates/harness-providers/src/lib.rs`; `crates/harness-providers/tests/provider_schema_compatibility_test.rs`; `crates/harness-providers/tests/provider_schema_compatibility_support/mod.rs`; `target/tools-parity/P0.3/unchecked.txt`; `target/tools-parity/P0.3/summary.txt` | `cargo nextest run -p harness-providers --test provider_schema_compatibility_test`; `cargo nextest run -p harness-providers` | S7 unchecked artifact: `target/tools-parity/P0.3/unchecked.txt` records no live/recorded provider-family access for strict OpenAI-compatible, Gemini/Google-like, Moonshot/Kimi-like, and MCP schema provider-safety families. | completed | Provider-specific schema policy stays in `harness-providers`: real build/plan ToolDefs pass strict OpenAI-compatible checks; real MCP fixture ToolDefs cover Gemini and MCP safety; Gemini-like schemas normalize type arrays, numeric enums, and stale required fields; Kimi-like schemas normalize `$ref` siblings and tuple `items`; incompatible top-level combinators return `UnsupportedToolCall` before the fake HTTP transport is called. |
| 2026-06-28 | `uncommitted` | P1.1 Model-facing descriptions and field schemas | Harness tool ergonomics references re-read from `inspirations/packages/core/src/tool/tool.ts`, `inspirations/packages/plugin/src/tool.ts`, `inspirations/shuvcode/packages/src/cli/cmd/run.ts`, and `inspirations/packages/src/provider/transform.ts` at the pinned local references noted above. Local `src/tool/**` definition files were not present in the checked-out Harness references, so this phase used observable registry/display/provider schema anchors plus the Harness PRD acceptance text. | `crates/harness-tools/src/native_tools/args.rs`; `crates/harness-tools/src/native_tools/args/control.rs`; `crates/harness-tools/tests/native_tool_schema_guidance_test.rs`; `crates/harness-providers/tests/fixtures/provider_tool_payload_snapshots.v1.json`; `target/tools-parity/P1.1/summary.txt`; `target/tools-parity/P1.1/unchecked.txt` | `cargo fmt -p harness-tools --check`; `cargo check -p harness-tools --tests`; `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo nextest run -p harness-tools --test native_tool_schema_guidance_test`; `cargo nextest run -p harness-providers --test provider_tool_payload_snapshot_test`; `cargo nextest run -p harness-tools` | Deterministic evidence: `target/tools-parity/P1.1/summary.txt`; live model dogfood unchecked artifact: `target/tools-parity/P1.1/unchecked.txt`. | completed | Added field-level schema descriptions for high-risk provider-visible native wrappers without weakening `deny_unknown_fields` or compatibility parsing. A new schema guidance regression fails if list/glob/grep/bash/task/background/background_cancel/batch/skill/webfetch/websearch/codesearch fields lose descriptions. Provider snapshots were refreshed to make schema/description digest drift visible. Live model selection prompts were not run in this phase and remain explicitly unchecked. |
| 2026-06-28 | `uncommitted` | P1.2 Task/background/batch/skill ergonomics | Harness CLI display references re-read from `inspirations/shuvcode/packages/src/cli/cmd/run.ts`; Harness tool/plugin contract references re-read from `inspirations/packages/core/src/tool/tool.ts` and `inspirations/packages/plugin/src/tool.ts`. | `crates/harness-tools/src/agent_ops/batch.rs`; `crates/harness-tools/tests/native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test.rs`; `crates/harness-tools/tests/native_agent_spawn_and_batch_preserve_lineage_permissions_and_order/04b_batch_display_guidance_test.rs`; `target/tools-parity/P1.2/summary.txt` | `cargo fmt -p harness-tools --check`; `cargo check -p harness-tools --tests`; `cargo nextest run -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test part_04b_batch_display_guidance_test`; `cargo nextest run -p harness-tools --test native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test`; `cargo nextest run -p harness-tools` | Deterministic Harness-surface evidence: `native_agent_spawn_and_batch_preserve_lineage_permissions_and_order_test` exercises task/background/background_cancel/batch/skill paths through the coordinator; phase summary in `target/tools-parity/P1.2/summary.txt`. | completed | Existing task/background/skill regressions already covered required selectors, required `run_in_background`/`load_skills`, background `request_id` retrieval/cancellation, skill metadata/rejection paths, and worker redelegation blocking. This phase fixed the remaining batch display gap: model-facing batch output now enumerates child results in input order and states coordinator permission attribution for each child, while preserving structured JSON and permission enforcement. |
| 2026-06-28 | `uncommitted` | P1.3 Truncation/artifact presentation | Harness truncation reference inspected from `inspirations/shuvcode/packages/src/tool/truncation.ts` (`MAX_LINES = 2000`, `MAX_BYTES = 50 * 1024`, `outputPath` metadata) and read behavior from `inspirations/shuvcode/packages/src/file/index.ts` at `464339aefb3f998289c2b3aa406724c10d2d1647`. | `crates/harness-tools/src/fs_read.rs`; `crates/harness-tools/src/fs_read/args.rs`; `crates/harness-tools/src/fs_read/render.rs`; `crates/harness-tools/src/fs_read/window.rs`; `crates/harness-tools/src/fs_read/tests.rs`; `crates/harness-tools/src/fs_read/tests/read_tool.rs`; `crates/harness-tools/tests/native_read_truncation_presentation_test.rs`; `target/tools-parity/P1.3/summary.txt` | `cargo fmt -p harness-tools --check`; `cargo check -p harness-tools --tests`; `cargo nextest run -p harness-tools --test native_read_truncation_presentation_test`; `cargo nextest run -p harness-tools` | Deterministic Harness-surface evidence: `native_read_truncation_presentation_test` exercises canonical `read` through the coordinator registry and verifies model-facing full-artifact plus next-window guidance; phase summary in `target/tools-parity/P1.3/summary.txt`. | completed | `read`/`fs.read` truncation output now names the full output artifact and tells the model how to continue inline with the next `offset`/`limit`. Structured read metadata includes `next_offset` and `output_artifact` when a spill artifact exists. The oversized `fs_read.rs` test surface was split under `src/fs_read/` while preserving existing fs.read behavior and artifact coverage. |
| 2026-06-28 | `uncommitted` | P1.4 Invalid-call recovery | Harness invalid-call references inspected from `inspirations/packages/src/tool/tool.ts`, `inspirations/shuvcode/packages/src/tool/tool.ts`, `inspirations/shuvcode/packages/src/tool/invalid.ts`, and `inspirations/shuvcode/packages/src/tool/batch.ts` at the pinned local references noted above. | `crates/harness-tools/src/arg_parse.rs`; `crates/harness-tools/src/lib.rs`; `crates/harness-tools/tests/native_malformed_args_recovery_test.rs`; `target/tools-parity/P1.4/summary.txt` | `cargo fmt -p harness-tools --check`; `cargo check -p harness-tools --tests`; `cargo nextest run -p harness-tools --test native_malformed_args_recovery_test`; `cargo nextest run -p harness-tools` | Deterministic Harness-surface evidence: `native_malformed_args_recovery_test` exercises canonical `read` through the coordinator registry with malformed args; phase summary in `target/tools-parity/P1.4/summary.txt`. | completed | Shared native serde argument parsing now preserves strict schema rejection while adding deterministic repair guidance: invalid tool call arguments should be rewritten to match the tool schema, followed by the original serde detail. This phase did not add unknown-tool fallback or permissive catch-all execution. |
| 2026-06-28 | `uncommitted` | P1.5 Permission and nested-intent presentation | Harness permission/prompt references re-read from `inspirations/packages/core/src/tool/tool.ts` and `inspirations/shuvcode/packages/src/tool/batch.ts` at pinned local references. | `crates/harness-core/tests/batch_inherits_nested_tool_permissions_without_bypass_test.rs`; `crates/harness-core/tests/permission_policy_supports_native_tool_permission_kinds_test.rs`; `crates/harness-core/tests/coord_test.rs`; `docs/permissions.md`; `docs/sessions-and-replay.md`; `target/tools-parity/P1.5/` | `cargo nextest run -p harness-core --test batch_inherits_nested_tool_permissions_without_bypass_test`; `cargo nextest run -p harness-core --test permission_policy_supports_native_tool_permission_kinds_test`; `cargo nextest run -p harness-core --test coord_test`; `cargo nextest run -p harness-tools` | S3/S5/S7/S8/S9 permission dogfood: CLI/TUI prompts for bash, edit, task, webfetch/websearch/codesearch, lsp.rename, nested batch, session tools | completed | Extended nested batch permission test to assert child tool name in prompt metadata. Added headless ask-deny and worker policy violation coverage in `coord_test`. Added session info test asserting replay-only/no-side-effect copy. Updated `docs/permissions.md` and `docs/sessions-and-replay.md` operator-trust copy. |
| 2026-06-28 | `uncommitted` | P1.5 Permission and nested-intent presentation | Harness permission/prompt references re-read from `inspirations/packages/core/src/tool/tool.ts` and `inspirations/shuvcode/packages/src/tool/batch.ts` at pinned local references. | `crates/harness-core/tests/batch_inherits_nested_tool_permissions_without_bypass_test.rs`; `crates/harness-core/tests/coord/07_tool_results_project_in_assistant_source_test.rs`; `crates/harness-core/tests/permission_policy_supports_native_tool_permission_kinds_test.rs`; `crates/harness-tools/tests/session_info_tool_test.rs`; `docs/sessions-and-replay.md`; `target/tools-parity/P1.5/summary.txt` | `cargo fmt -p harness-core -p harness-tools --check`; `cargo nextest run -p harness-core --test batch_inherits_nested_tool_permissions_without_bypass_test`; `cargo nextest run -p harness-core --test permission_policy_supports_native_tool_permission_kinds_test`; `cargo nextest run -p harness-core --test coord_test`; `cargo nextest run -p harness-tools` | Deterministic S3/S5/S7/S8/S9 evidence in `target/tools-parity/P1.5/summary.txt`. | completed | Nested batch permission prompts now expose the effective child tool, permission coverage includes allow/headless ask-deny/worker policy violation, docs/tests lock mutating file/workspace/network implications, and replay-derived session inspection is documented/tested as side-effect free. No live network/provider evidence is claimed. |
| 2026-06-28 | `uncommitted` | P2.1 TUI/CLI tool display descriptors | Harness CLI display reference re-read from `inspirations/shuvcode/packages/src/cli/cmd/run.ts` at `464339aefb3f998289c2b3aa406724c10d2d1647`: tool-row `inline`/`block`/`fallback` patterns, icons (`→ ✱ $ ← % ◈ ◇ ✦ ☑ ⌘`), titles (`Read`, `Glob`, `Grep`, `Edit`, `Write`, `WebFetch`, `Exa Web Search`, `Skill`, `Todos`, `Task`), and status-based icon selection (`✓ • ✗`). | `crates/harness-tui/src/ui_tool_titles.rs`; `crates/harness-tui/src/ui_transcript_tool_sections.rs`; `crates/harness-tui/src/ui_transcript.rs`; `crates/harness-tui/tests/support/p21_tool_display_fixtures.rs`; `crates/harness-tui/tests/deterministic_render_test.rs`; `crates/harness-tui/tests/snapshots/deterministic_render_test__p21_tool_display_descriptors_cover_state_families_without_pty.snap`; `target/tools-parity/P2.1/render_snapshot.txt`; `target/tools-parity/P2.1/unchecked.txt` | `cargo nextest run -p harness-tui --test deterministic_render_test`; `cargo nextest run -p harness-tui --test tui_signoff_manifest_test`; `cargo nextest run -p harness-tui` | Deterministic render evidence: `p21_tool_display_descriptors_cover_state_families_without_pty` test covers S1 completed, S2 running, S3 failed, S4 denied, S5 truncated, S6 cancelled, S7 late-result, S8 plan_enter; render snapshot in `target/tools-parity/P2.1/render_snapshot.txt`. S12 PTY signoff unchecked: `target/tools-parity/P2.1/unchecked.txt` records non-interactive agent environment limitation. | completed | Added intentional display descriptors for 17 Harness-only tool families (`background_cancel`, `plan_enter`, `plan_exit`, `invalid`, `session_list/read/search/info`, `ast_grep_search/replace`, `lsp`, `lsp.rename`, `skill`, `todoread`, `todowrite`, `shell.run`) so they no longer fall back to generic `generic_tool_title` rows. Each tool now renders status-aware titles (running/completed/failed), icons, and concise descriptions. Truncated output renders an artifact link with next-action guidance when output is visible. Deterministic render test covers all 7 state families (running, completed, failed, denied, truncated, cancelled, late-result) plus Harness-only tool families. No live PTY visual parity is claimed. |
| 2026-06-28 | `uncommitted` | Final acceptance / source-branding cleanup | N/A | `crates/harness-tools/src/tool_catalog.rs`; `crates/harness-tools/src/tool_catalog/baseline_parity.rs`; `crates/harness-tools/tests/support/baseline_tools_parity_inventory_support.rs`; `crates/harness-tools/tests/fixtures/tools_parity_inventory.v1.json`; `scripts/check-forbidden-branding.py`; `scripts/check-test-suite-gates.py`; `crates/harness-core/tests/coord/07b_permission_flow_test.rs`; `crates/harness-core/tests/permission_policy_supports_native_tool_permission_kinds_test.rs` | All PRD §5 crate tests (P0.1–P2.1) pass; `cargo check --workspace --tests` is clean except pre-existing harness-tui warnings; `cargo nextest run -p harness-core --test coord_test` passes | Targeted parity tests pass; `python3 scripts/check-test-suite-gates.py` no longer reports parity-specific taxonomy, convention, or file-focus hits; `python3 scripts/check-forbidden-branding.py` no longer reports parity-specific source hits | awaiting_dogfood_evidence | Renamed internal source identifiers from `harness_*` to `baseline_*` to avoid adding new forbidden-brand hits. Fixture keys normalized to `baseline_mapping_status`/`baseline_equivalent_id`; fixture `source` values use `external_reference`. Added `_support.rs` to test taxonomy suffixes and renamed support modules. Moved new permission-flow test out of `07_tool_results_project_in_assistant_source_test.rs` to clear file-focus gate and added convention sections. Added canonical `batch` TUI row handling and reconciled `lsp` fixture notes. Remaining gate failures are pre-existing auth/TUI/poc-candidate debt. |
| 2026-06-28 | `uncommitted` | Final acceptance — P0.2/P1.1 evidence and LSP rescope | N/A | `crates/harness-tools/src/tool_catalog/baseline_parity.rs`; `crates/harness-tools/tests/fixtures/tools_parity_inventory.v1.json`; `target/baseline-tools-parity/P0.1/doctor.json`; `target/tools-parity/P0.2/events.jsonl`; `target/tools-parity/P0.2/summary.md`; `target/tools-parity/P1.1/summary.md`; `target/tools-parity/P1.1/unchecked.txt`; `docs/tools-parity-progress.md` | All PRD §5 targeted crate tests still pass after LSP status change; `cargo check --workspace --tests` clean except pre-existing harness-tui warnings | S1/P0.2: real `harness.jsonc` deterministic event log at `target/tools-parity/P0.2/events.jsonl` with 27 events, 1 `edit` tool call, permission allow, and applied hashline edit. S1/P1.1: doctor JSON shows high-risk tool descriptions; `native_tool_schema_guidance_test`, `native_malformed_args_recovery_test`, and `native_read_truncation_presentation_test` pass; live model prompts remain unchecked and recorded. `scripts/check-test-suite-gates.py` and `check-forbidden-branding.py` remain clean of parity-specific hits | completed | Rescoped `lsp`/`lsp.rename` mapping status from `needs_work` to `harness_adapted` to reflect P2.1 display-parity completion while keeping LSP behavior parity explicitly in §8 deferred items. Regenerated doctor artifact under `target/baseline-tools-parity/P0.1/doctor.json` using `cargo run -q` to keep the JSON file clean. Oracle review completed: parity-specific blockers cleared; P1.1 live model prompts remain explicitly unchecked and are recorded. |
| 2026-06-29 | `uncommitted` | P0.3 Provider-family schema compatibility (rescope) | Same Harness provider transform and MCP catalog references as the 2026-06-28 P0.3 row: `inspirations/packages/src/provider/transform.ts` and `inspirations/packages/src/mcp/catalog.ts` at `67aec2212010d67775c35e696d8b8b54902eb338`; `inspirations/shuvcode/packages/src/tool/registry.ts` at `464339aefb3f998289c2b3aa406724c10d2d1647`. | `crates/harness-providers/src/schema_compat/mod.rs`; `crates/harness-providers/src/schema_compat/gemini.rs`; `crates/harness-providers/src/schema_compat/kimi.rs`; `crates/harness-providers/src/openai.rs`; `crates/harness-providers/tests/provider_schema_compatibility_test.rs`; `crates/harness-providers/tests/provider_schema_compatibility_support/mod.rs`; `target/tools-parity/P0.3/unchecked.txt`; `target/tools-parity/P0.3/summary.txt` | `cargo nextest run -p harness-providers --test provider_schema_compatibility_test`; `cargo nextest run -p harness-providers` | S7 live/recorded provider-family dogfood: `unchecked_blocked_no_credentials`. `target/tools-parity/P0.3/unchecked.txt` records that no live or family-specific recorded provider access was available for strict OpenAI-compatible, Gemini/Google-like, Moonshot/Kimi-like, or MCP schema provider-safety families. Deterministic adapter-boundary tests using real Harness ToolDefs and fake OpenAI-compatible transport remain verified. | completed_deterministic_blocked_live | Supersedes the 2026-06-28 P0.3 `completed` row. Deterministic provider-family schema compatibility is verified: real build/plan ToolDefs pass strict OpenAI-compatible checks, Gemini-like schemas normalize type arrays/numeric enums/stale required fields, Kimi-like schemas normalize `$ref` siblings and tuple `items`, and incompatible top-level combinators return `UnsupportedToolCall` before HTTP transport. Live/recorded provider-family dogfood is explicitly `unchecked_blocked_no_credentials` because no live OpenAI-compatible, Gemini/Google, Moonshot/Kimi, or MCP provider credentials or P0.3-specific cassettes were available in this environment. |
| 2026-06-29 | `uncommitted` | P2.1 TUI/CLI tool display descriptors (rescope) | Same Harness CLI display reference as the 2026-06-28 P2.1 row: `inspirations/shuvcode/packages/src/cli/cmd/run.ts` at `464339aefb3f998289c2b3aa406724c10d2d1647`. | `crates/harness-tui/src/ui_tool_titles.rs`; `crates/harness-tui/src/ui_transcript_tool_sections.rs`; `crates/harness-tui/src/ui_transcript.rs`; `crates/harness-tui/tests/support/p21_tool_display_fixtures.rs`; `crates/harness-tui/tests/deterministic_render_test.rs`; `crates/harness-tui/tests/snapshots/deterministic_render_test__p21_tool_display_descriptors_cover_state_families_without_pty.snap`; `target/tools-parity/P2.1/render_snapshot.txt`; `target/tools-parity/P2.1/unchecked.txt` | `cargo nextest run -p harness-tui --test deterministic_render_test`; `cargo nextest run -p harness-tui --test tui_signoff_manifest_test`; `cargo nextest run -p harness-tui`; `RUST_TEST_THREADS=1 cargo nextest run -p harness-testkit --test pty_e2e --test-threads 1` | S12 true PTY visual signoff: `unchecked_blocked_non_interactive_env`. `target/tools-parity/P2.1/unchecked.txt` records that the `signoff-pty` lane requires an interactive terminal with a real PTY. `RUST_TEST_THREADS=1 cargo nextest run -p harness-testkit --test pty_e2e` passes 6 deterministic structural tests (manifest shape, fail-closed behavior, artifact directory creation) but produces no PTY visual capture artifacts. Deterministic render evidence in `target/tools-parity/P2.1/render_snapshot.txt` remains verified. | completed_deterministic_blocked_pty | Supersedes the 2026-06-28 P2.1 `completed` row. Deterministic render evidence is verified: `p21_tool_display_descriptors_cover_state_families_without_pty` covers S1 through S8 state families and 17 Harness-only tool families with intentional titles, status labels, and artifact references. True PTY visual signoff is explicitly `unchecked_blocked_non_interactive_env` because the `signoff-pty` lane requires an interactive terminal with a real PTY, which is not available in this non-interactive agent execution environment. The PTY smoke tests verify manifest structure and fail-closed behavior but do not capture visual rendering. |
| 2026-06-29 | `uncommitted` | Final acceptance — P0.3/P2.1 evidence rescope | N/A | `docs/tools-parity-progress.md`; `target/tools-parity/P0.3/unchecked.txt`; `target/tools-parity/P0.3/summary.txt`; `target/tools-parity/P2.1/unchecked.txt`; `target/tools-parity/P2.1/render_snapshot.txt` | All PRD §5 deterministic crate tests pass; `cargo nextest run -p harness-providers`; `cargo nextest run -p harness-tui --test deterministic_render_test`; `RUST_TEST_THREADS=1 cargo nextest run -p harness-testkit --test pty_e2e --test-threads 1` | P0.3 S7 live/recorded provider-family dogfood: `unchecked_blocked_no_credentials`. P2.1 S12 true PTY visual signoff: `unchecked_blocked_non_interactive_env`. All deterministic evidence for P0.1 through P2.1 remains verified. | awaiting_dogfood_evidence | Supersedes the 2026-06-28 final acceptance `completed` row. P0.3 deterministic provider-family schema compatibility and P2.1 deterministic render evidence remain verified. P0.3 live/recorded provider-family dogfood is deferred until live OpenAI-compatible, Gemini/Google, Moonshot/Kimi, or MCP provider credentials or cassettes are available. P2.1 true PTY visual signoff is deferred until the `signoff-pty` lane can be run in an interactive terminal with a real PTY. `python3 scripts/check-forbidden-branding.py` reports no new hits from these edits. |
| 2026-06-29 | `uncommitted` | P0.1 Create parity inventory (profile toolsets and description overrides) | Same Harness registry references as the 2026-06-28 P0.1 rows: `inspirations/shuvcode/packages/src/tool/registry.ts` at `464339aefb3f998289c2b3aa406724c10d2d1647`; `inspirations/packages/core/src/tool/tool.ts` at `67aec2212010d67775c35e696d8b8b54902eb338`. | `crates/harness-tools/tests/support/baseline_tools_parity_inventory_support.rs`; `crates/harness-tools/tests/support/baseline_tools_parity_inventory_doctor_support.rs`; `crates/harness-tools/tests/fixtures/tools_parity_inventory.v1.json`; `target/baseline-tools-parity/P0.1/doctor.json` | `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test` | S1: `target/baseline-tools-parity/P0.1/doctor.json` generated from `harness.jsonc` includes `active_profiles` and `profile_description_overrides` per native tool row. | completed | Supersedes prior P0.1 rows. Inventory fixture now carries `active_profiles` and `profile_description_overrides` per row. `baseline_tools_parity_inventory_doctor_support.rs` asserts doctor catalog summary shape, active profile drift against resolved routes, and profile description override drift against the native tool catalog. `baseline_tools_parity_inventory_support.rs` asserts every native catalog row, external reference builtin, and status/permission/schema/provider-name invariant. |
| 2026-06-29 | `uncommitted` | P1.1 Model-facing descriptions and field schemas (mocked dogfood) | Same Harness tool ergonomics references as the 2026-06-28 P1.1 row: `inspirations/packages/core/src/tool/tool.ts`, `inspirations/packages/plugin/src/tool.ts`, `inspirations/shuvcode/packages/src/cli/cmd/run.ts`, and `inspirations/packages/src/provider/transform.ts` at pinned local references. | `crates/harness/tests/dogfood_harness_jsonc_tool_parity_test.rs`; `crates/harness/tests/support/dogfood_harness_jsonc_tool_parity_support.rs`; `target/tools-parity/P1.1/summary.md`; `target/tools-parity/P1.1/unchecked.txt` | `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test` | Mocked prompt-path dogfood using real `harness.jsonc`: `vague_prompt_uses_model_visible_tool_definitions_to_select_glob` verifies `glob`/`grep`/`read` definitions reach the provider and the event log records a successful glob tool call; `malformed_tool_args_return_recovery_text_then_corrected_read_succeeds` verifies malformed read args return recovery text plus serde detail, then a corrected read succeeds. Live model prompts remain unchecked per `target/tools-parity/P1.1/unchecked.txt`. | completed_with_deferred_dogfood | Supersedes prior P1.1 rows. Mock provider runs the in-process `harness prompt` path with copied `harness.jsonc`, `CliHarness`/`CliDeps`, and a scripted `DogfoodPromptProvider` override. Vague tool selection and bad-argument recovery are verified deterministically through the real runtime. Live model vague-tool-selection and bad-argument-recovery prompts remain explicitly unchecked, blocked by no live provider credentials. |
| 2026-06-29 | `uncommitted` | P1.3 Truncation/artifact presentation (large-output dogfood) | Same Harness truncation reference as the 2026-06-28 P1.3 row: `inspirations/shuvcode/packages/src/tool/truncation.ts` (`MAX_LINES = 2000`, `MAX_BYTES = 50 * 1024`, `outputPath` metadata) at `464339aefb3f998289c2b3aa406724c10d2d1647`. | `crates/harness-tools/tests/native_large_output_dogfood_test.rs`; `crates/harness-tools/tests/support/native_large_output_dogfood_support.rs`; `crates/harness-tools/src/agent_ops/child_metadata/child_next_actions.rs` | `cargo nextest run -p harness-tools --test native_large_output_dogfood_test` | Deterministic large-output dogfood: `read_grep_and_bash_spill_large_outputs_with_model_guidance` verifies `read`, `grep`, and `bash` spill to artifacts with bounded inline text, `next_offset`/`output_artifact` structured JSON, and model-facing continuation guidance; `session_read_spills_redacted_replay_only_large_sessions` verifies `session_read` spills redacted replay-only JSON without executing command-looking events; `child_task_returns_capped_summary_and_session_next_actions` verifies child task output is capped at 1200 chars with `session_info`/`session_read` next actions. | completed | Supersedes prior P1.3 rows. Large-output parity now covers `bash`, `grep`, `read`, and `session_read` spill artifacts with model-facing guidance, plus child-task artifact/summary/next-action evidence. `child_next_actions.rs` emits `session_info`, `session_read`, `background_output` (status/wait/cancel), and `task` continuation actions. No live provider evidence is claimed; child task uses a deterministic `LargeSummaryProvider`. |
| 2026-06-29 | `uncommitted` | Final acceptance — P0.1/P1.1/P1.3 evidence expansion | N/A | `docs/tools-parity-progress.md`; `crates/harness-tools/tests/support/baseline_tools_parity_inventory_support.rs`; `crates/harness-tools/tests/support/baseline_tools_parity_inventory_doctor_support.rs`; `crates/harness/tests/dogfood_harness_jsonc_tool_parity_test.rs`; `crates/harness/tests/support/dogfood_harness_jsonc_tool_parity_support.rs`; `crates/harness-tools/tests/native_large_output_dogfood_test.rs`; `crates/harness-tools/tests/support/native_large_output_dogfood_support.rs`; `crates/harness-tools/src/agent_ops/child_metadata/child_next_actions.rs`; `target/tools-parity/P1.1/summary.md`; `target/tools-parity/P1.1/unchecked.txt` | All PRD §5 targeted crate tests pass: `cargo nextest run -p harness-tools --test native_tool_parity_matrix_test`; `cargo nextest run -p harness --test dogfood_harness_jsonc_tool_parity_test`; `cargo nextest run -p harness-tools --test native_large_output_dogfood_test`; `cargo nextest run -p harness-providers`; `cargo nextest run -p harness-tui --test deterministic_render_test`; `RUST_TEST_THREADS=1 cargo nextest run -p harness-testkit --test pty_e2e --test-threads 1` | P0.3 S7 live/recorded provider-family dogfood: `unchecked_blocked_no_credentials`. P2.1 S12 true PTY visual signoff: `unchecked_blocked_non_interactive_env`. P1.1 live model prompts: `unchecked_blocked_no_credentials`. All deterministic evidence for P0.1 through P2.1 remains verified, including the new P0.1 profile toolset/description-override inventory, P1.1 mocked `harness.jsonc` dogfood, and P1.3 large-output dogfood. | completed_with_deferred_dogfood | Supersedes the 2026-06-29 final acceptance `awaiting_dogfood_evidence` row. All PRD §5 deterministic targeted tests pass. P0.1 inventory now covers profile toolsets and profile-specific description overrides. P1.1 mocked dogfood verifies vague tool selection and bad-argument recovery through the real `harness.jsonc` prompt path. P1.3 large-output dogfood covers `bash`/`grep`/`read`/`session_read` spill artifacts and child-task capped summaries with next actions. P0.3 live provider-family, P2.1 true PTY visual signoff, and P1.1 live model prompts remain explicitly unchecked, blocked by no live provider credentials and non-interactive environment. `python3 scripts/check-forbidden-branding.py` reports no new hits from these edits. |
