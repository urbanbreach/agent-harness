//! Current Harness changes, shared by welcome and the release-notes viewer.
//! Keep these tied to shipped code rather than copying reference product claims.
pub(crate) const CURRENT: [&str; 3] = [
    "Structural Markdown preserves nested styles, code indentation, tables and math.",
    "Browse transcript entries and recorded tool output in a searchable full view.",
    "The session dashboard keeps replies editable while new events arrive.",
];
