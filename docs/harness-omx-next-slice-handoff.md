# Legacy OMX next-slice handoff

This handoff is retained as background migration evidence only. It is not the product direction.

Use the current workflow registry, command inventory, and ultragoal ledger for active implementation work. Legacy OMO parity items are migration context and must stay subordinate to the single-operator workflow orchestration contract.
