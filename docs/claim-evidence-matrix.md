# Claim evidence matrix

This matrix maps maintained release-facing claims to current evidence or explicit limitations.

| Requirement / claim text | Evidence type | Evidence pointer | Verification | Observed result | Provenance | Status |
|---|---|---|---|---|---|---|
| Strict V1 command/hook lifecycle map is documented and source-guarded. | docs-reference + coord tests | `docs/extension-strategy.md`; `crates/harness/tests/config_docs_reference_test.rs`; `crates/harness-core/tests/coord/*hook*` | `cargo test -p harness --test config_docs_reference_test extension_strategy_documents_command_hook_lifecycle_map`; focused `cargo test -p harness-core --test coord_test ...hook...` tests | Markdown command interpolation is intentionally unsupported; every current `HookLifecycleEvent` appears in the lifecycle map; hook safety/replay boundaries are covered by focused coordinator tests. | 2026-05-30 strict V1 closeout G004 | PASS |
| Skill discovery precedence is deterministic across Harness-owned, built-in, global, and explicitly imported external editor/assistant/agent roots. | discovery tests + docs-reference guard | `crates/harness-tools/tests/skill_load_discovery/01_skill_load_discovers_project_and_global_test.rs`; `crates/harness-tools/tests/skill_load_discovery/03_v1_skill_contract_test.rs`; `docs/config.md`; `docs/starter-skills.md`; `docs/extension-strategy.md` | `cargo test -p harness-tools --test skill_load_discovery_test`; `cargo test -p harness --test config_docs_reference_test config_docs_capture_v1_skill_contract_and_authoring_guide` | Harness-owned roots stay default; `.external-editor/skills`, `.assistant/skills`, `.agents/skills`, and user equivalents load only when explicitly configured. When explicitly configured, compatibility roots are imported after Harness-owned and other non-compatibility roots, project compatibility roots beat global compatibility roots, configured compatibility order breaks ties, duplicate ids are `shadowed`, and shipped built-in skill ids still resolve to `skill:project:*`. | 2026-05-30 strict V1 skill precedence closeout | PASS |
| Bundled skill resources load only on activation and stay inside documented caps/path boundaries. | skill activation tests + docs | `crates/harness-tools/src/skill_catalog.rs`; `crates/harness-tools/tests/skill_load_discovery/03_v1_skill_contract_test.rs`; `docs/extension-strategy.md`; `docs/starter-skills.md` | `cargo test -p harness-tools --test skill_load_discovery_test -- --nocapture`; `cargo test -p harness-tools -- --nocapture` | `resources` frontmatter loads comma/newline relative files only on skill activation, caps at 5 files/64 KiB per file/200 KiB total/depth 4, redacts loaded resource text, and rejects `..`, absolute paths, globs, directories, and symlink escapes across project and global roots. | 2026-05-30 pre-V1 enhancements WS8 | PASS |
| Strict V1 roadmap closeout readiness evidence is restored and guarded against dangling references. | docs-reference test + progress log | `docs/v1-release-readiness-slice-prd.md`; `docs/v1-release-readiness-slice-progress.md`; `.omx/ultragoal/evidence/G001-prep-classification/roadmap-classification-baseline.md` | `cargo test -p harness --test config_docs_reference_test readiness_closeout_docs_are_current_and_back_roadmap_claims` | Readiness PRD/progress docs exist, roadmap/budgets references are non-dangling, and checked release-blocker rows require progress or matrix evidence before checkbox flips. | 2026-05-30 strict V1 closeout G002 | PASS |
| README first source build quick start commands (`--help`, `--version`, `config validate`, `doctor`, mocked `prompt`) are real CLI surfaces. | test/lane | `crates/harness/tests/binary_smoke.rs` | `scripts/test-lanes.sh signoff-binary` | Env-gated lane exercises the real binary from outside the repo and writes per-stage artifacts when run. | 2026-05-30 local lane runner | PASS |
| README deterministic mocked execution prompt path is explicitly scoped apart from live provider/auth proof. | test/docs | `README.md`; `crates/harness/tests/binary_smoke.rs` | `cargo test -p harness --test binary_smoke -- --ignored --exact` via `scripts/test-lanes.sh signoff-binary` | Mock prompt produces `Hello world` and an event log; README states this does not prove live transport health. | 2026-05-30 env-gated binary smoke | PASS |
| README doctor local readiness only claim is enforced by doctor JSON scope fields. | test/CLI | `crates/harness/tests/config_schema_cli/02_doctor_cli_reports_shipped_orchestration_health_test.rs` | `cargo test -p harness --test config_schema_cli_test doctor_cli_emits_json_report` | Doctor JSON reports `readiness_scope: local_readiness_only`, `provider_execution_proof: false`, and `no_network_probes: true`. | 2026-05-29 deterministic doctor test | PASS |
| README native tool catalog claim mirrors the runtime registry. | test | `crates/harness-tools/tests/native_tool_parity_matrix_test.rs` | `cargo test -p harness-tools --test native_tool_parity_matrix_test` | Registry/doc drift fails the test. | 2026-05-29 deterministic | PASS |
| `ast_grep_replace` ships behind edit permission with dry-run/apply safety gates. | native tool tests + parity/docs | `crates/harness-tools/tests/native_ast_grep_replace_test.rs`; `crates/harness-core/tests/coord_ast_grep_auth_test.rs`; `docs/native-tool-catalog.md`; `docs/ast-grep-replace-safety-gate.md` | `cargo test -p harness-tools --test native_ast_grep_replace_test`; `cargo test -p harness-tools --test native_tool_parity_matrix_test`; `cargo test -p harness-core --test coord_ast_grep_auth_test -- --nocapture`; `cargo test -p harness-core --test permission_policy_supports_native_tool_permission_kinds_test` | Tool capability is `EditFs`/public `edit`, defaults to dry-run, writes diff artifacts, validates workspace paths/byte ranges, rejects truncated apply, and coordinator path-scoped deny rules for list-valued `paths` prevent execution before adapter invocation while allowed list paths still execute. | 2026-05-30 strict V1 closeout G009 | PASS |
| Typed extension manifests ship as descriptor-only V1 metadata, not a plugin host. | core parser/schema tests + docs/doctor guard | `crates/harness-core/src/extension_manifest.rs`; `crates/harness-core/tests/extension_manifest_test.rs`; `configs/extension-manifest.v1.schema.json`; `docs/extension-strategy.md`; `crates/harness/src/doctor.rs` | `cargo test -p harness-core extension_manifest`; `cargo test -p harness --test config_schema_cli_test doctor_cli_json_reports_extension_roadmap_readiness_separately`; `cargo test -p harness --test config_docs_reference_test extension_strategy_documents_descriptor_only_manifest_seam` | `ExtensionManifestV1` validates stable ids, unique capabilities, permission-bearing tool descriptors, known hook lifecycles, static replay metadata, and rejects executable/unknown fields; runtime effects report descriptor-only scope with no tool registration, command execution, MCP launch, provider decorator invocation, external code loading, session mutation, runtime permission path, or extension tool event replay rendering. | 2026-05-30 strict V1 closeout G009 | PASS |
| External executable plugins and active upstream plugin compatibility are post-V1 policy, not V1 runtime behavior. | docs-reference guard + descriptor-only manifest tests | `docs/extension-strategy.md`; `docs/roadmap-v1.md`; `crates/harness-core/tests/extension_manifest_test.rs` | `cargo test -p harness --test config_docs_reference_test extension_strategy_documents_descriptor_only_manifest_seam`; `cargo test -p harness-core --test extension_manifest_test` | Extension strategy labels executable plugin hooks as `post_v1`, states arbitrary executable plugins and upstream plugin compatibility remain post-V1, and the typed manifest rejects executable host behavior. Roadmap policy boxes are checked while explicit post-V1 implementation boxes remain unchecked. | 2026-05-30 strict V1 closeout G008 | PASS |
| TUI signoff manifest covers startup, onboarding/auth, command palette, session picker/resume, permission/question, provider/tool failure, and diff review without claiming missing native screenshots. | deterministic render snapshots + PTY manifest artifact | `docs/tui-signoff-manifest.v1.json`; `crates/harness-tui/tests/deterministic_render_test.rs`; `crates/harness-testkit/tests/pty_e2e.rs`; `docs/testing.md` | `cargo test -p harness-tui --test deterministic_render_test`; `cargo test -p harness-tui onboarding -- --nocapture`; `cargo test -p harness auth -- --nocapture`; `env RUST_TEST_THREADS=1 cargo test -p harness-testkit --test pty_e2e`; `env RUST_TEST_THREADS=1 HARNESS_TUI_PTY_SIGNOFF=1 cargo test -p harness-tui --test pty_e2e`; `scripts/test-lanes.sh signoff-pty` | Manifest schema `harness-tui-signoff-manifest-v1` names all required flows, deterministic owners, PTY stages, and `signoff-native` as a documented env-gated gap when `HARNESS_NATIVE_VISUAL=1` plus `DISPLAY` are unavailable; onboarding/auth has deterministic CLI and render owners. Reference-image comparison is explicitly not required for this PRD. | 2026-05-30 pre-V1 enhancements WS6 | PASS |
| OpenAI-compatible provider is the supported execution path. | docs/test | `docs/provider-support.md` | `cargo test -p harness --test config_docs_reference_test` | Provider categories documented; broader providers are metadata/reference. | deterministic docs | PASS |
| Release-facing speed/performance claims require fresh artifacts. | limitation | `docs/budgets.md` | `scripts/test-lanes.sh perf` | Final production-class ratification is final-slice; current local budgets are provisional. | local perf lane | PASS |
| Release-blocking lanes are source-derived from `scripts/test-lanes.sh` and classified separately from local development aids. | docs-reference test | `docs/release-blockers.md`; `crates/harness/tests/test_lanes_script_test.rs` | `cargo test -p harness --test test_lanes_script_test release_blocker_taxonomy_maps_categories_to_real_lanes` | Every blocker category maps to at least one declared lane; local aids and doctor-vs-roadmap scope are documented. | 2026-05-29 deterministic docs test | PASS |
| Perf budget evidence fails closed when required artifacts are missing or stale. | lane/script | `scripts/check-perf-artifacts.py`; `scripts/test-lanes.sh` | `scripts/test-lanes.sh perf` | Perf lane runs `perf_artifact_freshness` after nextest; checker requires `large-session-surfaces.json` schema, timestamp, measurements, and provenance. | 2026-05-29 local perf gate | PASS |
| Prompt, permission, compaction, and skill tests use faux/mock providers by default. | test policy | `scripts/check-test-suite-gates.py` | `scripts/test-lanes.sh quality-gates`; `python3 scripts/check-test-suite-gates.py --self-test`; temporary `HARNESS_LIVE_PROXY` deterministic-test probe | Quality-gates passed with `static_test_suite_gates` and `forbidden_branding` green; a temporary deterministic test that read `HARNESS_LIVE_PROXY` failed the `live-provider-env` gate. | 2026-05-29 quality-gates `target/test-lanes/20260529-130106/summary.txt` | PASS |
| Doctor roadmap/extension readiness is separate from runtime health. | CLI test | `crates/harness/src/doctor.rs`; `crates/harness/tests/config_schema_cli/02_doctor_cli_reports_shipped_orchestration_health_test.rs` | `cargo test -p harness --test config_schema_cli_test doctor_cli_json_reports_extension_roadmap_readiness_separately` | Doctor JSON contains `extension_roadmap_readiness` with `separate_from_runtime_health: true`, `runtime_effects_scope: "descriptor_only"` for typed extension manifests, final-slice/post-V1 seams, and no network probes. | 2026-05-30 strict V1 closeout G009 | PASS |
| Outside-repository smoke covers TUI startup and a tool-enabled mock path, not only doctor/config preflight. | env-gated lane | `crates/harness/tests/binary_smoke.rs`; `scripts/test-lanes.sh` | `scripts/test-lanes.sh signoff-binary` | Binary smoke runs PTY-backed `tui --mock --exit-on-finish` and deterministic `run --scenario golden_path`; artifacts include TUI stdout/stderr/status with `success=true` and tool prompt events. | 2026-05-30 signoff-binary | PASS |
| README support export claim is backed by the sessions export surface and redaction tests. | test/docs | `crates/harness/tests/replay_sessions_cli/08_sessions_export_test.rs` | `cargo test -p harness --test replay_sessions_cli_test sessions_export` | Support bundles include replay-derived metadata, doctor JSON, non-secret summaries, artifact indexes, redaction manifest, and secret scan status without raw secrets. | 2026-05-29 deterministic session export tests | PASS |
| README session inspection claim is backed by replay-derived model-visible session tools. | test/docs | `docs/sessions-and-replay.md`; `crates/harness-tools/src/session_tools.rs` | `cargo test -p harness --test config_docs_reference_test thin_v1_docs_cover_their_source_surfaces` | Docs-reference guard asserts `list`, `inspect`, `replay`, `continue`, `export`, `tree`, `fork`, and `clone`; session tools remain replay-derived and side-effect free. | 2026-05-29 docs-reference test | PASS |
| Provider credential abstraction and store support OAuth/API-key precedence without a new transport. | core/provider tests + docs | `crates/harness-core/src/auth.rs`; `crates/harness-providers/src/openai.rs`; `docs/pre-v1-enhancements-progress.md` | `cargo test -p harness-core auth -- --nocapture`; `cargo test -p harness-providers openai_compatible_uses_credential_source_before_static_api_key -- --nocapture` | Stored OAuth/API-key credentials resolve before `apiKeyEnv`/inline fallbacks, the OpenAI-compatible provider obtains bearer tokens through a credential source before static API keys, and no bespoke streaming transport is introduced. | 2026-05-30 pre-V1 enhancements WS3 | PASS |
| Stored provider credentials stay outside config and are excluded from support bundles/events. | core/session export tests + docs | `crates/harness-core/src/auth.rs`; `crates/harness/src/sessions.rs`; `docs/config.md`; `docs/pre-v1-enhancements-progress.md` | `cargo test -p harness-core auth -- --nocapture`; `cargo test -p harness --test replay_sessions_cli_test sessions_export_cli_excludes_stored_credentials_and_scans_for_leaks -- --nocapture` | Credential store files live under platform data `credentials/{authProvider}.json`, are atomically replaced with restrictive POSIX mode, support bundles include a manifest rather than secret material, and the export path fails closed if stored credential values leak. | 2026-05-30 pre-V1 enhancements WS3 | PASS |
| OAuth access-token refresh is single-flight and surfaces credential failures through provider credential categories. | core/provider tests | `crates/harness-core/src/auth.rs`; `crates/harness-providers/src/lib.rs`; `crates/harness-providers/src/openai.rs`; `docs/pre-v1-enhancements-progress.md` | `cargo test -p harness-core auth -- --nocapture`; `cargo check --workspace` | Expired stored OAuth tokens refresh once across concurrent requests, refreshed credentials persist, and provider credential-source failures map to credential-category errors instead of transport-specific paths. | 2026-05-30 pre-V1 enhancements WS3 | PASS |
| Codex OAuth uses mocked PKCE loopback/device-code flows and decorates the OpenAI-compatible transport. | core/provider tests + docs | `crates/harness-core/src/auth/codex.rs`; `crates/harness-providers/src/openai.rs`; `docs/pre-v1-enhancements-progress.md` | `cargo test -p harness-core codex -- --nocapture`; `cargo test -p harness-providers codex_auth_profile_rewrites_endpoint_and_adds_context_headers -- --nocapture` | PKCE verifier/challenge, state validation, callback errors/timeouts, device pending→exchange storage, JWT account-id extraction, Codex endpoint rewrite, bearer/account/session/request headers, and stale Authorization stripping are deterministic and secret-redacted. | 2026-05-30 pre-V1 enhancements WS4 | PASS |
| OpenAI-compatible prompt-cache parity is implemented with stable session cache keys and volatile-tail prompt ordering. | provider/prompt/TUI tests + docs | `crates/harness-providers/src/openai.rs`; `crates/harness/src/dynamic_prompt.rs`; `crates/harness-tui/src/app.rs`; `docs/pre-v1-enhancements-progress.md` | `cargo test -p harness-providers openai_compatible_ -- --nocapture`; `cargo test -p harness dynamic_prompt_keeps_volatile_environment_at_stable_prefix_tail -- --nocapture`; `cargo test -p harness-tui cache_read_write_tokens_render_as_separate_status_labels -- --nocapture` | Requests carry a clamped per-session `prompt_cache_key` except missing-session/retention-none cases, volatile date/branch metadata stays behind stable prompt content, and cache read/write token telemetry renders separately in the TUI. | 2026-05-30 pre-V1 enhancements WS1 | PASS |
| GitHub Copilot OAuth uses mocked device-code flow and decorates the OpenAI-compatible transport for public/Enterprise deployments. | core/provider tests + docs | `crates/harness-core/src/auth/copilot.rs`; `crates/harness-providers/src/openai.rs`; `docs/pre-v1-enhancements-progress.md` | `cargo test -p harness-core copilot -- --nocapture`; `cargo test -p harness-providers github_copilot_auth_profile_rewrites_public_and_enterprise_headers -- --nocapture` | Device-code pending/slow_down/success/error/timeout paths are deterministic, denied/expired/malformed paths store no credential, the direct GitHub-token bearer decision is recorded, Enterprise domains normalize to `copilot-api.<domain>`, and request decoration sets `x-initiator`, `Openai-Intent`, `Copilot-Vision-Request` only for media, and strips stale auth headers. | 2026-05-30 pre-V1 enhancements WS5 | PASS |
| Authenticated Codex/Copilot credentials activate provider models and prompt routing without editing project config. | runtime/TUI/CLI tests + docs | `crates/harness/src/runtime_catalog.rs`; `crates/harness/src/models.rs`; `crates/harness/src/prompt.rs`; `crates/harness/src/tui.rs`; `crates/harness-tui/tests/model_switcher/palette_test.rs`; `docs/auth-model-parity-progress.md` | `cargo test -p harness runtime_catalog -- --nocapture`; `cargo test -p harness no_config_tui -- --nocapture`; `cargo test -p harness prompt::tests::no_config_prompt -- --nocapture`; `cargo test -p harness models::tests::no_config_models -- --nocapture`; `cargo test -p harness-tui model_switcher -- --nocapture`; `cargo test -p harness-tui no_provider_prompt_submission -- --nocapture` | Stored Codex/Copilot credentials produce built-in `openai-codex` and `github-copilot` runtime catalogs, no-provider no-config state blocks prompt submission with connect guidance, `/model` groups/searches provider-labelled rows, model switches carry selected provider/model metadata, persisted recent selections restore when valid, and auth-refresh opens model selection without restart. | 2026-05-31 auth-model parity | PASS |
| Auth management is available outside first-run onboarding and does not print credentials. | CLI/TUI tests + docs | `crates/harness/src/auth_cmd.rs`; `crates/harness-tui/src/keybindings.rs`; `crates/harness-tui/src/app/session_navigation.rs`; `docs/config.md`; `docs/pre-v1-enhancements-progress.md` | `cargo test -p harness auth -- --nocapture`; `cargo test -p harness-tui auth -- --nocapture` | `harness auth login/list/logout` work in-process with deterministic API-key stdin and mocked OAuth storage, list/doctor output redacts secrets/account ids, logout preserves config/env fallbacks, and `/auth` plus `/login`/palette `Auth` emit the same auth-manager UI intent mid-session. | 2026-05-30 pre-V1 enhancements WS6 | PASS |
| First-run onboarding is skippable, redacted, and suppressed for preconfigured auth paths. | TUI render/view-model tests + auth readiness test | `crates/harness-tui/src/app/onboarding.rs`; `crates/harness-tui/src/ui_lifecycle.rs`; `crates/harness/src/auth_cmd.rs`; `docs/pre-v1-enhancements-progress.md` | `cargo test -p harness-tui onboarding -- --nocapture`; `cargo test -p harness auth -- --nocapture` | The onboarding inventory covers provider/auth method, Codex browser/device, Copilot public/enterprise, API-key, success/error/skip/first-prompt, and skill-selection screens with Harness branding and redacted placeholders; skipping is launch-local and writes no credential intent; stored/env/inline credentials suppress onboarding. | 2026-05-30 pre-V1 enhancements WS6 | PASS |
| Non-GPT family prompt bodies are data-backed, branding-free, and drift-guarded. | prompt drift/golden tests + doctor readiness test | `.agent-harness/prompt-families/*.md`; `crates/harness/src/dynamic_prompt.rs`; `crates/harness/src/doctor.rs`; `docs/pre-v1-enhancements-progress.md` | `cargo test -p harness family_prompt -- --nocapture`; `cargo test -p harness --test bootstrap_profiles_test shipped_v1_family_prompt_assets_match_golden_snapshots -- --nocapture`; `cargo test -p harness --test config_schema_cli_test prompt_family_asset -- --nocapture` | Anthropic/Gemini/Kimi/Trinity prompt families load from `.agent-harness/prompt-families/{family}.md`, golden snapshots cover composed prompts, drift tests reject missing/empty/forbidden-branding assets, and doctor reports a default-prompt fallback warning for missing assets. | 2026-05-30 pre-V1 enhancements WS7 | PASS |

Rows must include an evidence pointer and verification command before a roadmap box can be flipped. Empty evidence or stale artifacts fail closeout.

## Maintained claim phrase list

The drift guard tracks a small set of real release-facing phrases rather than a broad debt baseline:

- first source build
- deterministic mocked execution
- local readiness only
- native tool catalog
- OpenAI-compatible provider
- support export
- session inspection
- release-blocking lanes

## Freshness policy

Lane artifact rows must name the lane, command, timestamp, artifact root, and observed pass/fail status. A row that points to a missing artifact or a command that has not been rerun for the current slice is stale and cannot back a checked box.

## Limitations policy

If a claim is not backed yet, either soften the claim in README/docs or mark the matrix row as an explicit documented limitation. Limitations are acceptable; unverifiable success claims are not.

## Review checklist

Before final closeout, compare README and public docs against this table, run the docs-reference tests, and update the progress log with the observed result for every checked PRD or roadmap box.
